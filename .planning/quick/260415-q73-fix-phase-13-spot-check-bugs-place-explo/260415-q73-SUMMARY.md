# Quick Task 260415-q73: Fix Phase 13 Spot-Check Bugs — Summary

**Completed:** 2026-04-16
**Commits:** bfb642d, 54375f2, b95cf84, bcd7c6d

## What was done

### Verified (already correct, no changes needed)

1. **precompute-links.js** — Already outputs `latitude`/`longitude` (not `lat`/`lon`), `place_code`, and `has_tgn` in place-index.json
2. **places.js** — Already has `place_code` fallback (`nl-{id}`) for lugar.njk permalink
3. **place-explorer.js** — Already loads place-index.json without field aliasing, uses `has_wikidata`/`has_tgn`/`has_whg`/`has_hgis` for authority facet, uses `place_code` for card links, extracts `place_code` from Pagefind URLs for map sync
4. **Viewport filter** — Correctly excludes coordinate-less places, shows "Sin resultados" when 0 matches, recomputes scoped facet counts from in-bounds places
5. **Checkbox closures** — Type uses IIFE correctly; coords/authority aren't in loops so no closure issue
6. **Auto-update on pan/zoom** — `moveend` handler correctly debounces and calls `this.search()`
7. **lugar.njk** — Already uses `permalink: "/{{ place.place_code }}/"`
8. **Pagefind places glob** — Already `nl-*/**/*.html` in all three build configs

### Fixed

1. **renderWorldCopies: false** (bfb642d) — Added to MapLibre map init in both `place-explorer.js` and `place.js` to prevent South American markers appearing duplicated over Australia/Pacific
2. **Entity pagefind glob** (54375f2) — Changed from `entidad/**/*.html` to `ne-*/**/*.html` in package.json, build.sh, and deploy.yml to match current entity permalink pattern
3. **lugar.njk permalink** (b95cf84) — Changed from `nl-{{ place.id }}` to `{{ place.place_code }}`; added Protomaps API key to site.js
4. **Entity explorer facets** (bcd7c6d) — Moved date facet above primary function in both sidebar render paths; added scoped facet counts to overload guard path

### Build verification

- `node scripts/precompute-links.js` — 6,918 place-index records with correct fields
- `DEV_MODE=true npx @11ty/eleventy` — 310 pages, place pages at `nl-*` paths
- `npx pagefind --site _site --output-subdir pagefind-places --glob "nl-*/**/*.html"` — indexed successfully
- `npx pagefind --site _site --output-subdir pagefind-entities --glob "ne-*/**/*.html"` — 78,476 pages indexed
- Main pagefind index — 802,731 words indexed

## Entity explorer verification notes

- **Date facet order**: Both `renderSidebarFacets()` (line 1078) and `renderSearchResults` sidebar builder (line 1256) render entity_type → date tree → primary_function. Correct.
- **Overload scoped counts**: Overload path (line 316) runs `this.pagefind.search()` with active filters and passes `overloadSearch.filters` to both `renderSidebarFacets` and `renderSearchResults`. Correct.
