---
context: phase
phase: 10-explorer-ux-redesign
task: NEW
total_tasks: 4
status: not_started
last_updated: 2026-04-05T20:32:39.936Z
---

<current_state>
Phase 10, Plan 04 is effectively complete — graph rendering and visual verification were fixed in commit 3b83d5e. However, the user has decided on a major UX pivot: the entity explorer should switch from the full-width graph-above-results layout to a three-column layout based on the Figma "EntityExplorerAltPage" design. This requires new planning and implementation work beyond the original Plan 04 scope.

The original Plan 04 tasks (dead code removal + visual verification) are done. What follows is new work driven by the user's design review.
</current_state>

<completed_work>

Plans 10-01 through 10-03: Complete (Figma design, pipeline fixes, CuratedEntityGraph class).
Plan 10-04 Task 1: Dead code removal — committed (30cf68d).
Plan 10-04 Task 2: Visual verification — fixed and committed (3b83d5e):
  - Precompute script: percentile-based normalisation (5th/95th percentile bounds, outlier clamping)
  - Graph renderer: nodeCanvasObject with labels, hover highlighting, logarithmic node sizing
  - Click tooltips: entity type, name, code, connection counts, detail/exploration links
  - Tooltip positioning: appended to .graph-panel (not .graph-canvas) to avoid overflow:hidden clipping
  - CSS: graph-tooltip classes restored in main.css (had been stripped)
  - Build scripts: npm build/build:dev now produce all three Pagefind indices
  - Engine: cooldownTime(500) + d3AlphaDecay(0.5) + onEngineStop(zoomToFit) for reliable fitting

Extra commits (not in phase plan, done in prior session):
  - Nav dropdown: Explorar → Documentos/Entidades/Lugares
  - URL changes: /entidades/ and /lugares/ top-level paths
</completed_work>

<remaining_work>

## New tasks (UX pivot to three-column layout with ego-subset graph)

1. **Three-column layout** — Restructure entidades.njk from full-width-graph-above to:
   - Left: sidebar filters (260px, existing)
   - Centre: results list (flex: 1, existing)
   - Right: sticky graph panel (400px, height: calc(100vh - 160px), top: 80px)
   - CSS changes in input.css for three-column layout

2. **Ego-subset graph** — Rewrite CuratedEntityGraph to show ~15-25 nodes:
   - Pick one focal entity (most-connected) on page load
   - Filter curated graph JSON client-side: focal node + its direct neighbours only
   - Render in the compact right panel instead of the full 100-node hairball
   - Keep curated-entity-graph.json as the full data source

3. **Infinite exploration + results sync** — The core UX:
   - Click a node → it becomes the new focal entity → graph refocuses showing its neighbours
   - Results list updates to show entities currently in/related to the graph
   - Filter pills reflect current exploration state
   - Pagefind search still works alongside (type a name → find entity → click → graph refocuses)
   - This is the key difference from entity detail page graphs: here exploration is open-ended

4. **Expand/collapse toggle** — Option to switch between:
   - Compact: sticky right panel (alt Figma design, default)
   - Expanded: full-width panel above results (main Figma design)

5. **Full build + E2E verification** — Full Eleventy build (~14 min) to verify Pagefind entity index + facets work, then complete visual verification of the new layout
</remaining_work>

<decisions_made>

Session fixes (committed 3b83d5e):
- Percentile-based normalisation instead of min/max — prevents outliers stretching the bounding box
- nodeCanvasObject rendering with log10 node sizing — matches entity.js UX pattern
- Tooltip on .graph-panel not .graph-canvas — avoids overflow:hidden clipping
- cooldownTime(500) + onEngineStop for reliable zoomToFit — cooldownTicks(1) was unreliable
- Build scripts produce all three Pagefind indices locally — matches CI workflow

UX pivot decisions (not yet implemented):
- MAJOR: Switch from full-width graph above results to three-column layout (Figma alt design)
- 100 nodes with 1428 edges is a hairball — show ego-subset of ~15-25 nodes focused on one entity
- Graph becomes an open-ended discovery tool — click to refocus, infinite exploration
- Graph drives results list — filter pills and results update as you explore
- The explorer graph's key differentiator from entity detail page: exploration never ends here
</decisions_made>

<blockers>
None. All prior blockers (graph centering, tooltip CSS) resolved.
</blockers>

## Required Reading (in order)
1. `src/js/curated-entity-graph.js` — current state after fixes: nodeCanvasObject rendering, tooltip system, hover highlighting
2. `src/js/entity.js` lines 319–426 and 475–620 — proven tooltip/interaction patterns to maintain consistency
3. `src/explorar/entidades.njk` — current template (needs restructuring to three-column)
4. `src/css/input.css` lines 2937–3076 — graph panel and tooltip CSS
5. `scripts/precompute-curated-graph.js` — precompute with percentile normalisation (data source stays, client filters it)
6. Figma alt design: `EntityExplorerAltPage.tsx` in Make file `LBeOKVLOkLXsJRe6zYdLyt` — access via `get_design_context` (empty nodeId) then `ReadMcpResourceTool` with `file://figma/make/source/LBeOKVLOkLXsJRe6zYdLyt/src/app/pages/EntityExplorerAltPage.tsx`
7. Figma main design: same file, `EntityExplorerPage.tsx` — the full-width layout for expanded mode

## Infrastructure State
- Eleventy dev server: stopped (kill any lingering processes on port 8080)
- Full build requires `NODE_OPTIONS="--max-old-space-size=8192"`
- DEV_MODE=true gives 310-page build (no entity Pagefind index)
- Live site: pagefind-entities and pagefind-places both 404 — will appear after next deploy
- http-server can serve _site/ for quick testing without rebuilding Eleventy
- Tailwind CSS: recompile with `npx @tailwindcss/cli -i src/css/input.css -o src/css/main.css` after CSS changes

<context>
The user reviewed the working graph (100 nodes, proper rendering, tooltips) and said it looked much better, but realised the fundamental problem: showing all 100 nodes with 1428 edges is pointless — it's a dense hairball that tells you nothing. The Figma "alt" design (EntityExplorerAltPage) puts the graph as a compact sticky right panel showing ~30 nodes, while the "main" design (EntityExplorerPage) is the full-width panel above results.

The user wants the alt as default, focused on one entity's ego-network (~15-25 nodes), with the option to expand. The critical insight: this explorer graph should enable infinite exploration (entity → neighbours → click another → new neighbours → keep going) unlike the entity detail page graph which is a dead end. The results list and filter pills should follow the graph state, creating a proper interconnected discovery interface.

This is essentially a new sub-plan within Phase 10 — the original Plan 04 verification work is done, and this is a design-driven pivot.
</context>

<next_action>
Start with: Read the Figma EntityExplorerAltPage.tsx source (via get_design_context + ReadMcpResourceTool on Make file LBeOKVLOkLXsJRe6zYdLyt) to confirm the three-column layout spec. Then restructure entidades.njk and the CSS to move the graph panel from full-width above results to a sticky right column. Test the layout before tackling the ego-subset filtering logic.
</next_action>
