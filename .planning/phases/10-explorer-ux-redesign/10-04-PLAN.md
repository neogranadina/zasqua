---
phase: 10-explorer-ux-redesign
plan: 04
type: execute
wave: 3
depends_on: [10-03]
files_modified:
  - scripts/precompute-bipartite-graph.js
  - scripts/precompute-cooccurrence.js
  - data/entity-doc-graph.json
  - data/entity-cooccurrence.json
  - eleventy.config.js
  - .github/workflows/deploy.yml
  - src/js/entity-network-graph.js
autonomous: false
requirements: [EXP-08, EXP-10]

must_haves:
  truths:
    - "Dead code from Phase 9 graph approach is removed — no bipartite precompute, no co-occurrence precompute, no entity-doc-graph.json passthrough"
    - "CI pipeline no longer runs removed precompute scripts"
    - "entity-network-graph.js is removed (replaced by curated-entity-graph.js in Plan 03)"
    - "User has verified both explorers work correctly in the browser"
  artifacts:
    - path: "eleventy.config.js"
      provides: "Clean passthrough copies — entity-doc-graph.json removed"
    - path: ".github/workflows/deploy.yml"
      provides: "Clean CI pipeline — old precompute steps removed"
  key_links:
    - from: "eleventy.config.js"
      to: "data/curated-entity-graph.json"
      via: "addPassthroughCopy (only curated graph, not old bipartite)"
      pattern: "curated-entity-graph"
---

<objective>
Clean up Phase 9 graph dead code (D-15) and verify both explorers work correctly end-to-end.

Purpose: Remove the old bipartite graph precompute script, co-occurrence precompute script, entity-doc-graph.json, entity-cooccurrence.json, and entity-network-graph.js. These are all superseded by the curated graph approach. Then verify both explorers visually.
Output: Clean codebase with no dead graph code, both explorers verified by the user.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-explorer-ux-redesign/10-CONTEXT.md
@.planning/phases/10-explorer-ux-redesign/10-02-SUMMARY.md
@.planning/phases/10-explorer-ux-redesign/10-03-SUMMARY.md
</context>

<threat_model>
ASVS Level 1 — file deletion and CI cleanup only. No new code paths, no user input handling, no secrets.
No threats identified.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Remove Phase 9 graph dead code and CI references</name>
  <files>scripts/precompute-bipartite-graph.js, scripts/precompute-cooccurrence.js, data/entity-doc-graph.json, data/entity-cooccurrence.json, src/js/entity-network-graph.js, eleventy.config.js, .github/workflows/deploy.yml</files>
  <read_first>
    - eleventy.config.js (lines 1-25 — identify entity-doc-graph.json passthrough to remove)
    - .github/workflows/deploy.yml (lines 50-65 — identify precompute-cooccurrence.js step to remove)
    - src/explorar/entidades.njk (confirm entity-network-graph.js is no longer referenced — should be gone after Plan 03)
    - src/entidad.njk (confirm entity.js does not import from entity-network-graph.js)
  </read_first>
  <action>
**Delete files (D-15, EXP-08):**
1. `rm scripts/precompute-bipartite-graph.js` — superseded by precompute-curated-graph.js
2. `rm scripts/precompute-cooccurrence.js` — co-occurrence data no longer needed; curated graph uses its own co-occurrence computation internally
3. `rm data/entity-doc-graph.json` — output of the old bipartite precompute, no longer loaded by any code
4. `rm data/entity-cooccurrence.json` — output of old co-occurrence precompute, no longer loaded by any code
5. `rm src/js/entity-network-graph.js` — replaced by curated-entity-graph.js (Plan 03)

**Update eleventy.config.js:**
Remove the passthrough copy for entity-doc-graph.json (line 20):
```javascript
// DELETE THIS LINE:
eleventyConfig.addPassthroughCopy({ "data/entity-doc-graph.json": "data/entity-doc-graph.json" });
```
Also remove the comment on line 19 (`// Entity-document bipartite graph (loaded by network graph)`).

Keep the `curated-entity-graph.json` passthrough added in Plan 02.

**Update .github/workflows/deploy.yml:**
Remove the co-occurrence precompute steps (lines 58-59):
```yaml
# DELETE THESE LINES:
          node scripts/precompute-cooccurrence.js
          ls -lh data/entity-cooccurrence.json
```

Keep the `node scripts/precompute-curated-graph.js` step added in Plan 02.
Keep the `node scripts/precompute-links.js` step (line 51) — that produces entity/place link shards which are still used by detail pages and ego-network expansion.

**Verify no remaining references:**
After deletions, grep for any remaining references to the deleted files:
- `grep -r "entity-network-graph" src/` should return 0 matches
- `grep -r "entity-doc-graph" .` (excluding .git, node_modules, .planning) should return 0 matches
- `grep -r "precompute-cooccurrence" .` (excluding .git, node_modules, .planning) should return 0 matches
- `grep -r "precompute-bipartite" .` (excluding .git, node_modules, .planning) should return 0 matches
- `grep -r "entity-cooccurrence" .` (excluding .git, node_modules, .planning, data/) should return 0 matches
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && test ! -f scripts/precompute-bipartite-graph.js && test ! -f scripts/precompute-cooccurrence.js && test ! -f data/entity-doc-graph.json && test ! -f data/entity-cooccurrence.json && test ! -f src/js/entity-network-graph.js && grep -c "entity-doc-graph" eleventy.config.js; grep -c "precompute-cooccurrence" .github/workflows/deploy.yml; echo "--- remaining refs ---" && grep -r "entity-network-graph\|entity-doc-graph\|precompute-cooccurrence\|precompute-bipartite" src/ eleventy.config.js .github/workflows/deploy.yml 2>/dev/null; echo "done"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/precompute-bipartite-graph.js` does not exist
    - `scripts/precompute-cooccurrence.js` does not exist
    - `data/entity-doc-graph.json` does not exist
    - `data/entity-cooccurrence.json` does not exist
    - `src/js/entity-network-graph.js` does not exist
    - `eleventy.config.js` does NOT contain `entity-doc-graph` (grep returns 0)
    - `.github/workflows/deploy.yml` does NOT contain `precompute-cooccurrence` (grep returns 0)
    - No remaining references to deleted files in `src/`, `eleventy.config.js`, or `.github/workflows/deploy.yml`
  </acceptance_criteria>
  <done>All Phase 9 graph dead code removed — scripts, data files, passthrough copies, and CI steps</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of both explorers</name>
  <files>src/explorar/entidades.njk, src/explorar/lugares.njk, src/js/curated-entity-graph.js, src/js/place.js, src/entidad.njk</files>
  <action>
Start the local dev server and present the user with verification steps for all Phase 10 changes across both explorers, place detail pages, and entity detail pages. Run `npx @11ty/eleventy --serve` and provide the user with the URLs and expected behaviours listed in the verify section. Wait for user approval.

Both explorer pages have been updated:

**Entity explorer (`/explorar/entidades/`):**
- Curated graph panel showing ~100 most-connected entities with ForceAtlas2 layout
- Graph loads from a single JSON file (no per-shard fetches, no browser crash)
- Click a node once to expand its 1-hop ego-network
- Click an expanded node again to navigate to its entity detail page
- Deep-link: add `?nodo=entity_code` to the URL to centre on a specific entity
- Dynamic entity count in intro text (from build-time data)
- Graph and search/filter results list are independent

**Place explorer (`/explorar/lugares/`):**
- Dynamic place count in intro text (shows 7,068, not 8,177)
- All other functionality unchanged

**Place detail pages (`/lugar/{name}/`):**
- Map tiles now load from OpenFreeMap (was broken Protomaps CDN)

**Entity detail pages (`/entidad/{code}/`):**
- New "Explorar en la red de entidades" link in Control section
  </action>
  <verify>
User performs the following checks:
1. Visit the entity explorer page — graph panel loads without crashing, ~100 nodes visible
2. Click a node — it expands to show neighbours
3. Click an expanded node — navigates to entity detail page
4. Test deep-linking: `/explorar/entidades/?nodo={known_code}` centres on that entity
5. Test fallback: `/explorar/entidades/?nodo=nonexistent_code` shows fallback message
6. Visit entity detail page — "Explorar en la red de entidades" link present
7. Visit place explorer — count is correct (not 8.177)
8. Visit place detail page with coordinates — map loads with basemap tiles visible
  </verify>
  <done>User has approved visual verification of both explorers, map fix, deep-linking, and ego-network expansion. Phase 10 implementation is complete.</done>
</task>

</tasks>

<verification>
1. No dead graph files remain in scripts/, data/, or src/js/
2. CI pipeline has no references to removed scripts
3. User has verified both explorers and the map fix work correctly
</verification>

<success_criteria>
- All 5 dead code files removed
- eleventy.config.js and deploy.yml cleaned of old references
- User approves visual verification of both explorers, map fix, and deep-linking
</success_criteria>

<output>
After completion, create `.planning/phases/10-explorer-ux-redesign/10-04-SUMMARY.md`
</output>
