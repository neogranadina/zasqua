---
phase: 02-component-updates
plan: 01
subsystem: ui
tags: [tailwind, css, tokenisation, design-tokens, stone-scale, periwinkle, burgundy]

# Dependency graph
requires:
  - phase: 01-css-foundations
    provides: "@theme block with all brand and stone-scale tokens in input.css"
provides:
  - "Header fully tokenised — stone tokens replace all hardcoded hex; periwinkle border-bottom nav hover applied (D-01)"
  - "Masonry overlay updated from burgundy-deep to primary burgundy rgba(139,41,66,0.85) (D-06)"
  - "Hero, home sections, buttons, breadcrumb, detail page, description list, placeholder image, utilities, and error page tokenised"
  - "Footer and warm-white background verified as complete from Phase 1"
affects:
  - 02-02-search-page
  - 02-03-description-repo-pages

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Use @apply text-stone-N / @apply bg-stone-N for color/background-color inside @layer components"
    - "Use var(--color-stone-N) for border shorthand properties (not @apply)"
    - "Preserve #fff / rgba values as-is — no token replacement needed for white or semi-transparent raw values"

key-files:
  created: []
  modified:
    - src/css/input.css

key-decisions:
  - "D-01: Nav hover/active uses border-bottom: 2px solid var(--color-periwinkle) with padding-bottom: calc(0.5rem - 2px) to preserve layout"
  - "D-06: .repo-overlay updated from rgba(107,31,51,0.85) (burgundy-deep) to rgba(139,41,66,0.85) (primary burgundy #8B2942)"
  - "Footer and warm-white background confirmed complete from Phase 1 — no changes needed in this plan"
  - "#ededed (placeholder-img border) mapped to var(--color-stone-200) — closest token match"
  - "#d8d8d8 (btn-secondary hover) mapped to var(--color-stone-300) — closest token match"

patterns-established:
  - "Pattern: @apply text-stone-N inside @layer components works in Tailwind v4 — confirmed via successful ./tailwindcss compilation"
  - "Pattern: var(--color-stone-N) works for border shorthand — stone scale CSS vars auto-generated by Tailwind v4"

requirements-completed:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-08

# Metrics
duration: 15min
completed: 2026-03-25
---

# Phase 02 Plan 01: CSS Tokenisation (Header through Error Page) Summary

**Stone-scale tokenisation of header, hero, masonry, footer (verify), buttons, breadcrumb, cards, detail page, description list, placeholder image, utilities, and error page sections of input.css — with periwinkle nav hover underline (D-01) and primary burgundy masonry overlay (D-06)**

## Performance

- **Duration:** ~15 min
- **Started:** 2026-03-25T00:55:00Z
- **Completed:** 2026-03-25T01:10:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Header section (lines 77-239) fully tokenised: all `#dedede`, `#1C1917`, `#A8A29E`, `#f0f0f0` replaced with stone token references; D-01 periwinkle border-bottom nav hover applied
- Masonry overlay updated from burgundy-deep rgba to primary burgundy rgba(139,41,66,0.85) per D-06
- Hero through error page sections tokenised: zero non-white hardcoded hex values remain in lines 241-917
- Footer and warm-white background verified as already complete from Phase 1 — no rework needed
- Tailwind CSS compiled successfully (`./tailwindcss -i src/css/input.css -o /tmp/test-main.css` exits 0)

## Task Commits

Each task was committed atomically:

1. **Task 1: Tokenise header section + apply D-01 nav hover underline** - `2e37f05` (chore)
2. **Task 2: Tokenise hero through error page + apply D-06** - `3d71a89` (chore)

## Files Created/Modified

- `src/css/input.css` — Hardcoded hex values in lines 61-917 replaced with Tailwind stone token references and brand token variables; D-01 and D-06 interactive state rules applied

## Decisions Made

- D-01 implemented as `border-bottom: 2px solid var(--color-periwinkle)` with `padding-bottom: calc(0.5rem - 2px)` — preserves nav link vertical rhythm (the nav link has `padding: 0.5rem 0`; subtracting 2px for the border keeps the element height constant)
- `#ededed` in `.placeholder-img` mapped to `var(--color-stone-200)` — no exact token match, stone-200 is the closest neutral
- `#d8d8d8` in `.btn-secondary:hover` mapped to `var(--color-stone-300)` — closest tone approximation within the stone scale

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None — Tailwind CSS compiled without errors after all changes. Stone-scale CSS vars (`--color-stone-N`) resolved correctly via auto-generation by Tailwind v4.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Plan 02-02 (search page) can begin: active filter pills (D-07), active pagination (D-08), sort/facet stone tokenisation (D-09)
- Plan 02-03 (description and repository pages) can begin: Miller columns tokenisation, description field link colour (D-10), TIFY viewer overrides
- No blockers from this plan

---
*Phase: 02-component-updates*
*Completed: 2026-03-25*
