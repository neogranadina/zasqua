---
phase: 02-component-updates
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - src/css/input.css
autonomous: false
requirements:
  - COMP-06
  - COMP-07

must_haves:
  truths:
    - "Miller columns section uses stone tokens instead of hardcoded hex, selection state unchanged per D-11"
    - "Description page metadata headers tokenised per D-12, detail-field links keep dark stone-900 default with burgundy-light hover per D-10"
    - "TIFY viewer overrides tokenised with all !important flags preserved"
    - "Children tree section uses stone tokens instead of hardcoded hex"
    - "Entire input.css has no remaining hardcoded hex outside @theme, #fff/#ffffff/white, rgba values, and the few documented exceptions"
  artifacts:
    - path: "src/css/input.css"
      provides: "Fully tokenised CSS for the entire site"
      contains: "var(--color-stone-"
  key_links:
    - from: "src/css/input.css (.detail-field a)"
      to: "@layer base a:hover"
      via: "inherited hover colour (burgundy-light)"
      pattern: "detail-field a"
    - from: "src/css/input.css (TIFY overrides)"
      to: "var(--color-stone-*) !important"
      via: "CSS specificity override"
      pattern: "!important"
---

<objective>
Tokenise all hardcoded hex values in the Miller Columns Tree, Description Page, and Children Tree sections of input.css (lines ~1735-2454). Apply D-10 (description links stay dark, burgundy on hover), D-11 (Miller selection unchanged), and D-12 (metadata headers tokenised). Complete final hex audit of entire file. Visual verification of all page types.

Purpose: Completes the tokenisation pass across the entire codebase -- after this plan, no hardcoded hex values remain outside of @theme declarations, white values, rgba raw values, and documented exceptions.
Output: Fully tokenised input.css ready for visual verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-updates/02-CONTEXT.md
@.planning/phases/02-component-updates/02-UI-SPEC.md
@.planning/phases/02-component-updates/02-02-SUMMARY.md

@src/css/input.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenise Miller columns, description page, TIFY overrides, and children tree</name>
  <files>src/css/input.css</files>
  <read_first>
    - src/css/input.css (full file -- must see current state after Plans 01 and 02)
    - .planning/phases/02-component-updates/02-UI-SPEC.md (description page and repository page component contracts)
    - .planning/phases/02-component-updates/02-RESEARCH.md (D-10/D-11/D-12 specs, pitfall 5 about TIFY !important)
  </read_first>
  <action>
Replace all hardcoded hex values in the Miller Columns Tree (lines ~1735-1999), Description Page (lines ~2000-2321), and Children Tree (lines ~2322-2454) sections of src/css/input.css.

**Miller Columns Tree (~1735-1999) -- D-11: selection state stays as-is:**

Token replacements:
- `border: 1px solid #dedede` -> `border: 1px solid var(--color-stone-300)` (container, column borders)
- `background: #fff` -> keep as-is
- `border-right: 1px solid #dedede` -> `border-right: 1px solid var(--color-stone-300)`
- `color: #1C1917` -> `@apply text-stone-900` (header text, item text)
- `background: #F5F5F4` -> `@apply bg-stone-100` (column headers)
- `border-bottom: 1px solid #dedede` -> `border-bottom: 1px solid var(--color-stone-300)` (headers)
- `border-bottom: 1px solid #f0f0f0` -> `border-bottom: 1px solid var(--color-stone-100)` (items)
- `color: #fff` -> keep as-is (selected item text)
- `color: #A8A29E` -> `@apply text-stone-400` (muted labels, no-children text)
- `color: #ccc` -> `color: var(--color-stone-300)` (arrow indicators)
- `border-top: 1px solid #ddd` -> `border-top: 1px solid var(--color-stone-300)` (info bar)
- `color: #78716C` -> `@apply text-stone-500` (info text)

D-11 compliance: Do NOT change `.miller-item.selected` background/colour, `.miller-item.selected-ancestor` background/colour, or the rgba values for selected-ancestor. These stay exactly as-is.

Mobile Miller overrides (`@media (max-width: 768px)`):
- `#e8e8e8` -> `@apply bg-stone-200`
- `#d8d8d8` -> `background: var(--color-stone-300)`
- `#333` -> `@apply text-stone-800`
- `#1C1917` -> `@apply text-stone-900`
- `#A8A29E` -> `@apply text-stone-400`
- `color: #fff` -> keep as-is
- `border: 1px solid #ccc` -> `border: 1px solid var(--color-stone-300)`

**Description Page (~2000-2321) -- D-10, D-12, D-13:**

D-13 (already done -- verify): `.level-badge` should already use `background: var(--color-periwinkle)`. Do not change. `#b8c4f0` border on level-badge -> keep as-is (no matching token, documented exception).

D-12 (metadata section headers -- tokenise):
- `color: #1C1917` -> `@apply text-stone-900`
- `background: #fafafa` -> `@apply bg-stone-50` (or keep as very close to --color-bg)
- `border: 1px solid #dedede` -> `border: 1px solid var(--color-stone-300)`
- `color: #A8A29E` -> `@apply text-stone-400`
- `color: #78716C` -> `@apply text-stone-500`

D-10 (description page links -- Claude's discretion resolved as selective):
- `.detail-field a` (line ~2104 area): `color: #1C1917` -> `@apply text-stone-900` (keep dark default, burgundy-light comes from `@layer base a:hover` already)
- `.detail-field a` `text-decoration-color: #ccc` -> `text-decoration-color: var(--color-stone-300)`
- Do NOT change `.detail-field a` to `text-burgundy` -- keep it as `text-stone-900` for default state. The existing `@layer base a:hover { @apply text-burgundy-light underline }` provides the hover treatment.

Additional description page tokenisation:
- `background: #F5F5F4` -> `@apply bg-stone-100`
- `background-color: #f5f5f5` -> `@apply bg-stone-100`
- `background: #fff` -> keep as-is

**TIFY Viewer Overrides (~2182-2285 area):**

CRITICAL: Preserve ALL `!important` flags. TIFY injects its own CSS and overrides require `!important`.

- `background: #fff !important` -> keep as-is (white)
- `border-bottom: 1px solid #dedede !important` -> `border-bottom: 1px solid var(--color-stone-300) !important`
- `border: 1px solid #ddd !important` -> `border: 1px solid var(--color-stone-300) !important`
- `background: #f8f8f8 !important` -> `background: var(--color-stone-50) !important`
- `color: #78716C !important` -> `color: var(--color-stone-500) !important`
- `background: #fff !important` -> keep as-is
- `border-color: #bbb !important` -> `border-color: var(--color-stone-400) !important`
- `background: #000 !important` -> keep as-is (black viewer background)

Non-!important TIFY-adjacent elements:
- `border: 1px solid #ddd` -> `border: 1px solid var(--color-stone-300)`
- `background: #f8f8f8` -> `@apply bg-stone-50`
- `color: #78716C` -> `@apply text-stone-500`
- `background: #fff` -> keep as-is
- `border-color: #bbb` -> `border-color: var(--color-stone-400)`

**Children Tree (~2322-2454):**
- `border-bottom: 1px solid #f0f0f0` -> `border-bottom: 1px solid var(--color-stone-100)`
- `color: #A8A29E` -> `@apply text-stone-400`
- `color: #1C1917` -> `@apply text-stone-900`
- `border-left: 2px solid #f0f0f0` -> `border-left: 2px solid var(--color-stone-100)`
- `border-bottom: 1px solid #f5f5f5` -> `border-bottom: 1px solid var(--color-stone-100)`

Do NOT change any class names. tree.js uses `.miller-item`, `.miller-column-header`, `.selected`, `.selected-ancestor` etc.
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && npx @11ty/eleventy --dryrun 2>&1 | tail -3 && echo "---total-hex---" && grep -cE '#[0-9a-fA-F]{3,6}\b' src/css/input.css && echo "---important-preserved---" && grep -c '!important' src/css/input.css</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds (eleventy --dryrun exits 0)
    - Total hardcoded hex count in entire input.css is below 50 (down from ~192; remaining are @theme definitions, #fff/#ffffff, #000, rgba values, #b8c4f0 level-badge border, #e0dcd4 decorative border, #fff3cd warning)
    - `.miller-item.selected` and `.miller-item.selected-ancestor` rules are UNCHANGED from their pre-plan state (D-11)
    - `.level-badge` still contains `var(--color-periwinkle)` (D-13 verified)
    - All `!important` flags in TIFY section preserved (count should be same as before)
    - `.detail-field a` uses `text-stone-900` or `var(--color-stone-900)` for default colour (D-10)
    - `.detail-field a` uses `var(--color-stone-300)` for text-decoration-color
    - No class names changed (`.miller-item`, `.level-badge`, `.detail-field`, `.desc-viewer`, `.children-tree-*` all still present)
  </acceptance_criteria>
  <done>Miller columns, description page, TIFY overrides, and children tree fully tokenised. D-10, D-11, D-12 applied. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all four page types</name>
  <files>src/css/input.css</files>
  <action>
Build the site locally and present it for visual verification. Run `npx @11ty/eleventy --serve` to start the dev server. The user will review all four page types to confirm the visual identity update is complete and no regressions exist.
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && npx @11ty/eleventy --dryrun 2>&1 | tail -3</automated>
  </verify>
  <what-built>
Complete tokenisation of input.css across all sections:
- Header: periwinkle nav hover underline (D-01), stone token border/text (D-03)
- Homepage: masonry overlay primary burgundy (D-06), hero unchanged (D-04/D-05)
- Search page: burgundy active filter pills (D-07), periwinkle active pagination (D-08), stone scale sort/facets (D-09)
- Description page: dark stone-900 links with burgundy-light hover (D-10), metadata headers tokenised (D-12), level badges unchanged (D-13)
- Repository page: Miller columns tokenised, selection state unchanged (D-11)
- Footer: verified as already complete (COMP-02)
- Background: warm white verified (COMP-08)
- All ~192 hardcoded hex values replaced with token references
  </what-built>
  <how-to-verify>
Run a local build and open the site in browser:

```bash
cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev
npm run build
# or: npx @11ty/eleventy --serve
```

Then check each page type:

1. **Homepage** (localhost:8080/):
   - Header border is subtle grey (stone-300), not jarring
   - Nav links show periwinkle underline on hover, text stays dark
   - Masonry grid cards show burgundy overlay on hover
   - Hero section looks normal

2. **Search page** (localhost:8080/buscar/):
   - Apply a filter -- pill should be burgundy with white text
   - Navigate to page 2 -- active page number should have periwinkle background with dark text
   - Sort dropdown and facet labels should be neutral stone tones

3. **Description page** (any description URL):
   - Level badges are periwinkle (unchanged from Phase 1)
   - Links in metadata fields are dark stone-900, turn burgundy-light on hover
   - TIFY viewer (if present) header and controls look normal

4. **Repository page** (any repository URL):
   - Miller columns render with stone borders
   - Selection highlight unchanged from Phase 1

5. **General**:
   - Background is warm white (#FAFAF9) throughout
   - No blue accent colours visible anywhere
   - No jarring colour shifts or missing styles
  </how-to-verify>
  <acceptance_criteria>
    - User confirms all four page types render correctly
    - No visual regressions reported
    - All interactive states (nav hover, filter pills, pagination, masonry overlay) behave as expected
  </acceptance_criteria>
  <done>User has approved visual appearance of all page types</done>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
After both tasks:
1. Build passes
2. Total hardcoded hex count in input.css dramatically reduced from ~192
3. All 13 decisions (D-01 through D-13) implemented or verified
4. Visual review confirms no regressions across all page types
</verification>

<success_criteria>
- Miller columns tokenised, selection state preserved (D-11, COMP-07)
- Description page links dark with burgundy-light hover (D-10, COMP-06)
- Level badges verified as periwinkle (D-13, COMP-06)
- Metadata headers tokenised (D-12, COMP-06)
- TIFY overrides tokenised with !important preserved
- Children tree tokenised
- Full visual verification approved by user
- Entire input.css free of hardcoded hex values (except documented exceptions)
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-updates/02-03-SUMMARY.md`
</output>
