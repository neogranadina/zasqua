---
phase: 02-component-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/css/input.css
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-08

must_haves:
  truths:
    - "Header border, logo text, nav links, and search pill use stone token references instead of hardcoded hex"
    - "Nav link hover/active shows periwinkle underline with dark text instead of grey colour change"
    - "Masonry grid hover overlay uses primary burgundy rgba(139,41,66,0.85)"
    - "Hero, home sections, footer, buttons, breadcrumb, and cards sections have no hardcoded hex values (except #fff/white and rgba values without tokens)"
  artifacts:
    - path: "src/css/input.css"
      provides: "Tokenised CSS for header, hero, masonry, footer, buttons, breadcrumb, cards, and utilities"
      contains: "border-bottom: 2px solid var(--color-periwinkle)"
  key_links:
    - from: "src/css/input.css (.site-nav a:hover)"
      to: "var(--color-periwinkle)"
      via: "border-bottom property"
      pattern: "border-bottom.*periwinkle"
    - from: "src/css/input.css (.repo-overlay)"
      to: "rgba(139, 41, 66, 0.85)"
      via: "background property"
      pattern: "rgba.*139.*41.*66"
---

<objective>
Tokenise all hardcoded hex values in the header, hero, masonry grid, footer, buttons, breadcrumb, cards, detail page, repository header, description list, placeholder image, utilities, and error page sections of input.css. Apply the nav hover periwinkle underline (D-01) and masonry overlay burgundy update (D-06).

Purpose: Eliminates hardcoded hex values from the first half of input.css (lines 61-917) and applies two key interactive state decisions.
Output: input.css with tokenised values in all sections from Layout through Error Page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-updates/02-CONTEXT.md
@.planning/phases/02-component-updates/02-RESEARCH.md
@.planning/phases/02-component-updates/02-UI-SPEC.md

@src/css/input.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenise header section + apply D-01 nav hover underline</name>
  <files>src/css/input.css</files>
  <read_first>
    - src/css/input.css (full file -- executor must see current state)
    - .planning/phases/02-component-updates/02-UI-SPEC.md (component contracts)
    - .planning/phases/02-component-updates/02-RESEARCH.md (token replacement map and pitfall warnings)
  </read_first>
  <action>
Replace hardcoded hex values in the Header section (lines ~77-239) of src/css/input.css with stone token references per D-03. Apply D-01 nav hover underline.

Specific replacements in the Header section:

1. `.site-header` (line ~83): `border-bottom: 1px solid #dedede` -> `border-bottom: 1px solid var(--color-stone-300)`

2. `.site-logo-text` (line ~112): `color: #1C1917` -> `@apply text-stone-900`

3. `.site-nav a` (line ~137): `color: #1C1917` -> `@apply text-stone-900`

4. `.site-nav a:hover, .site-nav a.active` (lines ~141-145): Replace the entire rule with:
```css
.site-nav a:hover,
.site-nav a.active {
  @apply text-stone-900;
  text-decoration: none;
  border-bottom: 2px solid var(--color-periwinkle);
  padding-bottom: calc(0.5rem - 2px);
}
```
This implements D-01: periwinkle underline, text stays dark stone-900.

5. `.header-search` (line ~150): `border: 1px solid #dedede` -> `border: 1px solid var(--color-stone-300)`

6. `.header-search input` (line ~170 area): `color: #1C1917` -> `@apply text-stone-900`

7. `.header-search-btn` and related (line ~187 area): `color: #1C1917` -> `@apply text-stone-900`

8. Mobile menu rules: `border-top: 1px solid #dedede` -> `border-top: 1px solid var(--color-stone-300)`, `border-bottom: 1px solid #f0f0f0` -> `border-bottom: 1px solid var(--color-stone-100)`

Also tokenise any remaining `#A8A29E` -> `@apply text-stone-400` or `color: var(--color-stone-400)` in the header section.

NOTE: Preserve all existing comments, class names, and structural CSS. Only replace colour values. Do NOT change any class names used by JS (header.js uses `.hamburger-toggle`, `.site-nav`, `.nav-open`).
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && npx @11ty/eleventy --dryrun 2>&1 | tail -3 && echo "---" && grep -c '#dedede\|#1C1917\|#A8A29E\|#f0f0f0' src/css/input.css</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds (eleventy --dryrun exits 0)
    - src/css/input.css lines 77-239 contain zero instances of #dedede, #1C1917, #A8A29E, or #f0f0f0
    - src/css/input.css contains the string `border-bottom: 2px solid var(--color-periwinkle)` in the .site-nav a:hover rule
    - src/css/input.css contains `padding-bottom: calc(0.5rem - 2px)` in the .site-nav a:hover rule
    - src/css/input.css .site-header contains `var(--color-stone-300)` for border
    - No class names changed (`.site-header`, `.site-nav`, `.header-search`, `.hamburger-toggle` all still present)
  </acceptance_criteria>
  <done>Header section fully tokenised, nav hover shows periwinkle underline per D-01, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Tokenise hero, masonry, footer-verify, buttons through error page sections + apply D-06</name>
  <files>src/css/input.css</files>
  <read_first>
    - src/css/input.css (re-read after Task 1 changes)
    - .planning/phases/02-component-updates/02-UI-SPEC.md (token replacement map)
    - .planning/phases/02-component-updates/02-RESEARCH.md (masonry overlay D-06 spec)
  </read_first>
  <action>
Replace all hardcoded hex values in the following sections of src/css/input.css (lines ~241-917):

**Hero -- Parallax (lines ~241-358):**
- `color: #1C1917` -> `@apply text-stone-900`
- `color: #A8A29E` -> `@apply text-stone-400`
- `color: #fff` -> keep as-is (white has no token)

**Home Sections (lines ~359-404):**
- `color: #1C1917` -> `@apply text-stone-900`

**Masonry Grid (lines ~405-526) -- D-06:**
- `.repo-overlay` (line ~484): `background: rgba(107, 31, 51, 0.85)` -> `background: rgba(139, 41, 66, 0.85)` (primary burgundy #8B2942 at 0.85 opacity, per D-06)
- `color: #fff` -> keep as-is

**Footer (lines ~527-591):**
- Verify footer already uses token references from Phase 1. If any hardcoded hex remains, tokenise it. Footer should already have `var(--color-burgundy-dark)` -- just verify, do not rewrite.
- `color: #fff` -> keep as-is

**Buttons (lines ~592-645):**
- `color: #fff` -> keep as-is
- `background: #F5F5F4` -> `@apply bg-stone-100`
- `color: #1C1917` -> `@apply text-stone-900`
- `background: #d8d8d8` -> `background: var(--color-stone-300)` (close enough approximation)

**Breadcrumb (lines ~646-675):**
- `color: #A8A29E` -> `@apply text-stone-400`

**Cards (lines ~676-690):**
- No hardcoded hex values expected -- verify

**Detail Page (lines ~691-747):**
- `background: #F5F5F4` -> `@apply bg-stone-100`
- `color: #A8A29E` -> `@apply text-stone-400` or `color: var(--color-stone-400)`

**Repository Header (lines ~748-797):**
- `color: #A8A29E` -> `@apply text-stone-400` or `color: var(--color-stone-400)`
- `border-bottom: 1px solid #dedede` -> `border-bottom: 1px solid var(--color-stone-300)`
- `color: #1C1917` -> `@apply text-stone-900`
- `color: #78716C` -> `@apply text-stone-500`

**Description List (lines ~798-850):**
- `border: 1px solid #dedede` -> `border: 1px solid var(--color-stone-300)`
- `border-bottom: 1px solid #f0f0f0` -> `border-bottom: 1px solid var(--color-stone-100)`
- `color: #1C1917` -> `@apply text-stone-900`
- `background-color: #F5F5F4` -> `@apply bg-stone-100`
- `color: #A8A29E` -> `@apply text-stone-400`
- `border: 2px solid #ededed` -> `border: 2px solid var(--color-stone-200)` (closest stone match)

**Placeholder Image (lines ~851-869):**
- Tokenise any hardcoded values present

**Utilities (lines ~870-880):**
- `.text-muted { color: #A8A29E }` -> `.text-muted { @apply text-stone-400 }`
- `.bg-gray { background-color: #F5F5F4 }` -> `.bg-gray { @apply bg-stone-100 }`

**Error Page (lines ~881-917):**
- `background: #F5F5F4` -> `@apply bg-stone-100`
- `border: 1px solid #dedede` -> `border: 1px solid var(--color-stone-300)`
- `color: #78716C` -> `@apply text-stone-500`
- `color: #A8A29E` -> `@apply text-stone-400`
- `border-left: 1px solid #dedede` -> `border-left: 1px solid var(--color-stone-300)`

General rules:
- `#fff` / `#ffffff` / `white` -> keep as-is (no token needed)
- Use `@apply` for `color` and `background-color` where a single Tailwind utility suffices
- Use `var(--color-stone-N)` for border shorthand properties
- Preserve all `!important` flags
- Do NOT change class names
- `#e0dcd4` (line ~1503 area, if in range) -> keep as-is (no exact token match, it's a decorative border)
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && npx @11ty/eleventy --dryrun 2>&1 | tail -3 && echo "---hex-count---" && sed -n '241,917p' src/css/input.css | grep -cE '#[0-9a-fA-F]{3,6}\b'</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds (eleventy --dryrun exits 0)
    - Lines 241-917 of src/css/input.css contain at most 15 hardcoded hex values (only #fff/#ffffff/white and rgba raw values, plus any decorative values with no token match)
    - `.repo-overlay` contains `rgba(139, 41, 66, 0.85)` (not the old `rgba(107, 31, 51, 0.85)`)
    - `.text-muted` uses `@apply text-stone-400` instead of `color: #A8A29E`
    - `.bg-gray` uses `@apply bg-stone-100` instead of `background-color: #F5F5F4`
    - No class names changed
  </acceptance_criteria>
  <done>All sections from Hero through Error Page tokenised, masonry overlay updated to primary burgundy per D-06, build passes</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx @11ty/eleventy --dryrun` passes without errors
2. Lines 61-917 of input.css contain no instances of #dedede, #1C1917, #A8A29E, #f0f0f0, #F5F5F4, #78716C, #57534E, #292524 (only #fff and rgba values)
3. Nav hover rule contains `border-bottom.*periwinkle`
4. Masonry overlay contains `rgba(139, 41, 66, 0.85)`
</verification>

<success_criteria>
- Header fully tokenised with periwinkle nav hover underline (D-01, D-03)
- Hero, masonry, footer, buttons, breadcrumb, cards, detail page, repo header, desc list, utilities, error page all tokenised
- Masonry overlay updated to primary burgundy (D-06)
- Footer verified as already complete from Phase 1 (COMP-02)
- Warm white background verified as already complete from Phase 1 (COMP-08)
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-updates/02-01-SUMMARY.md`
</output>
