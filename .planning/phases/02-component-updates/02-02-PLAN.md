---
phase: 02-component-updates
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/css/input.css
autonomous: true
requirements:
  - COMP-05

must_haves:
  truths:
    - "Active/selected filter pills have burgundy background with white text"
    - "Active pagination number has periwinkle background with dark stone-900 text"
    - "Sort controls, facet labels, and panel borders use stone scale tokens instead of hardcoded hex"
    - "Search results section and search page section have no hardcoded hex values except #fff and rgba"
  artifacts:
    - path: "src/css/input.css"
      provides: "Tokenised search page CSS with updated interactive states"
      contains: ".filter-pill"
  key_links:
    - from: "src/css/input.css (.filter-pill)"
      to: "var(--color-burgundy)"
      via: "background property"
      pattern: "filter-pill.*burgundy"
    - from: "src/css/input.css (.pagination-link.active)"
      to: "var(--color-periwinkle)"
      via: "background property"
      pattern: "pagination-link.active"
---

<objective>
Tokenise all hardcoded hex values in the Search Results and Search Page sections of input.css (lines ~918-1734). Apply the active filter pill burgundy treatment (D-07) and active pagination periwinkle treatment (D-08). Tokenise sort controls and facet labels to stone scale (D-09).

Purpose: Completes the search page visual identity update -- the most interactive page in the site.
Output: input.css search sections fully tokenised with correct interactive states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-component-updates/02-CONTEXT.md
@.planning/phases/02-component-updates/02-UI-SPEC.md
@.planning/phases/02-component-updates/02-01-SUMMARY.md

@src/css/input.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tokenise search results section + sort/facet controls (D-09)</name>
  <files>src/css/input.css</files>
  <read_first>
    - src/css/input.css (full file -- must see current state after Plan 01)
    - .planning/phases/02-component-updates/02-UI-SPEC.md (search page component contract)
    - .planning/phases/02-component-updates/02-RESEARCH.md (token replacement map, pitfall 1 about stone variable availability)
  </read_first>
  <action>
Replace all hardcoded hex values in the Search Results section (lines ~918-1033) and the Search Page section (lines ~1034-1379) of src/css/input.css. These are the sort controls, facet panels, facet labels, and search refinement UI -- all mapped to stone scale per D-09.

**Search Results section (~918-1033):**

Token replacement map for this section:
- `#78716C` -> `@apply text-stone-500` (secondary text)
- `#F5F5F4` -> `@apply bg-stone-100` (light backgrounds)
- `#dedede` -> `var(--color-stone-300)` (borders)
- `#A8A29E` -> `@apply text-stone-400` (muted text)
- `#57534E` -> `@apply text-stone-600` (secondary labels)
- `#f0f0f0` -> `var(--color-stone-100)` (light borders)
- `#1C1917` -> `@apply text-stone-900` (dark text)
- `#fff3cd` (warning background, line ~1029) -> keep as-is (no token, it's a functional warning colour)
- `#fff` -> keep as-is

**Search Page section (~1034-1379):**

This section contains sort dropdowns, facet panels, facet items, facet counts, mobile search controls, and search state messages. All hardcoded hex values map to stone tokens per D-09:

- `#A8A29E` -> `@apply text-stone-400` or `color: var(--color-stone-400)` (muted labels, facet counts)
- `#1C1917` -> `@apply text-stone-900` (active labels, facet item text)
- `#dedede` -> `var(--color-stone-300)` (panel borders, dividers)
- `#f0f0f0` -> `var(--color-stone-100)` (light dividers)
- `#F5F5F4` -> `@apply bg-stone-100` (facet panel backgrounds)
- `#78716C` -> `@apply text-stone-500` (secondary info text)
- `#ccc` (line ~1111) -> `var(--color-stone-300)` (close enough)
- `#ddd` (lines ~1129, etc.) -> `var(--color-stone-300)` (borders)
- `#f8f8f8` (line ~1132) -> `@apply bg-stone-50` (very light background)
- `#555` (lines ~1254, 1273, 1372) -> `@apply text-stone-600` (closest match)
- `#999` (lines ~1258, 1294) -> `@apply text-stone-400` (closest match)
- `#333` (lines ~1314, 1327, 1333) -> `@apply text-stone-800` (closest match)
- `#888` (lines ~1327, 1352) -> `@apply text-stone-500` (closest match)

General rules:
- Use `@apply` for single-property colour/background utilities
- Use `var(--color-stone-N)` inside border shorthand
- Keep `#fff` and functional colours (like `#fff3cd` warning bg) as-is
- Do NOT change class names -- search.js creates DOM elements with these class names
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && npx @11ty/eleventy --dryrun 2>&1 | tail -3 && echo "---hex-count---" && sed -n '918,1379p' src/css/input.css | grep -cE '#[0-9a-fA-F]{3,6}\b'</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds (eleventy --dryrun exits 0)
    - Lines 918-1379 of input.css contain at most 5 hardcoded hex values (only #fff and #fff3cd warning)
    - No instances of #A8A29E, #1C1917, #dedede, #f0f0f0, #F5F5F4, #78716C, #57534E in lines 918-1379
    - All class names preserved (`.search-results-*`, `.facet-*`, `.sort-*` etc.)
  </acceptance_criteria>
  <done>Search results and search page sort/facet sections fully tokenised to stone scale per D-09, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Apply D-07 active filter pills + D-08 active pagination + tokenise pills/pagination section</name>
  <files>src/css/input.css</files>
  <read_first>
    - src/css/input.css (re-read after Task 1 changes)
    - src/js/search.js (verify filter pill class usage -- search for `filter-pill` to confirm all rendered pills are active selections)
    - .planning/phases/02-component-updates/02-UI-SPEC.md (interaction states table)
    - .planning/phases/02-component-updates/02-RESEARCH.md (D-07 and D-08 code examples, pitfall 2 about filter pill class names)
  </read_first>
  <action>
Apply the D-07 and D-08 interactive state decisions and tokenise remaining hardcoded values in the filter pills and pagination area (lines ~1380-1734) of src/css/input.css.

**D-07 -- Active filter pills (burgundy):**

In search.js, `createPill()` creates all pills with className `filter-pill`. All rendered pills ARE active selections (they only appear when a user has applied a filter). There is no unselected pill state. Therefore, change the base `.filter-pill` background from stone-900 to burgundy:

```css
.filter-pill {
  display: inline-flex;
  align-items: center;
  gap: 0.25rem;
  padding: 0.25rem 0.5rem;
  background: var(--color-burgundy);
  color: #fff;
  border-radius: 50px;
  font-size: 0.75rem;
}
```

Change: `background: #292524` -> `background: var(--color-burgundy)`
Keep: `color: #fff` as-is (white text on burgundy)

**Clear filters button tokenisation:**
- `.clear-filters-btn` (line ~1410): `background: #e8e8e8` -> `@apply bg-stone-200`
- `.clear-filters-btn` (line ~1416): `color: #333` -> `@apply text-stone-800`
- `.clear-filters-btn:hover` (line ~1421): `background: #d8d8d8` -> `background: var(--color-stone-300)`

**D-08 -- Active pagination (periwinkle bg, stone-900 text):**

```css
.pagination-link.active {
  background: var(--color-periwinkle);
  @apply text-stone-900;
  border-color: var(--color-periwinkle);
}
```

Changes:
- `background: var(--color-burgundy-deep)` -> `background: var(--color-periwinkle)`
- `color: #fff` -> `@apply text-stone-900`
- `border-color: var(--color-burgundy-deep)` -> `border-color: var(--color-periwinkle)`

**Pagination link tokenisation:**
- `.pagination-link` (line ~1433): `border: 1px solid #dedede` -> `border: 1px solid var(--color-stone-300)`
- `.pagination-link` (line ~1436): `color: #1C1917` -> `@apply text-stone-900`
- `.pagination-link.disabled` (line ~1453): `color: #A8A29E` -> `@apply text-stone-400`

**Remaining search page elements (lines ~1460-1734):**

Tokenise all remaining hardcoded hex values in this range. These include search state messages, mobile search controls, and responsive overrides. Apply the same token map:
- `#A8A29E` -> `@apply text-stone-400`
- `#1C1917` -> `@apply text-stone-900`
- `#e8e8e8` -> `@apply bg-stone-200`
- `#d8d8d8` -> `background: var(--color-stone-300)`
- `#333` -> `@apply text-stone-800`
- `#aaa` -> `@apply text-stone-400`
- `#e0dcd4` (line ~1503, decorative border) -> keep as-is (no exact token)
- `#555` -> `@apply text-stone-600`
- `#ccc` -> `var(--color-stone-300)`
- `#fff` -> keep as-is

Do NOT modify any class names. search.js creates `.filter-pill`, `.filter-pill-remove`, `.pagination-link`, `.clear-filters-btn` etc.
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && npx @11ty/eleventy --dryrun 2>&1 | tail -3 && echo "---" && grep 'filter-pill' src/css/input.css | head -5 && echo "---" && grep 'pagination-link.active' src/css/input.css</automated>
  </verify>
  <acceptance_criteria>
    - Build succeeds (eleventy --dryrun exits 0)
    - `.filter-pill` rule contains `background: var(--color-burgundy)` (not `#292524`)
    - `.filter-pill` rule still contains `color: #fff` (white text)
    - `.pagination-link.active` contains `background: var(--color-periwinkle)` (not `var(--color-burgundy-deep)`)
    - `.pagination-link.active` contains `text-stone-900` (not `color: #fff`)
    - `.pagination-link.active` contains `border-color: var(--color-periwinkle)`
    - `.clear-filters-btn` contains `bg-stone-200` or `var(--color-stone-200)`
    - Lines 1380-1734 contain at most 10 hardcoded hex values (#fff, #e0dcd4 decorative, rgba)
  </acceptance_criteria>
  <done>Active filter pills show burgundy per D-07, active pagination shows periwinkle per D-08, all search pill/pagination/remaining search CSS tokenised, build passes</done>
</task>

</tasks>

<verification>
After both tasks:
1. `npx @11ty/eleventy --dryrun` passes
2. `.filter-pill` background is `var(--color-burgundy)`
3. `.pagination-link.active` background is `var(--color-periwinkle)` with `text-stone-900`
4. Lines 918-1734 of input.css contain no instances of #A8A29E, #1C1917, #dedede, #f0f0f0, #F5F5F4, #78716C, #57534E, #292524
</verification>

<success_criteria>
- Active filter pills are burgundy with white text (D-07, COMP-05)
- Active pagination is periwinkle with dark text (D-08, COMP-05)
- Sort controls and facet labels use stone tokens (D-09, COMP-05)
- All search-related CSS sections fully tokenised
- Build compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-component-updates/02-02-SUMMARY.md`
</output>
