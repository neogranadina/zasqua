---
phase: 03-ahrb-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/_data/site.js
autonomous: true
requirements:
  - AHRB-01

must_haves:
  truths:
    - "Fresh export contains 10,000+ AHRB records in descriptions.json"
    - "Export data is uploaded to B2 zasqua-export bucket"
    - "Dev repo version is bumped to 0.4.0"
  artifacts:
    - path: "/tmp/zasqua-export/descriptions.json"
      provides: "Fresh backend export with AHRB descriptions"
    - path: "/tmp/zasqua-export/repositories.json"
      provides: "Repository metadata"
    - path: "/tmp/zasqua-export/children/"
      provides: "Per-parent child JSON files"
    - path: "src/_data/site.js"
      provides: "Version 0.4.0"
  key_links:
    - from: "export_frontend_data"
      to: "/tmp/zasqua-export/"
      via: "Django management command"
      pattern: "descriptions\\.json"
    - from: "/tmp/zasqua-export/"
      to: "b2://zasqua-export/"
      via: "b2 file upload and b2 sync"
      pattern: "zasqua-export"
---

<objective>
Run a fresh backend data export from zasqua-backend-dev, upload to Backblaze B2, and bump the version to 0.4.0 in the dev repo.

Purpose: AHRB-01 requires a fresh export (D-01). The version bump (D-11) happens in dev first so it can be ported to the public repo in Plan 02 as the final commit.
Output: Fresh data in B2, dev repo at version 0.4.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ahrb-import/03-CONTEXT.md
@.planning/phases/03-ahrb-import/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run fresh backend export and upload to B2</name>
  <read_first>
    - /Users/juancobo/Databases/zasqua/scripts/publish.sh (publishing pipeline — export and upload commands)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/.planning/phases/03-ahrb-import/03-RESEARCH.md (verification plan section)
  </read_first>
  <files>No files modified in this repo — operates on backend and B2</files>
  <action>
Per D-01 and D-02, run the export from zasqua-backend-dev (not the public backend repo):

1. Run the export command directly from zasqua-backend-dev:
   ```
   cd /Users/juancobo/Databases/zasqua/zasqua-backend-dev
   venv/bin/python manage.py export_frontend_data --output-dir /tmp/zasqua-export
   ```
   NOTE: Do NOT use publish.sh --export because it hardcodes BACKEND_DIR to zasqua-backend (public repo). D-01 requires running from zasqua-backend-dev.

2. Verify the export output:
   ```
   ls -lh /tmp/zasqua-export/descriptions.json /tmp/zasqua-export/repositories.json
   ls /tmp/zasqua-export/children/ | wc -l
   python3 -c "
   import json
   with open('/tmp/zasqua-export/descriptions.json') as f:
       data = json.load(f)
   ahrb = [d for d in data if d.get('repository_code') == 'co-ahrb']
   print(f'AHRB: {len(ahrb)} records')
   print(f'Total: {len(data)}')
   "
   ```
   Expected: 10,000+ AHRB records, ~106K total.

3. Upload to B2 using the upload commands from publish.sh:
   ```
   b2 file upload zasqua-export /tmp/zasqua-export/descriptions.json descriptions.json --quiet
   b2 file upload zasqua-export /tmp/zasqua-export/repositories.json repositories.json --quiet
   b2 sync --delete /tmp/zasqua-export/children/ b2://zasqua-export/children/
   ```

If b2 CLI is not authenticated, create a checkpoint for the user to run `b2 account authorize`.
  </action>
  <verify>
    <automated>python3 -c "import json; data=json.load(open('/tmp/zasqua-export/descriptions.json')); ahrb=[d for d in data if d.get('repository_code')=='co-ahrb']; assert len(ahrb)>10000, f'Only {len(ahrb)} AHRB records'; assert len(data)>100000, f'Only {len(data)} total'; print(f'OK: {len(ahrb)} AHRB, {len(data)} total')"</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/zasqua-export/descriptions.json exists and is > 100 MB
    - Python count of records with repository_code == 'co-ahrb' is >= 10000
    - Total record count is >= 100000
    - b2 upload commands complete without error (exit code 0)
    - /tmp/zasqua-export/children/ contains >= 1500 JSON files
  </acceptance_criteria>
  <done>Fresh export with 10,000+ AHRB records is in /tmp/zasqua-export/ and uploaded to B2 zasqua-export bucket</done>
</task>

<task type="auto">
  <name>Task 2: Bump version to 0.4.0 in dev repo</name>
  <read_first>
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/_data/site.js (current version — 0.3.1)
  </read_first>
  <files>src/_data/site.js</files>
  <action>
Per D-11, change the version field in src/_data/site.js from "0.3.1" to "0.4.0".

The file currently contains:
```js
version: "0.3.1"
```

Change to:
```js
version: "0.4.0"
```

No other changes to the file. Commit with message: `bump version to 0.4.0`
  </action>
  <verify>
    <automated>grep -q 'version: "0.4.0"' /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/_data/site.js && echo "OK: version is 0.4.0"</automated>
  </verify>
  <acceptance_criteria>
    - src/_data/site.js contains exactly `version: "0.4.0"`
    - No other lines in site.js are modified
    - Change is committed to dev repo
  </acceptance_criteria>
  <done>Dev repo site.js shows version 0.4.0, committed and ready for porting</done>
</task>

</tasks>

<verification>
- /tmp/zasqua-export/descriptions.json contains 10,000+ AHRB records and 100,000+ total
- B2 upload completed successfully (all three upload commands exited 0)
- src/_data/site.js contains version: "0.4.0"
</verification>

<success_criteria>
Fresh AHRB data is in Backblaze B2 ready for the CI build to download, and the dev repo version is 0.4.0.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ahrb-import/03-01-SUMMARY.md`
</output>
