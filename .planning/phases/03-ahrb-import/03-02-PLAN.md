---
phase: 03-ahrb-import
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified: []
autonomous: false
requirements:
  - AHRB-02
  - AHRB-03

must_haves:
  truths:
    - "Public repo contains all Phase 1-3 changes including Tailwind CLI build step"
    - "GitHub Actions build produces ~106K pages"
    - "AHRB repository landing page loads on zasqua.org"
    - "AHRB volume description pages load with IIIF viewer links"
    - "Footer displays v0.4.0"
  artifacts:
    - path: "/Users/juancobo/Databases/zasqua/zasqua-frontend/.github/workflows/deploy.yml"
      provides: "Deploy workflow with Tailwind CSS compilation step"
      contains: "Build CSS with Tailwind"
    - path: "/Users/juancobo/Databases/zasqua/zasqua-frontend/src/css/input.css"
      provides: "Tailwind input stylesheet (new file ported from dev)"
    - path: "/Users/juancobo/Databases/zasqua/zasqua-frontend/src/_data/site.js"
      provides: "Version 0.4.0"
      contains: 'version: "0.4.0"'
  key_links:
    - from: "deploy.yml"
      to: "src/css/input.css"
      via: "Tailwind CLI compilation step"
      pattern: "input.css.*main.css"
    - from: "gh workflow run"
      to: "deploy.yml"
      via: "workflow_dispatch trigger"
      pattern: "workflow_dispatch"
---

<objective>
Port all Phase 1-3 dev changes to the public zasqua-frontend repo, push, trigger a CI build, and verify the live site serves AHRB content in the new visual identity.

Purpose: AHRB-02 requires a full rebuild deployed to R2 with ~106K pages. AHRB-03 requires the AHRB landing page and volume pages to display correctly. D-07/D-08 mandate porting all changes with thematic commits reviewed before committing.
Output: Live site at zasqua.org with AHRB volumes, new visual identity, version 0.4.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ahrb-import/03-CONTEXT.md
@.planning/phases/03-ahrb-import/03-RESEARCH.md
@.planning/phases/03-ahrb-import/03-01-SUMMARY.md

Porting guidelines: Follow CLAUDE.md sections on "Porting to public repos", "Public Repo Hygiene", and "Closing Out a Release".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port all Phase 1-3 changes to the public repo in thematic commits</name>
  <read_first>
    - /Users/juancobo/Databases/zasqua/CLAUDE.md (porting guidelines, public repo hygiene, commit style)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/.planning/phases/03-ahrb-import/03-CONTEXT.md (D-07 through D-10 — porting file lists)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/.planning/phases/03-ahrb-import/03-RESEARCH.md (recommended porting commit groups, anti-patterns)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/css/input.css (new file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/css/main.css (changed file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/_layouts/base.njk (changed file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/_includes/header.njk (changed file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/_includes/footer.njk (changed file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/index.njk (changed file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/repository.njk (changed file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/description.njk (changed file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/js/search.js (changed file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/build.sh (changed file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/.github/workflows/deploy.yml (changed file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/src/_data/site.js (version 0.4.0 to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/scripts/check-css-tokens.sh (new file to port)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend/.github/workflows/deploy.yml (public repo — confirm it lacks Tailwind step)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend/src/_data/site.js (public repo — confirm version is 0.3.1)
  </read_first>
  <files>All porting happens in /Users/juancobo/Databases/zasqua/zasqua-frontend/ (public repo)</files>
  <action>
Per D-07, D-08, D-09, D-10, port all Phase 1-3 dev changes to the public `zasqua-frontend` repo. Copy full files from dev to public — no surgical edits. Present each commit draft for user review before applying.

CRITICAL RULES (from CLAUDE.md):
- NEVER use `git add .` or `git add -A` — always add files by name
- NEVER commit .planning/, CLAUDE.md, .claude/, or any GSD artefacts
- Commit messages: sentence case, no conventional commit prefixes, no emojis, no phase numbers, no AI references
- Present each commit message draft for review before committing

Porting commit groups (from 03-RESEARCH.md recommended grouping):

**Commit 1 — Visual identity fonts and colour tokens:**
```
cp zasqua-frontend-dev/src/css/input.css zasqua-frontend/src/css/input.css
cp zasqua-frontend-dev/src/css/main.css zasqua-frontend/src/css/main.css
```
Draft message: "Update visual identity: DM Sans, Crimson Text, burgundy palette"
Files to stage: `src/css/input.css`, `src/css/main.css`

**Commit 2 — New branding assets:**
```
cp zasqua-frontend-dev/src/img/zasqua-3-burgundy-sm.svg zasqua-frontend/src/img/zasqua-3-burgundy-sm.svg
cp zasqua-frontend-dev/src/img/ampl-cropped-1.png zasqua-frontend/src/img/ampl-cropped-1.png
```
Draft message: "Add new branding images for v0.4.0 visual identity"
Files to stage: `src/img/zasqua-3-burgundy-sm.svg`, `src/img/ampl-cropped-1.png`

**Commit 3 — Header, footer, base layout:**
```
cp zasqua-frontend-dev/src/_layouts/base.njk zasqua-frontend/src/_layouts/base.njk
cp zasqua-frontend-dev/src/_includes/header.njk zasqua-frontend/src/_includes/header.njk
cp zasqua-frontend-dev/src/_includes/footer.njk zasqua-frontend/src/_includes/footer.njk
```
Draft message: "Redesign header and footer for new visual identity"
Files to stage: `src/_layouts/base.njk`, `src/_includes/header.njk`, `src/_includes/footer.njk`

**Commit 4 — Page templates:**
```
cp zasqua-frontend-dev/src/index.njk zasqua-frontend/src/index.njk
cp zasqua-frontend-dev/src/repository.njk zasqua-frontend/src/repository.njk
cp zasqua-frontend-dev/src/description.njk zasqua-frontend/src/description.njk
```
Draft message: "Update homepage, repository, and description page styling"
Files to stage: `src/index.njk`, `src/repository.njk`, `src/description.njk`

**Commit 5 — Search JS:**
```
cp zasqua-frontend-dev/src/js/search.js zasqua-frontend/src/js/search.js
```
Draft message: "Update search page for new colour tokens"
Files to stage: `src/js/search.js`

**Commit 6 — Build pipeline (CRITICAL — must include input.css or Tailwind step fails):**
```
cp zasqua-frontend-dev/build.sh zasqua-frontend/build.sh
cp zasqua-frontend-dev/.github/workflows/deploy.yml zasqua-frontend/.github/workflows/deploy.yml
```
Draft message: "Add Tailwind CSS compilation step to build pipeline"
Files to stage: `build.sh`, `.github/workflows/deploy.yml`
VERIFY: After staging, confirm deploy.yml contains "Build CSS with Tailwind" step between npm ci and Eleventy steps. Also confirm input.css was already ported in Commit 1 — if not, the CI build will fail.

**Commit 7 — CSS token verification script:**
```
cp zasqua-frontend-dev/scripts/check-css-tokens.sh zasqua-frontend/scripts/check-css-tokens.sh
```
Draft message: "Add CSS token verification script"
Files to stage: `scripts/check-css-tokens.sh`

**Commit 8 — Version bump (MUST be last per D-11):**
```
cp zasqua-frontend-dev/src/_data/site.js zasqua-frontend/src/_data/site.js
```
Draft message: "Bump version to 0.4.0"
Files to stage: `src/_data/site.js`
VERIFY: site.js contains `version: "0.4.0"` before committing.

After all commits, push to the public repo: `cd zasqua-frontend && git push`
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend && grep -q 'version: "0.4.0"' src/_data/site.js && grep -q 'input.css' .github/workflows/deploy.yml && test -f src/css/input.css && echo "OK: version 0.4.0, Tailwind step present, input.css exists"</automated>
  </verify>
  <acceptance_criteria>
    - zasqua-frontend/src/_data/site.js contains `version: "0.4.0"`
    - zasqua-frontend/.github/workflows/deploy.yml contains "Build CSS with Tailwind" or "input.css"
    - zasqua-frontend/src/css/input.css exists (new file)
    - zasqua-frontend/src/img/zasqua-3-burgundy-sm.svg exists (new file)
    - zasqua-frontend/src/img/ampl-cropped-1.png exists (new file)
    - zasqua-frontend/scripts/check-css-tokens.sh exists (new file)
    - git log in zasqua-frontend shows 8 thematic commits
    - No .planning/, CLAUDE.md, .claude/ files are present in zasqua-frontend
    - All commits are pushed to origin
  </acceptance_criteria>
  <done>All Phase 1-3 changes ported to public repo in thematic commits, pushed to origin</done>
</task>

<task type="auto">
  <name>Task 2: Trigger CI build and monitor deployment</name>
  <read_first>
    - /Users/juancobo/Databases/zasqua/zasqua-frontend/.github/workflows/deploy.yml (confirm workflow is correct after porting)
    - /Users/juancobo/Databases/zasqua/zasqua-frontend-dev/.planning/phases/03-ahrb-import/03-RESEARCH.md (verification plan section)
  </read_first>
  <files>No files modified — triggers CI and monitors</files>
  <action>
Per D-12, trigger the GitHub Actions build on the public repo:

```
gh workflow run deploy.yml --repo neogranadina/zasqua-frontend
```

Monitor the workflow run:
```
gh run list --repo neogranadina/zasqua-frontend --limit 1
gh run watch --repo neogranadina/zasqua-frontend
```

The build takes ~30-50 minutes. Watch for:
1. "Build CSS with Tailwind" step completes (confirms input.css was ported)
2. Eleventy produces ~106K pages (look for page count in output)
3. R2 upload completes
4. Cloudflare cache purge completes

If the workflow fails:
- Check if it's the Tailwind step (missing input.css → fix in public repo)
- Check if it's B2 download (stale credentials → user must update secrets)
- Check if it's timeout (re-trigger — uploads are idempotent)
  </action>
  <verify>
    <automated>gh run list --repo neogranadina/zasqua-frontend --limit 1 --json status,conclusion --jq '.[0] | "\(.status) \(.conclusion)"' | grep -q "completed success" && echo "OK: workflow succeeded"</automated>
  </verify>
  <acceptance_criteria>
    - gh run list shows most recent run with status "completed" and conclusion "success"
    - Workflow log contains page count line showing 106,000+ pages
  </acceptance_criteria>
  <done>GitHub Actions build completed successfully with ~106K pages deployed to R2</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify live site — AHRB pages and visual identity</name>
  <files>No files modified — manual verification of live site</files>
  <action>
Per D-13, the user verifies the live site at zasqua.org. Present the checklist below and wait for approval.
  </action>
  <what-built>
Full v0.4.0 deployment: AHRB volumes live on zasqua.org with the new visual identity (DM Sans, Crimson Text, burgundy palette).
  </what-built>
  <how-to-verify>
Per D-13, verify the following on https://zasqua.org:

1. **Homepage** (https://zasqua.org/)
   - Page loads with new visual identity (DM Sans body text, burgundy palette)
   - AHRB appears in the masonry grid of repositories

2. **AHRB repository landing page** (https://zasqua.org/co-ahrb/)
   - Page loads and displays "Archivo Historico Regional de Boyaca" (or similar)
   - Subtitle shows "Materiales digitalizados por Neogranadina en el Archivo Historico Regional de Boyaca, Tunja, Colombia."
   - Repository image (/img/AHRB_front.jpg) is visible
   - Miller columns tree navigation shows volume listings

3. **AHRB volume description page** (pick any file-level record from the tree)
   - Page loads with metadata fields (title, dates, extent, etc.)
   - Periwinkle level badge visible
   - Breadcrumb navigation works

4. **AHRB record with IIIF viewer** (pick a record with a digitised item)
   - TIFY viewer link/button is present
   - Clicking it opens the IIIF viewer

5. **Footer version**
   - Footer displays "v0.4.0"

6. **Search** (https://zasqua.org/buscar/)
   - Search page loads
   - Searching for "Tunja" or "notaria" returns AHRB results
  </how-to-verify>
  <verify>
    <automated>curl -s https://zasqua.org/co-ahrb/ | grep -q "co-ahrb" && echo "OK: AHRB page responds"</automated>
  </verify>
  <acceptance_criteria>
    - https://zasqua.org/ loads with DM Sans font and burgundy palette
    - https://zasqua.org/co-ahrb/ loads and shows AHRB repository content
    - At least one AHRB volume page loads with metadata
    - Footer shows "v0.4.0"
  </acceptance_criteria>
  <done>User confirms all 6 verification checks pass on the live site</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- Public repo has all Phase 1-3 changes in thematic commits
- deploy.yml contains the Tailwind CSS compilation step
- GitHub Actions build completed with ~106K pages
- Live site at zasqua.org shows AHRB content in the new visual identity
- Footer displays v0.4.0
</verification>

<success_criteria>
The 542+ AHRB notarial volumes are live on zasqua.org rendered in the new visual identity, the build produced ~106K pages, and the site version is 0.4.0.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ahrb-import/03-02-SUMMARY.md`
</output>
