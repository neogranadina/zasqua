---
phase: 05-pmtiles-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - .github/workflows/deploy.yml
  - build.sh
autonomous: false
requirements: [BUILD-04, BUILD-05]

user_setup:
  - service: cloudflare-r2
    why: "Dedicated R2 bucket for PMTiles storage"
    env_vars: []
    dashboard_config:
      - task: "Create zasqua-tiles R2 bucket"
        location: "Cloudflare Dashboard -> R2 Object Storage -> Create Bucket -> name: zasqua-tiles"
  - service: cloudflare-workers
    why: "Custom domain for tiles Worker (required for caching and CORS)"
    env_vars: []
    dashboard_config:
      - task: "Assign tiles.zasqua.org custom domain to zasqua-tiles Worker"
        location: "Cloudflare Dashboard -> Workers & Pages -> zasqua-tiles -> Settings -> Domains & Routes -> Add Custom Domain -> tiles.zasqua.org"

must_haves:
  truths:
    - "CI pipeline generates PMTiles from place data and uploads to zasqua-tiles R2 bucket"
    - "Local build script includes tile generation step"
    - "HTTP 206 responses are returned for Range requests to tiles.zasqua.org"
    - "CORS headers permit cross-origin requests from zasqua.org"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "CI steps for Tippecanoe install, GeoJSON conversion, PMTiles generation, and R2 upload"
      contains: "tippecanoe"
    - path: "build.sh"
      provides: "Local tile generation step"
      contains: "places-to-geojson"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: "scripts/places-to-geojson.js"
      via: "node scripts/places-to-geojson.js step"
      pattern: "node scripts/places-to-geojson"
    - from: ".github/workflows/deploy.yml"
      to: "zasqua-tiles R2 bucket"
      via: "boto3 upload step"
      pattern: "zasqua-tiles"
---

<objective>
Wire PMTiles generation and upload into the CI pipeline and local build script, then verify end-to-end that the tiles Worker serves Range requests with CORS.

Purpose: Plan 01 created the scripts and Worker code. This plan integrates them into the build pipeline so tiles are generated and uploaded on every deploy, then verifies the full chain works on the production domain.

Output: Updated `deploy.yml` and `build.sh`, verified tile serving at `tiles.zasqua.org`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-pmtiles-infrastructure/05-CONTEXT.md
@.planning/phases/05-pmtiles-infrastructure/05-RESEARCH.md
@.planning/phases/05-pmtiles-infrastructure/05-01-SUMMARY.md

<interfaces>
<!-- Files created by Plan 01 that this plan references -->

scripts/places-to-geojson.js:
- Reads data/places.json, writes data/places.geojson
- Node.js stdlib only, no npm dependencies
- Filters to places with non-null lat/lon
- Outputs GeoJSON FeatureCollection

worker-tiles/:
- Deployed via: cd worker-tiles && npx wrangler deploy
- Uses CLOUDFLARE_ACCOUNT_ID env var (same secret already in CI)
- R2 binding: BUCKET -> zasqua-tiles bucket

From .github/workflows/deploy.yml (current structure):
- Steps: Checkout -> Setup Node -> Setup Python -> Install Python deps -> Download data from B2 -> Pre-compute links -> Pre-compute co-occurrence -> Install npm -> Build CSS -> Build Eleventy -> Pagefind -> Deploy to R2 -> Purge cache
- Secrets available: R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, CLOUDFLARE_ACCOUNT_ID, CF_API_TOKEN, CF_ZONE_ID
- Python + pip already set up before data download
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PMTiles generation and upload to CI pipeline</name>
  <files>.github/workflows/deploy.yml, build.sh</files>
  <read_first>
    - .github/workflows/deploy.yml (current full pipeline — must see exact step names, ordering, indentation)
    - build.sh (current local build script — must see exact structure)
    - .planning/phases/05-pmtiles-infrastructure/05-RESEARCH.md (CI Steps code example, Pattern 3: Tippecanoe Invocation, Pattern 4: PMTiles R2 Upload)
  </read_first>
  <action>
**deploy.yml changes — add 3 new steps after "Pre-compute co-occurrence graph" and before "Install npm dependencies":**

Per D-04 and D-08, Tippecanoe runs in CI and tiles regenerate on every deploy.

Step 1 — Install Tippecanoe (after "Pre-compute co-occurrence graph"):
```yaml
      - name: Install Tippecanoe
        run: pip install tippecanoe==2.72.0
```
Pin to 2.72.0 for reproducible builds (per research Open Question 3).

Step 2 — Generate PMTiles:
```yaml
      - name: Generate PMTiles
        run: |
          node scripts/places-to-geojson.js
          tippecanoe -Z0 -z14 --drop-densest-as-needed -l places \
            -o data/zasqua-places.pmtiles data/places.geojson
          ls -lh data/zasqua-places.pmtiles
```

Step 3 — Upload PMTiles to R2 (per D-09, upload to `zasqua-tiles` bucket, not `zasqua-site`):
```yaml
      - name: Upload PMTiles to R2
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ENDPOINT: https://${{ secrets.CLOUDFLARE_ACCOUNT_ID }}.r2.cloudflarestorage.com
        run: |
          python3 - << 'EOF'
          import boto3, os
          s3 = boto3.client('s3',
              endpoint_url=os.environ['R2_ENDPOINT'],
              aws_access_key_id=os.environ['R2_ACCESS_KEY_ID'],
              aws_secret_access_key=os.environ['R2_SECRET_ACCESS_KEY'],
          )
          s3.upload_file('data/zasqua-places.pmtiles', 'zasqua-tiles', 'zasqua-places.pmtiles',
              ExtraArgs={'ContentType': 'application/octet-stream'})
          print("PMTiles uploaded to zasqua-tiles/zasqua-places.pmtiles")
          EOF
```

These three steps go BEFORE "Install npm dependencies" — they use only Python/pip (already set up) and Node.js (already set up), no npm packages needed. Per Phase 04 decision: pre-compute steps placed before npm install since scripts use only Node.js stdlib.

**build.sh changes — add tile generation after co-occurrence step:**

After the line `ls -lh data/entity-cooccurrence.json`, add:

```bash
echo "=== Generating PMTiles ==="
node scripts/places-to-geojson.js
if command -v tippecanoe &> /dev/null; then
  tippecanoe -Z0 -z14 --drop-densest-as-needed -l places \
    -o data/zasqua-places.pmtiles data/places.geojson
  ls -lh data/zasqua-places.pmtiles
else
  echo "Tippecanoe not installed — skipping PMTiles generation"
  echo "Install with: brew install tippecanoe (macOS) or pip install tippecanoe (Linux)"
fi
```

The `command -v` guard makes the local build work even without Tippecanoe installed — the GeoJSON is always generated (useful for inspection), but tile generation is optional locally.

Do NOT modify any existing steps. Do NOT modify the site Worker deploy or cache purge steps.
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && grep -q 'Install Tippecanoe' .github/workflows/deploy.yml && grep -q 'Generate PMTiles' .github/workflows/deploy.yml && grep -q 'Upload PMTiles to R2' .github/workflows/deploy.yml && grep -q 'zasqua-tiles' .github/workflows/deploy.yml && grep -q 'tippecanoe' .github/workflows/deploy.yml && grep -q 'places-to-geojson' build.sh && grep -q 'tippecanoe' build.sh && echo "All CI checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - deploy.yml contains step "Install Tippecanoe" with `pip install tippecanoe==2.72.0`
    - deploy.yml contains step "Generate PMTiles" with `node scripts/places-to-geojson.js` and `tippecanoe` command
    - deploy.yml contains step "Upload PMTiles to R2" with boto3 uploading to bucket `zasqua-tiles`
    - The three new steps appear AFTER "Pre-compute co-occurrence graph" and BEFORE "Install npm dependencies"
    - deploy.yml Tippecanoe command uses flags: `-Z0 -z14 --drop-densest-as-needed -l places`
    - deploy.yml upload targets file `zasqua-places.pmtiles` in bucket `zasqua-tiles`
    - build.sh contains `places-to-geojson.js` invocation
    - build.sh contains `command -v tippecanoe` guard for optional local tile generation
    - Existing deploy.yml steps (Checkout through Purge cache) are unchanged
    - No modifications to worker/ directory
  </acceptance_criteria>
  <done>CI pipeline generates PMTiles from place data via Tippecanoe and uploads to zasqua-tiles R2 bucket on every deploy. Local build script includes optional tile generation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end tile serving</name>
  <what-built>
Complete PMTiles infrastructure: GeoJSON conversion script, Protomaps tiles Worker at worker-tiles/, CI pipeline steps for Tippecanoe + R2 upload, local build script update.

Before this verification can succeed, you need to complete two manual setup steps:
1. Create the `zasqua-tiles` R2 bucket in Cloudflare Dashboard
2. Deploy the Worker once: `cd worker-tiles && npx wrangler deploy`
3. Assign `tiles.zasqua.org` as custom domain to the `zasqua-tiles` Worker in Cloudflare Dashboard
4. Run the CI workflow (or manually upload a test PMTiles file)
  </what-built>
  <how-to-verify>
After the manual setup and a successful CI run:

1. Verify PMTiles file exists in R2:
   Check the zasqua-tiles bucket in Cloudflare Dashboard — `zasqua-places.pmtiles` should be present

2. Test HTTP 206 Range response:
   ```bash
   curl -v -H "Range: bytes=0-512" \
     -H "Origin: https://zasqua.org" \
     https://tiles.zasqua.org/zasqua-places \
     2>&1 | grep -E "HTTP|content-range|access-control|cf-cache"
   ```
   Expected: HTTP/2 206, content-range header, access-control-allow-origin: https://zasqua.org

3. Test CORS preflight:
   ```bash
   curl -v -X OPTIONS \
     -H "Origin: https://zasqua.org" \
     -H "Access-Control-Request-Method: GET" \
     https://tiles.zasqua.org/zasqua-places \
     2>&1 | grep -E "HTTP|access-control"
   ```
   Expected: 200/204 with access-control-allow-origin

4. Test in Firefox: Open Firefox DevTools Network tab, navigate to a test page that fetches `https://tiles.zasqua.org/zasqua-places` with a Range header — verify no CORS errors

5. Test in Safari: Same as Firefox test — verify no CORS errors

Per STATE.md blocker: "Protomaps Worker + custom domain CORS chain has known gap; allow extra debug time; test Firefox and Safari"
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any CORS/Range issues encountered</resume-signal>
</task>

</tasks>

<verification>
- CI pipeline includes Tippecanoe install, GeoJSON conversion, PMTiles generation, and R2 upload steps in correct order
- Local build script includes optional tile generation
- HTTP 206 + CORS headers verified on tiles.zasqua.org in Firefox and Safari
- Existing site Worker and CI steps are untouched
</verification>

<success_criteria>
- `deploy.yml` has 3 new steps (Tippecanoe install, PMTiles generation, R2 upload) in correct position
- `build.sh` has optional local tile generation step
- `curl` to `tiles.zasqua.org/zasqua-places` with Range header returns HTTP 206 with CORS headers
- Firefox and Safari both load tiles without CORS errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-pmtiles-infrastructure/05-02-SUMMARY.md`
</output>
