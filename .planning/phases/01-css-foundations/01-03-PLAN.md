---
phase: 01-css-foundations
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/css/input.css
  - src/buscar.njk
  - src/description.njk
  - src/js/search.js
  - src/js/tree.js
  - src/js/description.js
autonomous: false
requirements: [COL-02, COL-03]

must_haves:
  truths:
    - "Search page filter pills and active pagination use periwinkle/burgundy — no blue accent colours remain"
    - "Miller column selected state uses burgundy-deep instead of blue"
    - "Description page TIFY viewer overrides use new colour tokens with !important preserved"
    - "No hardcoded hex values exist in the compiled CSS outside the :root/@theme block"
    - "All JS-created DOM elements retain their class-name styling via @layer components"
  artifacts:
    - path: "src/css/input.css"
      provides: "Complete Tailwind input stylesheet with all 20 CSS sections migrated"
      contains: ".miller-column"
    - path: "src/buscar.njk"
      provides: "Search page template with Tailwind utility classes"
      contains: "text-"
    - path: "src/description.njk"
      provides: "Description page template with Tailwind utility classes"
      contains: "text-"
  key_links:
    - from: "src/js/search.js"
      to: "src/css/input.css"
      via: "JS-created class names matching @layer components rules"
      pattern: "filter-pill|search-results|pagefind"
    - from: "src/js/tree.js"
      to: "src/css/input.css"
      via: "JS-created class names matching @layer components rules"
      pattern: "miller-column|miller-item"
    - from: "src/js/description.js"
      to: "src/css/input.css"
      via: "JS-created class names and inline styles"
      pattern: "tify|viewer"
---

<objective>
Migrate the remaining complex interactive CSS sections — search results/page, Miller columns, description page (including TIFY viewer), and children tree — into @layer components. Convert the search and description templates to Tailwind utility classes. Update JS files to use CSS variable references instead of hardcoded colour values. Eliminate all remaining blue accent colours and hardcoded hex values.

Purpose: Completes the full CSS migration. After this plan, zero hardcoded hex values remain outside @theme, zero legacy colour variables survive, and all interactive components render in the new burgundy/periwinkle palette.

Output: Complete input.css with all sections migrated, all templates converted, JS colour references updated, and all verification checks passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-css-foundations/01-CONTEXT.md
@.planning/phases/01-css-foundations/01-RESEARCH.md
@.planning/phases/01-css-foundations/01-UI-SPEC.md
@.planning/phases/01-css-foundations/01-01-SUMMARY.md
@.planning/phases/01-css-foundations/01-02-SUMMARY.md

<interfaces>
<!-- From Plans 01 and 02 — the input.css structure the executor extends -->
The executor will find input.css with:
- @import "tailwindcss" source("../src");
- @theme block with 9 colour + 3 font tokens
- :root block with --max-width and --header-height
- @layer base with element resets
- @layer components populated with layout shell sections (header, footer, hero, masonry, buttons, breadcrumb, cards, detail, repository, description list, placeholder, utilities, error)

The executor adds the remaining sections to @layer components:
- Search Results (old lines 831-946)
- Search Page (old lines 947-1664)
- Miller Columns Tree (old lines 1696-1969)
- Description Page (old lines 1970-2306) including TIFY viewer overrides
- Children Tree (old lines 2307-2441)

Available Tailwind utility classes from @theme:
- bg-burgundy, bg-burgundy-deep, bg-burgundy-light, bg-burgundy-dark, bg-pale-rose, bg-periwinkle, bg-bg
- text-burgundy, text-burgundy-light, text-burgundy-deep, text-burgundy-dark, text-periwinkle, text-ochre, text-sage
- font-sans, font-serif, font-display
- stone-50 through stone-900 (Tailwind built-in)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate search, Miller columns, description page, and children tree CSS into @layer components</name>
  <files>src/css/input.css, src/js/search.js, src/js/tree.js, src/js/description.js</files>
  <read_first>
    - src/css/input.css (current state from Plan 02 — read full file to find insertion point in @layer components)
    - The ORIGINAL src/css/main.css content for the sections being migrated. Retrieve via git if needed: `git show` from the commit before Plan 01. The executor needs the original CSS for these line ranges:
      - Search Results: lines 831-946
      - Search Page: lines 947-1664
      - Miller Columns Tree: lines 1696-1969
      - Description Page: lines 1970-2306 (includes TIFY viewer overrides)
      - Children Tree: lines 2307-2441
    - src/js/search.js (full file — 1,610 lines. Scan for all className assignments, classList operations, and inline style colour values)
    - src/js/tree.js (full file — 413 lines. Scan for className assignments and inline styles)
    - src/js/description.js (full file — 123 lines. Scan for className assignments and inline styles)
    - .planning/phases/01-css-foundations/01-RESEARCH.md (Pitfall 1: JS class names, Pitfall 5: TIFY !important, code examples for JS inline styles)
    - .planning/phases/01-css-foundations/01-UI-SPEC.md (eliminated colours table, periwinkle usage)
  </read_first>
  <action>
**Part A: Migrate CSS sections into @layer components in input.css**

Add these sections to the existing `@layer components` block in input.css, AFTER the layout shell sections from Plan 02. Apply the same colour replacement rules as Plan 02:

| Old Variable/Value | New Token/Value |
|---|---|
| `var(--accent-primary)` / `rgba(41,98,255,0.8)` | `var(--color-burgundy-deep)` / `#6B1F33` |
| `var(--accent-hover)` / `#f18e00` | `var(--color-periwinkle)` / `#C9D5FF` |
| `var(--accent-link)` / `#F2784B` | `var(--color-burgundy-light)` / `#B14D66` |
| `var(--accent-overlay)` / `rgba(41,98,255,0.8)` | `rgba(107,31,51,0.85)` |
| `#2962FF` (blue) | `var(--color-burgundy-deep)` |
| `#2c3e50` (dark blue) | `var(--color-burgundy-dark)` |
| `var(--bg-color)` / `#ffffff` | `var(--color-bg)` / `#FAFAF9` |
| `var(--bg-gray)` / `#f2f2f2` | `#F5F5F4` (stone-100) |
| `var(--text-color)` / `#757575` | `#78716C` (stone-500) |
| `var(--text-dark)` / `#000` | `#1C1917` (stone-900) |

**1. Search Results section** (old lines 831-946):
- Migrate all `.search-results-*` classes
- Replace blue button backgrounds with `var(--color-burgundy-deep)`
- Replace hover states with `var(--color-periwinkle)` or `var(--color-burgundy)`

**2. Search Page section** (old lines 947-1664, ~717 lines — the largest section):
- Migrate all search page classes: `.search-page`, `.results-info`, `.result-card`, `.refine-search`, `.operator-selector`, `.sidebar`, `.facet-*`, `.date-tree-*`, `.filter-pill`, `.pagination`, `.search-state-*`, `.loading-*`, `.browse-prompt`, `.mobile-filter-*`, `.filter-panel-*`
- **Filter pills**: Replace blue active state with `var(--color-periwinkle)` background, dark text. The `.filter-pill` class should use: `@apply inline-flex items-center gap-1 px-2 py-1 rounded-full text-xs bg-stone-800 text-white;` (per Research Pattern 3)
- **Pagination**: Active page uses `var(--color-burgundy-deep)` background. Hover uses `var(--color-periwinkle)` background
- **Search input**: Use `.search-input` class with `@apply border border-stone-300 rounded-full bg-stone-50 px-4 py-3;` and focus state `@apply border-burgundy-light bg-white outline-none;` (per Research Pattern 3)
- **Facet groups**: Replace any blue highlights with periwinkle
- **Date tree**: Replace any blue selection colours with periwinkle
- **Responsive media queries**: Preserve all `@media (max-width: 768px)` and other breakpoint rules within the component classes. These desktop-first overrides stay as-is

**3. Miller Columns section** (old lines 1696-1969):
- Migrate all `.miller-*` classes: `.miller-columns`, `.miller-column`, `.miller-item`, `.miller-item.selected`, `.miller-item.selected-ancestor`, `.tree-header`, `.column-header-filter`
- **Selected state**: `.miller-item.selected` gets `@apply bg-burgundy-deep text-white;` (per Research Pattern 3)
- **Selected ancestor**: `.miller-item.selected-ancestor` gets `background: rgba(107,31,51,0.5); @apply text-white;` (burgundy-deep at 50% opacity)
- **Hover state**: `var(--color-periwinkle)` background with dark text
- Preserve responsive rules for Miller columns

**4. Description Page section** (old lines 1970-2306):
- Migrate all `.description-*`, `.level-badge`, `.notice-box`, `.reproduction-*`, `.repo-link-*` classes
- **Level badge**: Use `var(--color-periwinkle)` background with dark text
- **Links**: Use `var(--color-burgundy)` text colour, `var(--color-burgundy-light)` on hover
- **TIFY viewer overrides** (old lines ~2150-2306): Preserve ALL `!important` declarations — these are needed to override TIFY's internal CSS per Pitfall 5. Replace colour values:
  - Any blue backgrounds → `var(--color-burgundy-deep)` or `var(--color-periwinkle)`
  - Any blue borders → `var(--color-burgundy-light)`
  - Button backgrounds → `var(--color-burgundy-deep)`
  - Button hover → `var(--color-burgundy)`
  - Keep all `!important` flags intact

**5. Children Tree section** (old lines 2307-2441):
- Migrate all `.children-tree-*` classes
- Replace any blue selection/highlight colours with periwinkle or burgundy

**Part B: Update JS files — replace hardcoded colour values with CSS variable references**

Scan each JS file for inline style colour assignments and update them:

**`src/js/search.js`** — look for patterns like:
- `element.style.backgroundColor = '#...'` → `element.style.backgroundColor = 'var(--color-burgundy-deep)'`
- `element.style.color = '#...'` → `element.style.color = 'var(--color-burgundy-light)'`
- `element.style.borderColor = '#...'` → `element.style.borderColor = 'var(--color-burgundy)'`
- Any `rgba(41,98,255` references → replace with burgundy equivalents
- Any `#f18e00` or `#F2784B` references → replace with periwinkle or burgundy
- Do NOT change class name strings — those must match @layer components exactly

**`src/js/tree.js`** — same pattern: replace hardcoded colour values in inline styles with `var(--color-*)` references. Specifically look for blue highlight colours used for selection state.

**`src/js/description.js`** — same pattern: replace any hardcoded colour values with CSS variable references.

**Rule per Research code examples:** Always use `element.style.color = 'var(--color-burgundy-deep)'` (CSS variable reference), NOT `getComputedStyle()` to read then assign. The variable reference adapts dynamically.
  </action>
  <verify>
    <automated>grep -ci "rgba(41,98,255\|2c3e50\|003660\|f18e00\|F2784B\|2962FF" src/css/input.css src/js/search.js src/js/tree.js src/js/description.js 2>/dev/null | awk -F: '{s+=$2} END {print "Legacy colours remaining:", s}'</automated>
  </verify>
  <acceptance_criteria>
    - src/css/input.css contains `.miller-columns` and `.miller-item` classes in @layer components
    - src/css/input.css contains `.miller-item.selected` with `bg-burgundy-deep` or `var(--color-burgundy-deep)`
    - src/css/input.css contains `.filter-pill` class in @layer components
    - src/css/input.css contains `.search-input` class in @layer components
    - src/css/input.css contains `.pagination` related classes
    - src/css/input.css contains `.level-badge` class
    - src/css/input.css contains `.children-tree` or equivalent classes
    - src/css/input.css contains TIFY override rules with `!important` flags preserved
    - src/css/input.css does NOT contain `rgba(41,98,255` anywhere (COL-03: blue accent eliminated)
    - src/css/input.css does NOT contain `#2c3e50` anywhere (COL-03: dark blue eliminated)
    - src/css/input.css does NOT contain `#003660` anywhere (navy eliminated)
    - src/css/input.css does NOT contain `#f18e00` or `#F2784B` anywhere (COL-04: orange eliminated)
    - src/css/input.css does NOT contain `var(--accent-primary)`, `var(--accent-hover)`, `var(--accent-link)`, `var(--accent-overlay)` (old variable names eliminated)
    - src/js/search.js does NOT contain `rgba(41,98,255` or `#f18e00` or `#F2784B` (JS inline style colours updated)
    - src/js/tree.js does NOT contain `rgba(41,98,255` or `#2c3e50` (JS inline style colours updated)
    - src/js/description.js does NOT contain hardcoded hex colour values for accent colours
    - All class names used in JS `className` assignments exist as rules in src/css/input.css @layer components
  </acceptance_criteria>
  <done>All 5 remaining CSS sections are migrated into @layer components, all legacy colour values are eliminated from CSS and JS, and JS-created DOM elements retain their class-name styling</done>
</task>

<task type="auto">
  <name>Task 2: Convert search and description templates to Tailwind utility classes</name>
  <files>src/buscar.njk, src/description.njk</files>
  <read_first>
    - src/buscar.njk (current search page template — read full file)
    - src/description.njk (current description page template — read full file)
    - src/css/input.css (to see available @layer components classes — read full file after Task 1)
    - .planning/phases/01-css-foundations/01-UI-SPEC.md (colour contract, eliminated colours)
    - .planning/phases/01-css-foundations/01-CONTEXT.md (D-03: full migration, D-05: complex components stay as named classes)
  </read_first>
  <action>
Apply the same conversion principles as Plan 02 Task 2:
- Simple styling → Tailwind utility classes directly in HTML
- Complex component classes referenced by JS → keep named class from @layer components
- Named classes with many properties → keep, optionally augment with utilities

**1. `src/buscar.njk` (search page):**
- Add utility classes for layout: container, grid/flex for sidebar+results layout
- Search input container: keep `.search-input` named class, add `font-sans` if needed
- Result cards: keep named classes, add text colour utilities (`text-stone-700`, `text-burgundy-light` for links)
- Sidebar: keep named classes for facets, add `text-sm text-stone-600` where appropriate
- Filter pills: keep `.filter-pill` named class (JS creates these dynamically)
- Pagination: keep named class, add `font-sans text-sm`
- All interactive elements that JS manipulates: keep their named classes, do NOT replace with utility-only approaches

**2. `src/description.njk` (description page):**
- Metadata section: add `text-stone-700`, `text-stone-500` for labels/values
- Level badge: keep `.level-badge` named class, add `text-xs rounded-full` if not already in @layer components
- Links: add `text-burgundy hover:text-burgundy-light`
- Section headings: add `font-semibold text-stone-900`
- TIFY viewer container: keep all named classes (JS manages the viewer)
- Notice boxes: keep `.notice-box` named class
- Children tree: keep named classes (JS manages expansion)
- Breadcrumb on this page: already handled by Plan 02's breadcrumb.njk partial

**Desktop-first responsive:** Same as Plan 02 — do NOT add conflicting responsive utility classes. Responsive behaviour comes from @layer components media queries.
  </action>
  <verify>
    <automated>grep -c "text-stone\|text-burgundy\|font-sans\|font-serif\|bg-" src/buscar.njk src/description.njk 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - src/buscar.njk contains at least one Tailwind utility class (e.g., `text-stone-700`, `font-sans`)
    - src/buscar.njk preserves all JS-referenced named classes (`.filter-pill`, `.search-input`, `.pagination`, etc. still appear in class attributes)
    - src/description.njk contains at least one Tailwind utility class
    - src/description.njk preserves all JS-referenced named classes (`.level-badge`, `.notice-box`, TIFY container classes, `.children-tree-*` classes)
    - Tailwind CLI compiles without errors: `./tailwindcss -i src/css/input.css -o src/css/main.css --minify` exits 0
  </acceptance_criteria>
  <done>Search and description page templates converted to Tailwind utility classes while preserving all JS-referenced named classes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete CSS migration</name>
  <files>n/a</files>
  <action>Present the completed migration for human visual inspection across all page types.</action>
  <what-built>Complete CSS migration from vanilla CSS to Tailwind v4. All 20 sections of the 2,489-line main.css have been migrated to input.css with @theme tokens. All 8 templates converted to Tailwind utilities. All JS inline colour values updated. New typefaces (DM Sans, Crimson Text, Cormorant Garamond) loaded. Burgundy/periwinkle palette applied throughout. No legacy blue/orange/navy colours remain.</what-built>
  <how-to-verify>
1. Run `./tailwindcss -i src/css/input.css -o src/css/main.css --minify` — should exit 0
2. Run `bash scripts/check-css-tokens.sh` — all checks should pass
3. Run `npx eleventy --serve` (in a separate terminal) and open http://localhost:8080
4. **Homepage** — verify: DM Sans body text, burgundy search button in hero, masonry grid with burgundy hover overlay, warm white (#FAFAF9) background
5. **Header** — verify: "Neogranadina: Zasqua" in Crimson Text (serif), nav links in DM Sans, no blue or orange colours
6. **Footer** — verify: dark burgundy background (#4A1522), white text, periwinkle link hovers
7. **Search page** (/buscar/) — verify: periwinkle filter pills, burgundy active pagination, no blue accents, search input with rounded pill shape
8. **Any description page** — verify: periwinkle level badge, burgundy links, TIFY viewer renders correctly with overrides intact
9. **Repository page** — verify: correct colours, Miller columns (if visible) use burgundy selection highlight
10. **Browser DevTools → Computed Styles** — verify body font-family is "DM Sans" not "Lato"
11. **Browser DevTools → Network → Fonts** — verify DM Sans, Crimson Text, Cormorant Garamond loaded; NO Lato or IM Fell DW Pica
  </how-to-verify>
  <verify>
    <automated>bash scripts/check-css-tokens.sh</automated>
  </verify>
  <done>Human confirms visual rendering matches the new visual identity across all page types</done>
  <resume-signal>Type "approved" or describe any visual issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. `./tailwindcss -i src/css/input.css -o src/css/main.css --minify` exits 0
2. `bash scripts/check-css-tokens.sh` — all checks pass (COL-01 through COL-04)
3. `grep -ci "rgba(41,98,255\|2c3e50\|003660\|f18e00\|F2784B\|2962FF" src/css/input.css` returns 0
4. `grep -ci "var(--accent-\|var(--footer-bg\|var(--bg-color\|var(--bg-gray\|var(--text-color\|var(--text-dark\|var(--text-muted\|var(--font-body\|var(--font-heading\|var(--font-logo\|var(--spacing-" src/css/input.css` returns 0 (no old variable names)
5. `grep "DM+Sans" src/_layouts/base.njk` returns a match
6. `grep -c "Lato\|IM.Fell" src/_layouts/base.njk` returns 0
7. `npx eleventy` exits 0 (full build succeeds)
</verification>

<success_criteria>
- Zero legacy colour variables (--accent-*, --footer-bg, etc.) in input.css
- Zero blue accent colours (rgba(41,98,255), #2c3e50, #003660, #2962FF) in CSS or JS files
- Zero orange hover colours (#f18e00, #F2784B) in CSS or JS files
- All JS-created DOM elements retain their class-name styling (miller-item, filter-pill, search-input, etc.)
- TIFY viewer overrides preserved with !important flags
- Search and description templates use Tailwind utility classes
- Full Eleventy build succeeds
- Human visual verification confirms correct rendering of all page types
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-foundations/01-03-SUMMARY.md`
</output>
