---
phase: 01-css-foundations
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/css/input.css
  - src/_layouts/base.njk
  - src/_includes/header.njk
  - src/_includes/footer.njk
  - src/_includes/breadcrumb.njk
  - src/index.njk
  - src/repository.njk
  - src/404.njk
autonomous: true
requirements: [COL-02, COL-04]

must_haves:
  truths:
    - "The header renders with correct burgundy/periwinkle colours and DM Sans navigation"
    - "The footer renders with dark burgundy (#4A1522) background instead of navy (#003660)"
    - "The homepage hero renders with burgundy button and masonry grid"
    - "No orange hover colours (#f18e00, #F2784B) remain in the compiled CSS"
    - "All structural shell components (header, footer, hero, cards, breadcrumb, buttons) are styled via Tailwind"
  artifacts:
    - path: "src/css/input.css"
      provides: "@layer components rules for layout shell sections (header, footer, hero, masonry, buttons, breadcrumb, cards, detail, repository, description list, error, placeholder, utilities)"
      contains: ".site-header"
    - path: "src/_includes/header.njk"
      provides: "Header template with Tailwind utility classes"
      contains: "bg-"
    - path: "src/_includes/footer.njk"
      provides: "Footer template with Tailwind utility classes"
      contains: "bg-burgundy-dark"
    - path: "src/index.njk"
      provides: "Homepage template with Tailwind utility classes"
      contains: "bg-burgundy"
  key_links:
    - from: "src/_includes/header.njk"
      to: "src/css/input.css"
      via: "Tailwind utility classes + @layer components named classes"
      pattern: "site-header|bg-|text-"
    - from: "src/_includes/footer.njk"
      to: "src/css/input.css"
      via: "bg-burgundy-dark replacing --footer-bg"
      pattern: "burgundy-dark"
    - from: "src/index.njk"
      to: "src/css/input.css"
      via: "Hero and masonry component classes"
      pattern: "hero|masonry"
---

<objective>
Migrate the layout shell CSS sections into Tailwind — header, footer, hero, homepage, masonry grid, buttons, breadcrumb, cards, detail page, repository header, description list, placeholder, utilities, and error page. Convert corresponding templates to Tailwind utility classes. Eliminate all orange hover colours and replace structural colour references with brand tokens.

Purpose: After this plan, the site's structural shell renders in the new visual identity. The remaining work (Plan 03) covers only the complex interactive components — search, Miller columns, description page, and TIFY viewer.

Output: Updated input.css with ~14 CSS sections migrated into @layer components, and 6 templates converted to Tailwind utility classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-css-foundations/01-CONTEXT.md
@.planning/phases/01-css-foundations/01-RESEARCH.md
@.planning/phases/01-css-foundations/01-UI-SPEC.md
@.planning/phases/01-css-foundations/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — the input.css structure the executor will extend -->
The executor will find input.css with:
- @import "tailwindcss" source("../src");
- @theme block with 9 colour tokens + 3 font tokens
- :root block with --max-width and --header-height
- @layer base with html/body/heading/anchor/img resets
- Empty @layer components block (to be populated by this plan)

Available Tailwind utility classes from @theme tokens:
- Colours: bg-burgundy, bg-burgundy-deep, bg-burgundy-light, bg-burgundy-dark, bg-pale-rose, bg-ochre, bg-sage, bg-periwinkle, bg-bg, text-burgundy, text-burgundy-light, etc.
- Fonts: font-sans, font-serif, font-display
- Neutrals: stone-50 through stone-900 (Tailwind built-in)

Layout constants (plain CSS custom properties, NOT utilities):
- var(--max-width) = 1200px
- var(--header-height) = 70px
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate layout shell CSS into @layer components</name>
  <files>src/css/input.css</files>
  <read_first>
    - src/css/input.css (current state from Plan 01 — read the full file)
    - src/css/main.css (the OLD file before Plan 01 overwrote it — if Plan 01 has already run and overwritten it, read from git: `git show HEAD~1:src/css/main.css` or check the Plan 01 SUMMARY for section content. The executor MUST have access to the original 2,489-line file to migrate sections)
    - src/js/search.js (to identify class names that must be preserved — only scan for className assignments, not full logic)
    - src/js/tree.js (to identify class names that must be preserved)
    - src/js/description.js (to identify class names that must be preserved)
    - .planning/phases/01-css-foundations/01-RESEARCH.md (Pattern 3: @layer components, anti-patterns)
    - .planning/phases/01-css-foundations/01-UI-SPEC.md (colour contract, eliminated colours table)
    - ../docs/frontend/guidelines/design-tokens.md (canonical token values)
  </read_first>
  <action>
**CRITICAL PREREQUISITE:** Before starting, ensure you have access to the ORIGINAL `src/css/main.css` content (2,489 lines). If Plan 01 has already overwritten it with Tailwind output, retrieve the original via `git show HEAD~1:src/css/main.css` or from the last commit before Plan 01. You need the original CSS rules to migrate them.

**Strategy:** Read each section of the old main.css and rewrite its rules into the `@layer components` block in input.css. For each rule:
- Replace hardcoded hex values with `var(--color-*)` references or `@apply` with Tailwind token classes
- Replace `var(--accent-primary)` (blue) with `var(--color-burgundy-deep)` or `@apply bg-burgundy-deep`
- Replace `var(--accent-hover)` (orange #f18e00) with `var(--color-periwinkle)` or `var(--color-burgundy)`
- Replace `var(--accent-link)` (orange #F2784B) with `var(--color-burgundy-light)`
- Replace `var(--accent-overlay)` (blue rgba) with `var(--color-burgundy-deep)` with appropriate opacity
- Replace `var(--footer-bg)` (#003660 navy) with `var(--color-burgundy-dark)` (#4A1522)
- Replace `var(--font-body)` / `var(--font-heading)` with `var(--font-sans)` (or `@apply font-sans`)
- Replace `var(--font-logo)` with `var(--font-serif)` (Crimson Text)
- Replace `var(--font-serif)` references to Cormorant Garamond with `var(--font-display)`
- Replace `var(--bg-color)` (#fff) with `var(--color-bg)` (#FAFAF9)
- Replace `var(--bg-gray)` (#f2f2f2) with Tailwind `stone-100` equivalent
- Replace `var(--text-color)` (#757575) with `@apply text-stone-500` or `color: theme(colors.stone.500)`
- Replace `var(--text-dark)` (#000) with `@apply text-stone-900` or `color: theme(colors.stone.900)`
- Replace `var(--text-muted)` (#858585) with `@apply text-stone-400`
- Replace `var(--spacing-*)` with direct rem values or Tailwind spacing utilities in @apply

**Sections to migrate (in order, all go into `@layer components`):**

1. **Layout** (lines 92-105): `.container` class — use `var(--max-width)`, keep `margin: 0 auto`, `padding: 0 15px`

2. **Header** (lines 106-268): `.site-header`, `.header-inner`, `.site-title`, `.nav-links`, `.search-icon-link`, `.hamburger-*` classes. Replace `var(--footer-bg)` references with `var(--color-burgundy-dark)`. Replace font-logo references with `font-serif`. Nav link hover: use `var(--color-periwinkle)` instead of orange.

3. **Hero** (lines 269-374): `.hero-*` classes. Replace blue button colour with `var(--color-burgundy-deep)`. Replace hero overlay/gradient blues with burgundy tones. Search form button: `var(--color-burgundy-deep)` background, `var(--color-periwinkle)` on hover.

4. **Home Sections** (lines 375-409): `.home-*` classes. Replace any hardcoded colours with token references.

5. **Masonry Grid** (lines 410-538): `.repo-grid`, `.masonry-item`, caption/hover overlay classes. Replace `var(--accent-overlay)` (blue rgba) with burgundy overlay: `rgba(107, 31, 51, 0.85)` (burgundy-deep with opacity). Replace any orange hovers with periwinkle.

6. **Footer** (lines 539-599): `.site-footer`, `.footer-*` classes. Background: `var(--color-burgundy-dark)` replacing `var(--footer-bg)` (#003660). Text: white. Link hover: `var(--color-periwinkle)`.

7. **Buttons** (lines 600-626): `.btn`, `.btn-primary`, `.btn-outline` classes. Primary button: `var(--color-burgundy-deep)` background, `var(--color-burgundy)` on hover. Outline button: `var(--color-burgundy)` border.

8. **Breadcrumb** (lines 627-655): `.breadcrumb`, `.breadcrumb-*` classes. Replace any hardcoded colours.

9. **Cards** (lines 656-670): `.card`, `.card-*` classes. Replace colours.

10. **Detail Page** (lines 671-727): `.detail-*` classes. Replace colours.

11. **Repository Header** (lines 728-777): `.repo-header-*` classes. Replace colours.

12. **Description List** (lines 778-830): `.desc-*` classes. Replace colours.

13. **Placeholder Image** (lines 1665-1683): `.placeholder-*` classes. Replace colours.

14. **Utilities** (lines 1684-1695): `.text-center`, `.mt-*`, `.mb-*` classes — these can likely be dropped since Tailwind provides them natively. Only keep if they have custom values not covered by Tailwind utilities.

15. **Error Page** (lines 2442-end): `.error-page` classes. Replace colours.

**Rules for class names referenced by JavaScript:**
Before removing or renaming any class, check if it appears in `src/js/search.js`, `src/js/tree.js`, or `src/js/description.js`. If a JS file creates elements with a class name (e.g., `element.className = 'masonry-item'`), that class MUST exist in `@layer components`. Do NOT convert JS-referenced classes to utility-only approaches.

For this plan, focus on the structural shell classes. The search/Miller/TIFY/description-page/children-tree sections are handled in Plan 03.

**Colour replacement reference (complete mapping):**

| Old Variable/Value | New Token/Value |
|---|---|
| `var(--accent-primary)` / `rgba(41,98,255,0.8)` | `var(--color-burgundy-deep)` / `#6B1F33` |
| `var(--accent-hover)` / `#f18e00` | `var(--color-periwinkle)` / `#C9D5FF` or `var(--color-burgundy)` |
| `var(--accent-link)` / `#F2784B` | `var(--color-burgundy-light)` / `#B14D66` |
| `var(--accent-overlay)` / `rgba(41,98,255,0.8)` | `rgba(107,31,51,0.85)` (burgundy-deep with opacity) |
| `var(--footer-bg)` / `#003660` | `var(--color-burgundy-dark)` / `#4A1522` |
| `var(--bg-color)` / `#ffffff` | `var(--color-bg)` / `#FAFAF9` |
| `var(--bg-gray)` / `#f2f2f2` | `#F5F5F4` (stone-100) |
| `var(--text-color)` / `#757575` | `#78716C` (stone-500) |
| `var(--text-dark)` / `#000000` | `#1C1917` (stone-900) |
| `var(--text-muted)` / `#858585` | `#A8A29E` (stone-400) |
| `#2962FF` (any remaining blue) | `var(--color-burgundy-deep)` |
| `#2c3e50` (dark blue) | `var(--color-burgundy-dark)` |

**Do NOT touch these sections (reserved for Plan 03):**
- Search Results (lines 831-946)
- Search Page (lines 947-1664)
- Miller Columns Tree (lines 1696-1969)
- Description Page (lines 1970-2306)
- Children Tree (lines 2307-2441)
  </action>
  <verify>
    <automated>grep -c "@layer components" src/css/input.css && grep -c "site-header" src/css/input.css && grep -c "site-footer" src/css/input.css && grep -c "hero" src/css/input.css && grep -c "f18e00\|F2784B\|003660" src/css/input.css</automated>
  </verify>
  <acceptance_criteria>
    - src/css/input.css contains `.container` class in @layer components with `var(--max-width)`
    - src/css/input.css contains `.site-header` class in @layer components
    - src/css/input.css contains `.site-footer` class with `var(--color-burgundy-dark)` (NOT #003660 or var(--footer-bg))
    - src/css/input.css contains `.hero-*` classes in @layer components
    - src/css/input.css contains `.masonry-item` and related masonry classes
    - src/css/input.css contains `.btn` and `.btn-primary` classes
    - src/css/input.css contains `.breadcrumb` class
    - src/css/input.css contains `.card` class
    - src/css/input.css does NOT contain `#f18e00` or `#F2784B` (orange hovers eliminated)
    - src/css/input.css does NOT contain `#003660` (navy footer eliminated)
    - src/css/input.css does NOT contain `rgba(41,98,255` (blue accent eliminated) — in these sections
    - src/css/input.css does NOT contain `var(--accent-primary)`, `var(--accent-hover)`, `var(--accent-link)`, `var(--accent-overlay)`, `var(--footer-bg)`, `var(--bg-color)`, `var(--bg-gray)`, `var(--text-color)`, `var(--text-dark)`, `var(--text-muted)`, `var(--font-body)`, `var(--font-heading)`, `var(--font-logo)` (old variable names eliminated)
    - src/css/input.css does NOT contain `var(--spacing-sm)`, `var(--spacing-md)`, `var(--spacing-lg)`, `var(--spacing-xl)`, `var(--spacing-xxl)` (old spacing vars eliminated)
    - The sections for Search, Miller Columns, Description Page, and Children Tree are NOT yet in input.css (reserved for Plan 03)
  </acceptance_criteria>
  <done>All structural shell CSS sections (layout, header, footer, hero, masonry, buttons, breadcrumb, cards, detail, repository, description list, placeholder, utilities, error) are migrated into @layer components with all colour tokens updated to the new palette</done>
</task>

<task type="auto">
  <name>Task 2: Convert layout shell templates to Tailwind utility classes</name>
  <files>src/_layouts/base.njk, src/_includes/header.njk, src/_includes/footer.njk, src/_includes/breadcrumb.njk, src/index.njk, src/repository.njk, src/404.njk</files>
  <read_first>
    - src/_layouts/base.njk (current state — read full file)
    - src/_includes/header.njk (current template — read full file)
    - src/_includes/footer.njk (current template — read full file)
    - src/_includes/breadcrumb.njk (current template — read full file)
    - src/index.njk (current homepage template — read full file)
    - src/repository.njk (current repository template — read full file)
    - src/404.njk (current error page template — read full file)
    - src/css/input.css (to see which classes are in @layer components — read full file)
    - .planning/phases/01-css-foundations/01-UI-SPEC.md (colour and typography contracts)
    - .planning/phases/01-css-foundations/01-CONTEXT.md (D-03: full migration, D-05: complex components stay as named classes)
  </read_first>
  <action>
Convert templates from old CSS class-only approach to Tailwind utility classes where appropriate, per D-03 (full migration) and D-05 (complex components stay as named classes in @layer components).

**Conversion principles:**
- Simple styling (colours, spacing, typography, display, flex) → Tailwind utility classes directly in the template HTML
- Complex component classes referenced by JS → keep the named class, style it in @layer components
- Named classes with >5 CSS properties that would create very long class strings → keep as @layer components classes, optionally add utilities alongside
- The `.container` class stays as a named class (used throughout, defined in @layer components)

**For each template, apply these changes:**

**1. `src/_layouts/base.njk`:**
- Add `class="bg-bg font-sans text-stone-600"` to `<body>` tag (mirrors @layer base but makes it explicit)
- Add `class="min-h-screen"` or equivalent to `<main>` if needed for layout
- The `<link rel="stylesheet" href="/css/main.css">` stays unchanged (Tailwind compiles to this path)

**2. `src/_includes/header.njk`:**
- Apply utility classes for header layout: `flex items-center justify-between`, spacing, colours
- Site title/logo: add `font-serif` for Crimson Text wordmark
- Nav links: add `font-sans text-sm` for DM Sans navigation
- Hover states: where possible use `hover:text-periwinkle` or `hover:text-burgundy-light`
- Keep `.site-header`, `.header-inner`, `.nav-links` etc. as named classes (they have many properties in @layer components) but ADD utility classes alongside for colour and typography

**3. `src/_includes/footer.njk`:**
- Add `bg-burgundy-dark text-white` to the footer container
- Footer links: `text-white hover:text-periwinkle`
- Keep `.site-footer` as a named class but augment with utilities

**4. `src/_includes/breadcrumb.njk`:**
- Add `text-sm text-stone-500` and spacing utilities
- Breadcrumb links: `text-stone-700 hover:text-burgundy-light`

**5. `src/index.njk`:**
- Hero section: add `bg-burgundy-deep text-white` or similar for hero background
- Hero search button: `bg-burgundy-deep hover:bg-periwinkle text-white`
- Section headings: add `font-display font-semibold` for Cormorant Garamond display headings where appropriate
- Repository grid / masonry items: keep named classes (`.masonry-item`, `.repo-grid`) as they are complex components. Add colour utilities where simple

**6. `src/repository.njk`:**
- Apply heading and text colour utilities
- Repository header: combine named class with `text-stone-900` etc.

**7. `src/404.njk`:**
- Apply utility classes for error page layout and colours

**Desktop-first responsive approach preserved:** The existing responsive design uses `@media (max-width: ...)` in CSS. In Tailwind v4, the default responsive prefixes are mobile-first (`md:`, `lg:`). Since the @layer components block preserves the desktop-first media queries from the original CSS, do NOT add conflicting responsive utility classes in templates. The responsive behaviour continues to come from @layer components.

**Do NOT touch:** `src/buscar.njk` and `src/description.njk` — these are handled in Plan 03.
  </action>
  <verify>
    <automated>grep -c "font-sans\|font-serif\|font-display\|bg-burgundy\|text-stone\|text-burgundy\|bg-periwinkle\|bg-bg" src/_includes/header.njk src/_includes/footer.njk src/index.njk src/404.njk 2>/dev/null | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - src/_includes/header.njk contains at least one Tailwind utility class (e.g., `font-serif`, `text-sm`, `bg-`)
    - src/_includes/header.njk contains `font-serif` for the site title/wordmark (Crimson Text per D-07)
    - src/_includes/footer.njk contains `bg-burgundy-dark` (dark burgundy footer per COL-01)
    - src/_includes/footer.njk contains `text-white` for footer text
    - src/_includes/breadcrumb.njk contains `text-sm` or `text-stone-` utility classes
    - src/index.njk contains `bg-burgundy` or `bg-burgundy-deep` for hero/button elements
    - src/404.njk contains at least one Tailwind utility class
    - src/repository.njk contains at least one Tailwind utility class
    - src/buscar.njk is UNCHANGED (reserved for Plan 03)
    - src/description.njk is UNCHANGED (reserved for Plan 03)
    - Tailwind CLI compiles without errors after template changes: `./tailwindcss -i src/css/input.css -o src/css/main.css --minify` exits 0
  </acceptance_criteria>
  <done>All layout shell templates (base, header, footer, breadcrumb, index, repository, 404) use Tailwind utility classes for colours, typography, and spacing while preserving named @layer components classes for complex components</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `./tailwindcss -i src/css/input.css -o src/css/main.css --minify` exits 0
2. `grep -ci "f18e00\|F2784B" src/css/input.css` returns 0 (no orange hovers)
3. `grep -ci "003660" src/css/input.css` returns 0 (no navy footer)
4. `grep -c "site-header\|site-footer\|hero\|masonry\|btn\|breadcrumb\|container" src/css/input.css` returns at least 7 (all structural classes present)
5. `grep -c "font-serif\|bg-burgundy-dark\|text-stone" src/_includes/header.njk src/_includes/footer.njk` returns at least 2
6. `bash scripts/check-css-tokens.sh` — COL-04 check passes (no orange hovers in compiled output)
</verification>

<success_criteria>
- All 14 structural CSS sections migrated into @layer components with new colour tokens
- No orange hover colours (#f18e00, #F2784B) anywhere in input.css
- No navy footer colour (#003660) anywhere in input.css
- No old CSS variable names (--accent-*, --footer-bg, --bg-color, --font-body, etc.) in input.css
- Footer background is dark burgundy (#4A1522)
- Header wordmark uses font-serif (Crimson Text)
- All 6 layout shell templates converted to use Tailwind utility classes
- Tailwind compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-css-foundations/01-02-SUMMARY.md`
</output>
