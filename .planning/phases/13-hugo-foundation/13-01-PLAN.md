---
phase: 13-hugo-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - exports/
  - data/
  - scripts/precompute-links.js
  - scripts/places-to-geojson.js
  - build.sh
  - package.json
  - vitest.config.js
  - tests/enrichment/date-format.test.js
  - tests/enrichment/ancestor-chain.test.js
  - tests/enrichment/link-enrichment.test.js
  - tests/enrichment/enriched-counts.test.js
  - tests/build/url-scheme.test.js
  - tests/build/css-compiled.test.js
  - tests/fixtures/descriptions.sample.json
  - tests/fixtures/entities.sample.json
  - tests/fixtures/places.sample.json
  - .nvmrc
autonomous: false
requirements:
  - ENRICH-05
user_setup:
  - service: hugo-extended
    why: "Hugo Extended v0.160.1+ is required to build the site; not currently installed on the dev machine"
    dashboard_config:
      - task: "Install via `brew install hugo`; confirm `hugo version` output contains `+extended`"
        location: "local dev machine"

must_haves:
  truths:
    - "Hugo Extended v0.160.1+ is installed and verified on the dev machine"
    - "The repository's build-time export directory is `exports/` (renamed from `data/`); no large JSON files remain at the project root's `data/` path"
    - "vitest is installed and a baseline test suite runs (all tests initially fail or are skipped — they will be filled in by Plans 02 and 05)"
    - "Test fixtures exist with real record shapes so Plan 02 can implement against them"
  artifacts:
    - path: "exports/descriptions.json"
      provides: "Renamed B2 export landing file (was data/descriptions.json)"
    - path: "exports/entities.json"
      provides: "Renamed B2 export landing file"
    - path: "exports/places.json"
      provides: "Renamed B2 export landing file"
    - path: "exports/repositories.json"
      provides: "Renamed B2 export landing file"
    - path: "exports/desc-entity-lookup.json"
      provides: "Renamed precompute-links output"
    - path: "exports/desc-place-lookup.json"
      provides: "Renamed precompute-links output"
    - path: "vitest.config.js"
      provides: "Vitest configuration pinning test roots and environment"
      min_lines: 8
    - path: "tests/enrichment/date-format.test.js"
      provides: "Golden-file RED test for formatDateNarrative port (ENRICH-02)"
      min_lines: 25
    - path: "tests/enrichment/ancestor-chain.test.js"
      provides: "RED test asserting every description carries ancestor_chain (ENRICH-01)"
    - path: "tests/enrichment/link-enrichment.test.js"
      provides: "RED test asserting display_name + role_label on entity/place links (ENRICH-03)"
    - path: "tests/enrichment/enriched-counts.test.js"
      provides: "RED test asserting enriched JSON record counts match canonical totals (ENRICH-04)"
    - path: "tests/build/url-scheme.test.js"
      provides: "RED test for D-18 flat-code URL scheme (to be exercised in Wave 3)"
    - path: "tests/build/css-compiled.test.js"
      provides: "RED test for Tailwind v4 output detection (to be exercised in Wave 3)"
    - path: "tests/fixtures/descriptions.sample.json"
      provides: "Small real-shape fixture (≤ 10 records taken from exports/descriptions.json) for isolated tests"
  key_links:
    - from: "scripts/precompute-links.js"
      to: "exports/"
      via: "fs read/write paths"
      pattern: "exports/"
    - from: "build.sh"
      to: "exports/"
      via: "B2 download target directory"
      pattern: "exports/"
    - from: "package.json scripts.test"
      to: "vitest"
      via: "npm run test"
      pattern: "vitest run"
---

<objective>
Prepare the Phase 13 foundation: verify Hugo Extended is installed, rename the build-time export directory from `data/` → `exports/` so Hugo's default `dataDir = "data"` can host the small UI-strings lookup without colliding with 208 MB archival exports, install vitest, and write RED test stubs that encode invariants I1–I8 from RESEARCH.md.

Purpose: Wave 0 exists so the entire rest of the phase has a clean launchpad — a Hugo binary that actually works, a directory layout that won't OOM Hugo on first run, and failing tests that Plans 02–05 will turn green. Without this, Plan 03 cannot even start `hugo` without triggering the OOM profile the migration is escaping.

Output:
- A renamed `exports/` directory holding all prior `data/*.json` and subdirectory contents
- All script references updated (`precompute-links.js`, `places-to-geojson.js`, `build.sh`, the Eleventy passthrough patterns in `eleventy.config.js` are left alone — Plan 05 deletes eleventy.config.js)
- `vitest` dev-dependency installed, `vitest.config.js` created, six failing test files scaffolded
- Real-shape JSON fixtures for isolated testing (sampled from the existing exports, so downstream tests don't need the full 208 MB)
- `.nvmrc` verified to be `22` (create if missing)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-hugo-foundation/13-CONTEXT.md
@.planning/phases/13-hugo-foundation/13-RESEARCH.md
@../docs/guidelines/code-conventions.md

<interfaces>
<!-- Existing scripts whose `data/` references must change to `exports/`.
     Executors should read these files in full before editing; the patterns
     are scattered across multiple lines. -->

From scripts/precompute-links.js:
  - Reads `data/descriptions.json`, `data/entities.json`, `data/places.json`, `data/entity_links.json`, `data/place_links.json`
  - Writes to `data/children/`, `data/entity-links/`, `data/place-links/`, `data/desc-entity-lookup.json`, `data/desc-place-lookup.json`, `data/entity-index.json`, `data/place-index.json`, `data/curated-entity-graph.json`
  - Replace every `data/` path literal with `exports/` — but keep the file names the same. The client JS still fetches `/data/children/...` at runtime; that URL-visible `/data/` is served by Hugo's `static/data/` in Plan 04, NOT from the renamed `exports/` directory. Do not conflate the two.

From scripts/places-to-geojson.js:
  - Reads `data/places.json`, writes `data/places.geojson` (used by PMTiles pipeline). Update paths to `exports/`.

From build.sh:
  - Contains `b2 sync ... data/` and `eleventy` invocations. Update the `b2 sync` target path from `data/` to `exports/`. Do NOT yet rewrite the Eleventy lines — Plan 05 rewrites the full pipeline.

From eleventy.config.js (no changes this plan — Plan 05 deletes it):
  - Has `addPassthroughCopy({ "data/children": "data/children" })`. Leave alone.

From data/ (the current B2-download directory, verified 2026-04-16):
  - descriptions.json — 106,529 records (source of truth for fixtures)
  - entities.json — 78,476 records
  - places.json — 6,722 records
  - repositories.json, entity_links.json, place_links.json, desc-entity-lookup.json, desc-place-lookup.json, entity-index.json, place-index.json
  - Subdirectories: children/, entity-links/, place-links/
  - zasqua-places.pmtiles, places.geojson, curated-entity-graph.json

Canonical golden-file cases for `formatDateNarrative` (from eleventy.config.js:37-64, verified 2026-04-16):
  "1723-03-15"                 → "15 de marzo de 1723"
  "1723-03"                    → "marzo de 1723"
  "1723"                       → "1723"
  "1723-03-15 .. 1725-12-01"   → "15 de marzo de 1723 – 1 de diciembre de 1725"
  ""                           → ""
  "fecha desconocida"          → "fecha desconocida"
  null                         → ""
  "1723-03-15 .. 1723-03-15"   → "15 de marzo de 1723 – 15 de marzo de 1723"

Note the range separator: input " .. " (space-dot-dot-space), output " – " (space-endash-space, U+2013).
Month names (lowercase, Colombian Spanish): enero, febrero, marzo, abril, mayo, junio, julio, agosto, septiembre, octubre, noviembre, diciembre.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install Hugo Extended and verify</name>
  <files>(none — human-only action: local toolchain install)</files>
  <read_first>
    - .planning/phases/13-hugo-foundation/13-RESEARCH.md §"Environment Availability" and §"Pitfall 4: Hugo non-extended binary"
  </read_first>
  <what-built>
    Hugo is NOT currently installed on the dev machine (verified 2026-04-16 by the researcher). Every subsequent task in Phase 13 depends on a working `hugo` binary of the Extended variant at version ≥ 0.160.1. Installing Hugo itself is a one-off local machine action that Claude cannot script into CI-free execution without homebrew permissions.
  </what-built>
  <action>
    Pause execution. Ask the human to perform these exact steps on the dev machine and paste results:

    1. `brew install hugo` (or `brew upgrade hugo` if already installed)
    2. `hugo version` — output MUST contain `+extended` and a version ≥ `0.160.1`
    3. `node --version` — must begin with `v22.`

    If `hugo version` lacks `+extended`, reinstall via `brew install hugo --HEAD` or download the Extended tarball from https://github.com/gohugoio/hugo/releases/tag/v0.160.1.
    If node is wrong: `nvm install 22 && nvm use 22`.

    Do not proceed to Task 2 until both outputs are confirmed in the chat.
  </action>
  <how-to-verify>
    Run the following commands locally and paste outputs back into the chat:

    1. `brew install hugo` (or `brew upgrade hugo` if already installed)
    2. `hugo version`
    3. The output MUST contain both `+extended` and a version ≥ `0.160.1`. Example acceptable output: `hugo v0.161.0+extended darwin/arm64 BuildDate=...`.

    If `hugo version` output does NOT contain `+extended`, uninstall and reinstall via `brew install hugo --HEAD` or download the Extended release tarball from https://github.com/gohugoio/hugo/releases/tag/v0.160.1 and install manually.

    Also confirm Node.js: `node --version` must print `v22.x.x` (matches `.nvmrc`). If not, `nvm install 22 && nvm use 22`.
  </how-to-verify>
  <verify>
    <automated>hugo version | grep -q '+extended' && node --version | grep -q '^v22\.'</automated>
  </verify>
  <done>Human has pasted both `hugo version` (contains `+extended`) and `node --version` (starts with `v22.`) into chat; both verify commands pass locally.</done>
  <acceptance_criteria>
    - User has pasted `hugo version` output into chat; output contains `+extended`
    - User has pasted `node --version` output; output begins with `v22.`
  </acceptance_criteria>
  <resume-signal>Paste `hugo version` and `node --version` outputs to continue.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rename data/ → exports/ and update script path references</name>
  <files>exports/, data/, scripts/precompute-links.js, scripts/places-to-geojson.js, build.sh</files>
  <read_first>
    - scripts/precompute-links.js (all 276 lines — path references are scattered across read and write operations)
    - scripts/places-to-geojson.js (full file)
    - build.sh (full file — look for `b2 sync`, any literal `data/` path in shell args)
    - .planning/phases/13-hugo-foundation/13-RESEARCH.md §"Pitfall 1: data/ directory collision"
    - eleventy.config.js (read only — do NOT modify; Plan 05 deletes it)
  </read_first>
  <behavior>
    - After the rename, `ls exports/` lists every file and subdirectory that was previously in `data/` at the project root
    - `ls data/` returns "No such file or directory" (directory is gone — Plan 03 re-creates an empty `data/` for Hugo's `ui.yaml` later)
    - `node scripts/precompute-links.js` runs without "ENOENT data/descriptions.json" errors; it reads from `exports/descriptions.json` and writes outputs to `exports/children/`, `exports/entity-links/`, etc. (Do not run it yet — just the grep-level verification below.)
    - `grep -rn "data/" scripts/precompute-links.js scripts/places-to-geojson.js build.sh` finds zero remaining path literals pointing at `data/` for file I/O (only `exports/` occurrences; documentation comments referring to the conceptual "data pipeline" are fine)
  </behavior>
  <action>
    1. `git mv data exports` (use `git mv` so history follows the rename; if git complains about untracked contents, fall back to `mv data exports && git add exports`)
    2. In `scripts/precompute-links.js`, replace every file I/O path literal `data/` → `exports/`. Concretely, change:
       - Read paths: `data/descriptions.json`, `data/entities.json`, `data/places.json`, `data/entity_links.json`, `data/place_links.json`
       - Write paths: `data/children/`, `data/entity-links/`, `data/place-links/`, `data/desc-entity-lookup.json`, `data/desc-place-lookup.json`, `data/entity-index.json`, `data/place-index.json`, `data/curated-entity-graph.json`
       All become `exports/...`. Use the Edit tool per-occurrence; do NOT sed-replace blindly (comments may mention "data" in prose).
    3. In `scripts/places-to-geojson.js`, change `data/places.json` → `exports/places.json` and `data/places.geojson` → `exports/places.geojson`.
    4. In `build.sh`, change the `b2 sync` destination from `data/` to `exports/`. Leave the `eleventy` build steps untouched — Plan 05 rewrites those.
    5. Add a narrative header + version footer per `../docs/guidelines/code-conventions.md` to any of the three files that lack one. Use `Version: v1.0.0` in the footer. Do not touch files that already have headers.
    6. Update `.gitignore` if it contains `data/` → change to `exports/` (preserving the intent of not committing B2 downloads). Keep `_site/` entries as-is (Plan 05 removes them).
  </action>
  <verify>
    <automated>test ! -d data && test -d exports && test -f exports/descriptions.json && test -f exports/entities.json && test -f exports/places.json && ! grep -Rn --include='*.js' --include='*.sh' -E '["'\''\s](data/)' scripts/precompute-links.js scripts/places-to-geojson.js build.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -d exports` exits 0
    - `test ! -d data` exits 0
    - `exports/descriptions.json`, `exports/entities.json`, `exports/places.json`, `exports/repositories.json` all exist and are readable
    - `grep -c 'exports/' scripts/precompute-links.js` ≥ 10 (every former data/ reference now uses exports/)
    - `grep -En '["'\''\s](data/)' scripts/precompute-links.js scripts/places-to-geojson.js build.sh` returns no matches (zero remaining path literals of the form `"data/...`, `'data/...`, or whitespace-prefixed `data/...`)
    - Any file modified or created has a narrative header + `Version: v1.0.0` footer per `../docs/guidelines/code-conventions.md`
  </acceptance_criteria>
  <done>
    The root-level `data/` directory no longer exists. All scripts read and write under `exports/`. Hugo (when run in a later plan) will not find a `data/` directory at all — so the Hugo `dataDir = "data"` default is free for the small UI-strings file Plan 03 creates.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Install vitest, scaffold config, create real-shape fixtures</name>
  <files>package.json, vitest.config.js, tests/fixtures/descriptions.sample.json, tests/fixtures/entities.sample.json, tests/fixtures/places.sample.json, .nvmrc</files>
  <read_first>
    - package.json (current devDependencies, scripts)
    - exports/descriptions.json (first 20 records — take the first few for fixture extraction; DO NOT commit 208 MB of raw data, only the sample)
    - exports/entities.json, exports/places.json (same — just first 5 records each)
    - .planning/phases/13-hugo-foundation/13-RESEARCH.md §"Validation Architecture" and §"Wave 0 Gaps"
  </read_first>
  <behavior>
    - `npx vitest --version` prints a version number
    - `npx vitest run tests/` exits with code 1 (tests scaffolded but red — expected at this stage)
    - The three fixture files are valid JSON, contain small real samples (3–5 records each) pulled from `exports/*.json`, and preserve the exact field shapes (no synthesized/invented metadata, per CLAUDE.md rule against fabricated archival metadata)
  </behavior>
  <action>
    1. Update `package.json`:
       - Add `"vitest": "^2.1.0"` to `devDependencies`
       - Add `"test": "vitest run"` and `"test:watch": "vitest"` to `scripts`
       - Do NOT remove `@11ty/eleventy` here — Plan 05 handles Eleventy removal
    2. Run `npm install` to install vitest and regenerate `package-lock.json`.
    3. Create `vitest.config.js` with a narrative header (per code-conventions.md) and a minimal config:
       ```js
       import { defineConfig } from 'vitest/config';
       export default defineConfig({
         test: {
           include: ['tests/**/*.test.js'],
           environment: 'node',
           testTimeout: 10000,
         },
       });
       ```
       Add `Version: v1.0.0` footer.
    4. Extract real fixture records from `exports/descriptions.json` (first 3 records that have `parent_reference_code` set so ancestor_chain tests have something to walk), `exports/entities.json` (first 5 records), `exports/places.json` (first 5 records). Write them to `tests/fixtures/descriptions.sample.json`, `tests/fixtures/entities.sample.json`, `tests/fixtures/places.sample.json`. Each file is a JSON array. Add a `README.md` alongside if helpful explaining they are verbatim samples (NOT fabricated metadata).
    5. Create `.nvmrc` if missing, containing the single line `22`.
  </action>
  <verify>
    <automated>npm install --silent && npx vitest --version && test -f vitest.config.js && test -f tests/fixtures/descriptions.sample.json && node -e 'const d=require("./tests/fixtures/descriptions.sample.json"); if(!Array.isArray(d)||d.length<3)process.exit(1); if(!d[0].reference_code)process.exit(2)'</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest --version` prints a non-empty version string
    - `vitest.config.js` exists, has a narrative header, and a `Version: v1.0.0` footer per code-conventions.md
    - `tests/fixtures/descriptions.sample.json` parses as JSON, is an array of ≥ 3 objects, each object has a `reference_code` field
    - `tests/fixtures/entities.sample.json` and `tests/fixtures/places.sample.json` similarly parse as arrays of ≥ 3 objects
    - `cat .nvmrc` prints `22`
    - `package-lock.json` has been updated (git diff shows vitest chain)
  </acceptance_criteria>
  <done>
    Vitest runs. Fixtures exist with real record shapes (no invented metadata). The test-harness foundation is in place for Plans 02 and 05 to fill with green tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Scaffold RED tests for invariants I1–I8</name>
  <files>tests/enrichment/date-format.test.js, tests/enrichment/ancestor-chain.test.js, tests/enrichment/link-enrichment.test.js, tests/enrichment/enriched-counts.test.js, tests/build/url-scheme.test.js, tests/build/css-compiled.test.js</files>
  <read_first>
    - tests/fixtures/descriptions.sample.json (produced in Task 3)
    - .planning/phases/13-hugo-foundation/13-RESEARCH.md §"Validation Architecture" → "Phase Requirements → Test Map"
    - eleventy.config.js:37-64 (`formatDateNarrative` — for the golden-file cases)
    - 13-CONTEXT.md D-15, D-16 (date format invariants)
  </read_first>
  <behavior>
    These are RED tests — they should FAIL when run against the current repo state. Each test file encodes one phase invariant:

    - `date-format.test.js` (I4): imports `formatDateNarrative` from `scripts/generate-content.js` (does not yet exist → import fails → test fails). Runs the 8 golden cases listed in the <interfaces> block above.
    - `ancestor-chain.test.js` (I2): loads `assets/hugo-data/descriptions.json` (does not yet exist → test fails). Asserts every record has `ancestor_chain: Array<{reference_code, title, description_level}>`. Also runs a consistency check on 10 random samples: walking the chain via `parent_reference_code` reconstructs the ancestor_chain.
    - `link-enrichment.test.js` (I2): loads `assets/hugo-data/descriptions.json`. Asserts every `entity_links[*]` has `entity_code`, `display_name`, `role_label`; `place_links[*]` has `place_code`, `display_name`.
    - `enriched-counts.test.js` (I1): loads all three `assets/hugo-data/*.json` files. Asserts:
      - `descriptions.json.length === 106529` (or the DEV_LIMIT if `process.env.DEV_LIMIT` is set)
      - `entities.json.length === 78476`
      - `places.json.length === 6722`
      With DEV_LIMIT, use `Math.min(canonical, Number(process.env.DEV_LIMIT))` as expected length.
    - `url-scheme.test.js` (I3): checks that `public/{smoke-test-reference-code}/index.html` exists and `public/descripcion/` does NOT. Pick a sample reference_code from the fixtures. Will fail until Plan 05 runs the build.
    - `css-compiled.test.js` (I8): after a build, asserts that the compiled CSS under `public/css/` (or wherever Hugo emits it) is > 1 KB and contains at least one class name also present in `public/{smoke-test-reference-code}/index.html`. Will fail until Plan 05.
  </behavior>
  <action>
    1. For each of the six files, create a JavaScript test using vitest's `describe`/`it`/`expect` API.
    2. Every file gets a narrative header per code-conventions.md explaining which invariant it encodes and why it's expected to be red until later plans make it green. Add `Version: v1.0.0` footer.
    3. Use `fs.readFileSync` + `JSON.parse` for file-based assertions (no extra deps needed). For the `url-scheme.test.js` and `css-compiled.test.js`, guard the file reads with `fs.existsSync` checks that throw a clear "expected public/... to exist after build" message — so the failure mode is informative.
    4. For `date-format.test.js`, use `import { formatDateNarrative } from '../../scripts/generate-content.js'`. The import will fail (file does not exist); that IS the red state. Plan 02 Task 1 makes this import resolve. Embed the 8 golden cases verbatim from the <interfaces> block.
    5. For `ancestor-chain.test.js` and `link-enrichment.test.js` and `enriched-counts.test.js`, use `path.resolve(process.cwd(), 'assets/hugo-data/descriptions.json')` etc. These files are created by Plan 02.
    6. For `enriched-counts.test.js`, respect `process.env.DEV_LIMIT`:
       ```js
       const canonical = { descriptions: 106529, entities: 78476, places: 6722 };
       const limit = process.env.DEV_LIMIT ? Number(process.env.DEV_LIMIT) : Infinity;
       const expected = Math.min(canonical.descriptions, limit);
       expect(descriptions.length).toBe(expected);
       ```
    7. For the two `tests/build/*.test.js` files, look for a sentinel file or env var — if `process.env.SKIP_BUILD_TESTS === '1'`, use `it.skip` so developers not running the full pipeline can still run enrichment tests in isolation. Default behaviour (no env var) is to fail loudly when `public/` is missing.
  </action>
  <verify>
    <automated>SKIP_BUILD_TESTS=1 npx vitest run tests/ --reporter=verbose 2>&1 | tee /tmp/vitest-output.txt; grep -q 'Test Files' /tmp/vitest-output.txt && grep -Eq '(failed|skipped)' /tmp/vitest-output.txt</automated>
  </verify>
  <acceptance_criteria>
    - All six test files exist under `tests/enrichment/` and `tests/build/`
    - Each file has a narrative header and `Version: v1.0.0` footer per code-conventions.md
    - `SKIP_BUILD_TESTS=1 npx vitest run tests/enrichment/` exits non-zero (tests are RED — expected at this stage)
    - At least one failure message references a missing `scripts/generate-content.js` export or missing `assets/hugo-data/descriptions.json` file (confirms tests are exercising the right things)
    - `SKIP_BUILD_TESTS=1 npx vitest run tests/build/` shows the two build tests as skipped (not errored)
  </acceptance_criteria>
  <done>
    Six failing test files exist, one per invariant, with golden-file expectations already embedded. Plans 02 and 05 will make them green. No executor downstream will need to derive test expectations — they are already written.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev machine ↔ Backblaze B2 | read-only signed-URL downloads; credentials in local env vars only |
| npm registry ↔ local | new devDependency `vitest` — supply-chain exposure via transitive deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `exports/` B2 download directory | accept | Integrity of exports is a backend/B2 concern (out of frontend scope); `precompute-links.js` and `generate-content.js` will fail loudly on malformed JSON |
| T-13-02 | Elevation of Privilege | vitest supply chain | accept | Vitest is a widely-used testing framework with active maintenance; pin to `^2.1.0` (already audited ecosystem) and rely on `npm audit` in CI (Phase 15) |
| T-13-03 | Information Disclosure | test fixtures committed to git | mitigate | Fixtures contain real archival metadata from public exports — verify no private/internal-only fields leak (all fields are already in the public B2 export; no PII/secrets exist in archival data) |
</threat_model>

<verification>
- All Task 2 verify commands pass
- All Task 3 verify commands pass
- Vitest runs and produces a red output when tests are executed against the repo with no enrichment script yet
- `ls exports/` shows every file formerly in `data/`; `ls data/` fails
- `grep -c '^import\|^const.*require' tests/enrichment/date-format.test.js` shows the import of `formatDateNarrative` from `../../scripts/generate-content.js`
- No new files lack a narrative header + version footer
</verification>

<success_criteria>
Plan 01 is complete when:

1. Hugo Extended v0.160.1+ install verified by the user (checkpoint resolved)
2. `exports/` exists with all former `data/` contents; `data/` does not exist
3. `scripts/precompute-links.js`, `scripts/places-to-geojson.js`, `build.sh` all reference `exports/` (no lingering `data/` file paths)
4. `npx vitest --version` works; six test files scaffolded and RED
5. `tests/fixtures/*.sample.json` contain ≥ 3 real records each (no fabricated metadata)
6. Every modified or created source file has a narrative header + `Version: v1.0.0` footer per `../docs/guidelines/code-conventions.md`
7. Plan 01's `files_modified` list committed in a single commit with message `phase 13-01: prep environment and rename data to exports`
</success_criteria>

<output>
After completion, create `.planning/phases/13-hugo-foundation/13-01-SUMMARY.md` summarising:
- Hugo version verified, Node version verified
- Rename commit SHA and test suite baseline
- Any deviations from the plan (e.g. if a script had a path literal the plan didn't anticipate)
- Pointer forward: Plan 02 ports `generate-content.js` against the RED tests created here
</output>
