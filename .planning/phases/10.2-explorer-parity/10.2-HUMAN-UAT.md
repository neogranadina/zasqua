---
status: resolved
phase: 10.2-explorer-parity
source: [10.2-VERIFICATION.md]
started: 2026-04-12T00:24:00Z
updated: 2026-04-12T00:45:00Z
---

## Current Test

[complete]

## Tests

### 1. Place explorer layout
expected: Explorer-grid renders with map top-left, selected card top-right, filters bottom-left, results bottom-right — matching /entidades/ layout
result: passed — grid structure matches entity explorer; map panel, selected card column, filters, results all in correct grid areas

### 2. Protomaps basemap — place explorer
expected: Terrain-only tiles load — earth tones, water, boundaries, place labels; no roads, buildings, or POIs
result: passed — terrain-only Protomaps tiles render; earth tones, cyan water, boundaries, place labels visible; no roads/buildings/POIs

### 3. Selected place card interaction
expected: Clicking a circle or list item populates card with name, type badge, doc count, authority links, "Ver ficha" link; close button resets to stub
result: passed — card shows name, LUGAR POBLADO badge, doc count, "Ver ficha" link; close button resets to stub text

### 4. Empty state and example place buttons
expected: Empty state overlay visible on first load; clicking example button hides overlay, highlights place, shows card
result: passed (after fix) — initial slug mismatch fixed in commit 44b2950; buttons now match actual place-index.json display_name slugs

### 5. Viewport filter toggle
expected: Button gains active state, label changes to "Filtrando por vista del mapa", map pan/zoom updates results
result: passed (after fix) — viewport URL matching bug fixed in commit 44b2950; button toggles active state, label changes, results filtered by viewport bounds

### 6. Protomaps basemap — place detail page
expected: Terrain-only tiles render on /lugar/{slug}/ pages; burgundy marker at correct coordinates; no console errors
result: passed — Protomaps terrain tiles render; burgundy marker at correct coordinates (40.915, -4.268333 for Abades); "Protomaps © OpenStreetMap" attribution

### 7. Click-to-pin tooltip on place detail marker
expected: Tooltip appears above marker showing name + type; close button and map pan dismiss it
result: passed — tooltip shows bold place name + muted place type; close button (×) visible and functional

### 8. Entity detail accordion role filter
expected: Collapsed accordion (no pills); group header expands; checkbox filters timeline; "Limpiar" appears when roles checked
result: passed — accordion groups collapsed on load; clicking header expands with minus chevron; individual role checkboxes visible inside groups

### 9. Focal node colour — person vs corporate/family
expected: Person focal node burgundy (#8B2942); corporate/family focal node periwinkle (#6666BB)
result: passed — person (A. Obando) shows burgundy focal node; corporate body (A. & T. Meluk & Co.) maps to corporate_body → #6666BB via fixed TYPE_MAP

### 10. Ver en explorador deep-link
expected: Button navigates to /entidades/?entidad=ne-XXXXX; entity auto-loads in explorer graph
result: passed — burgundy CTA button with network icon; href="/entidades/?entidad=ne-00978"; old "Red de entidades" label removed

## Summary

total: 10
passed: 10
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

Two bugs found and fixed during UAT:
1. Example place button slugs did not match actual place-index.json names (commit 44b2950)
2. Viewport filter used r.url (null on Pagefind stubs) instead of meta.title matching (commit 44b2950)
