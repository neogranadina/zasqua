---
phase: 10.2-explorer-parity
verified: 2026-04-12T00:23:27Z
status: human_needed
score: 13/13 must-haves verified
human_verification:
  - test: "Place explorer layout — open /lugares/ in browser"
    expected: "Explorer-grid renders: map filling left+centre of top row, selected place card in right column, viewport filter + place index below"
    why_human: "CSS grid layout requires visual inspection; cannot verify grid area assignment or responsive rendering with grep"
  - test: "Protomaps basemap renders on place explorer"
    expected: "Terrain-only tiles load — earth, water, administrative boundaries, and place labels visible; no roads, buildings, or POIs"
    why_human: "Tile network requests and rendered layer content require a live browser; cannot simulate CDN tile delivery"
  - test: "Selected place card on place explorer — click a circle on the map"
    expected: "Card in right column populates with place name, type badge, document count, any authority links (TGN/WHG), and 'Ver ficha' link; close button resets to stub"
    why_human: "Requires Pagefind index loaded, map rendered, and live click interaction"
  - test: "Empty state on first load — open /lugares/ with no URL params"
    expected: "Empty state overlay visible with 4 example place buttons (Santa Fe de Bogotá, Cartagena de Indias, Quito, Popayán); clicking one hides the overlay and highlights that place"
    why_human: "Requires live Pagefind index and map render to confirm the example-place lookup by normalised slug works"
  - test: "Viewport filter toggle on place explorer"
    expected: "Button toggles active state and label text changes between 'Filtrar por vista del mapa' and 'Filtrando por vista del mapa'; panning the map re-runs search when filter is active"
    why_human: "Requires live map interaction and Pagefind search execution"
  - test: "Protomaps basemap on place detail page — open any place with coordinates (e.g. /lugar/santa-fe-de-bogota/)"
    expected: "Terrain-only tiles render; burgundy dot marker visible at correct location; no old openfreemap tiles or errors"
    why_human: "Tile rendering requires live browser; cannot verify network response"
  - test: "Click-to-pin tooltip on place detail page marker"
    expected: "Tooltip appears above marker with place name and type; close button dismisses; panning map auto-dismisses"
    why_human: "Requires live map render and click interaction"
  - test: "Entity detail page accordion — open any entity detail page"
    expected: "Role filters render as collapsible accordion with 7 named groups; all groups collapsed on initial load; clicking a header expands it; group checkbox controls all members; 'Limpiar' button appears only when filters are active"
    why_human: "Accordion collapse state and tri-state checkbox behaviour require live JS execution and interaction"
  - test: "Focal node colour — open a person entity and a corporate/family entity detail page"
    expected: "Person focal node is burgundy (#8B2942); corporate body and family focal nodes are periwinkle (#6666BB)"
    why_human: "Graph rendering and colour assignment require a live bipartite graph render with real entity data"
  - test: "Ver en explorador button on entity detail page"
    expected: "Burgundy button with network icon visible in Control section; clicking it navigates to /entidades/?entidad=ne-XXXXX and the entity explorer loads that entity in the graph"
    why_human: "Cross-page navigation and entity explorer graph loading require live browser interaction"
---

# Phase 10.2: Explorer Parity Verification Report

**Phase Goal:** Bring place explorer and entity detail pages to visual and structural parity with the Phase 10.1 entity explorer redesign — Protomaps basemap, explorer-grid layout, selected card, empty state, viewport filter, role accordion, focal node colours, cross-nav button.
**Verified:** 2026-04-12T00:23:27Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Place explorer uses sidebar-left + map-right grid layout matching entity explorer | VERIFIED | `lugares.njk` line 36: `class="explorer-grid"`, line 22: `class="explorer-page"` — identical structural classes to `entidades.njk` |
| 2 | Selected place card is always reserved in the sidebar, not resizing the map | VERIFIED | `#selected-place-card.selected-entity-card` in `aside.explorer-selected-col` (template slot); JS populates it via `highlightPlace()` at line 388–440 of `place-explorer.js` |
| 3 | Place explorer shows Protomaps CDN Light terrain-only basemap (no roads, buildings, POIs, landuse) | VERIFIED | `buildTerrainStyle()` at line 133–156 of `place-explorer.js` filters `REMOVE = new Set(['roads', 'transit', 'buildings', 'pois', 'landuse', 'landcover'])`; URL `api.protomaps.com/tiles/v4.json` confirmed |
| 4 | Place explorer shows empty state with example places on first load | VERIFIED | `#map-empty-state.graph-empty-state` in template (line 44); 4 example `data-place` buttons at lines 56–60; `initEmptyState()` wires them in `place-explorer.js` at line 319 |
| 5 | Viewport filter toggle button matches entity explorer styling | VERIFIED | `#viewport-filter-toggle.viewport-filter-btn` at line 81 of `lugares.njk`; label toggles "Filtrar por vista del mapa" / "Filtrando por vista del mapa" at lines 363–364 and 484–485 of `place-explorer.js` |
| 6 | Place detail page map shows Protomaps CDN Light terrain-only basemap | VERIFIED | `place.js` lines 307–317: same REMOVE set, `api.protomaps.com` URL; `openfreemap` count = 0; `pmtiles` count = 0 |
| 7 | Clicking map marker on place detail page shows tooltip with place name and type | VERIFIED | `place.js` lines 354–389: `markerEl.addEventListener('click')` opens `.graph-tooltip.map-tooltip` with `escapeHtml(placeName)` + `escapeHtml(placeType)` content |
| 8 | Tooltip has a close button that dismisses it | VERIFIED | `place.js` line 361: `.map-tooltip-close` button calls `dismissTooltip()` on click |
| 9 | Tooltip dismisses automatically when user pans the map | VERIFIED | `place.js` line 389: `map.once('movestart', dismissTooltip)` |
| 10 | Entity detail page role filters use a collapsible 7-group accordion, not pill buttons | VERIFIED | `entity.js` line 43: `var roleGroups = [...]` (7 groups); `focal-role-facet`, `focal-role-group`, `focal-role-group-header`, `focal-role-group-chevron` class names present; `entity-role-btn` count = 0 |
| 11 | All accordion groups are collapsed on initial load | VERIFIED | `entity.js` line 303: `var expanded = someChecked` — groups only expand if a member is already in `activeRoles`; no unconditional `is-expanded` set on init |
| 12 | Roles not in any named group appear in an Otros bucket | VERIFIED | `entity.js` lines 218–230: ungrouped roles collected into `otrosMembers`; pushed as `{ id: 'otros', label_es: 'Otros' }` group |
| 13 | Focal entity node uses burgundy for persons and periwinkle for corporate/family | VERIFIED | `entity.js` line 405: `TYPE_MAP = { 'persona': 'person', 'institución': 'corporate_body', 'entidad corporativa': 'corporate_body', 'familia': 'family' }`; `entityColors` maps `person → #8B2942`, `corporate_body/family → #6666BB` |
| 14 | Entity detail page has a dedicated Ver en explorador button linking to /entidades/?entidad=CODE | VERIFIED | `entidad.njk` lines 141–146: `<a href="/entidades/?entidad={{ ent.entity_code }}" class="... bg-[#8B2942] ...">Ver en explorador</a>`; old "Red de entidades" label and "Explorar en la red de entidades" text both absent |

**Score:** 13/13 truths verified (all automated checks pass; visual/interactive verification still needed — see Human Verification section)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/explorar/lugares.njk` | Explorer-grid template matching entidades.njk structure | VERIFIED | Contains `explorer-grid`, `explorer-page`, `explorer-map`, `map-empty-state`, `selected-place-card`, `viewport-filter-toggle`, `data-protomaps-key`, `basemaps@5`; old `container font-sans` absent |
| `src/js/place-explorer.js` | Rewritten PlaceExplorer with sidebar, selected card, empty state, Protomaps basemap | VERIFIED | 1011 lines; `buildTerrainStyle`, `highlightPlace`, `api.protomaps.com`, `protomapsKey`, REMOVE set all present; `openfreemap` absent; auto-initialises via DOMContentLoaded at line 1008 |
| `src/_data/site.js` | Protomaps API key for basemap tiles | VERIFIED | Line 10: `protomaps_key: process.env.PROTOMAPS_KEY \|\| "YOUR_KEY_HERE"` |
| `src/js/place.js` | Protomaps terrain basemap and click-to-pin tooltip on map marker | VERIFIED | 400 lines; `buildTerrainStyle` inline IIFE, `dismissTooltip`, `movestart`, `map-tooltip`; `openfreemap` and `pmtiles` both absent |
| `src/lugar.njk` | Template with @protomaps/basemaps CDN script tag | VERIFIED | Line 214: `basemaps@5/dist/basemaps.js`; `data-place-name`, `data-place-type`, `data-protomaps-key` on `#place-map`; `pmtiles` import absent |
| `src/css/input.css` | CSS for map-tooltip close button | VERIFIED | Lines 3816–3837: `.map-tooltip`, `.map-tooltip-close`, `.map-tooltip-close:hover` |
| `src/js/entity.js` | Grouped accordion role facet, fixed focal node colour, cleaned cross-nav link | VERIFIED | 1026 lines; `roleGroups`, `groupedRoles`, `focal-role-facet`, `focal-role-group-chevron`, `Otros`, TYPE_MAP with `'familia'` and `'entidad corporativa'`; `entity-role-btn` absent |
| `src/entidad.njk` | Updated cross-navigation button with correct URL and styling | VERIFIED | Lines 141–146: burgundy `bg-[#8B2942]` button, `/entidades/?entidad={{ ent.entity_code }}`; "Red de entidades" and "Explorar en la red" absent |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/explorar/lugares.njk` | `src/js/place-explorer.js` | Script tag at line 112; `new PlaceExplorer(container)` at line 1010 of JS | WIRED | Script loaded; class auto-instantiated via DOMContentLoaded on `#place-explorer` container |
| `src/js/place-explorer.js` | `api.protomaps.com` | `buildTerrainStyle()` builds MapLibre style with Protomaps CDN tile URL | WIRED | Line 144: `url: 'https://api.protomaps.com/tiles/v4.json?key=' + this.protomapsKey` |
| `src/lugar.njk` | `src/js/place.js` | Script tag at line 219; JS reads `#place-map` data attributes | WIRED | Lines 295–304: `getElementById('place-map')`, `dataset.lat`, `dataset.lon`, `dataset.protomapsKey`, `dataset.placeName`, `dataset.placeType` |
| `src/js/place.js` | `api.protomaps.com` | Inline IIFE builds MapLibre style with Protomaps CDN tile URL | WIRED | Line 317: `url: 'https://api.protomaps.com/tiles/v4.json?key=' + apiKey` |
| `src/js/entity.js` | `roleGroups` accordion | `buildRoleFilters` replaced with accordion using 7-group taxonomy; `focal-role-group` class pattern | WIRED | Lines 43–58: array defined; lines 201–343: accordion rendered into `#entity-role-filters` |
| `src/entidad.njk` | `/entidades/?entidad=` | Deep-link button in Control section | WIRED | Line 141: `href="/entidades/?entidad={{ ent.entity_code }}"` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `place-explorer.js` | `allPlaces` (from place-index.json via Pagefind) | Pagefind search index, loaded client-side from pre-built static index | Yes — build-time JSON index | FLOWING |
| `place-explorer.js` → `#selected-place-card` | `place` record from `allPlaces` | Circle click or list click selects from live `allPlaces` array | Yes — real place data | FLOWING |
| `place.js` tooltip | `placeName`, `placeType` | `dataset.placeName`, `dataset.placeType` on `#place-map` div, populated from Nunjucks template | Yes — server-rendered from place data | FLOWING |
| `entity.js` accordion | `roleCounts` from `allLinks` | `allLinks` array populated from pre-built entity shard JSON | Yes — real document-link data | FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — all artifacts require a running browser with MapLibre GL, Pagefind WASM, and CDN tile access. No server-runnable entry points.

### Requirements Coverage

Phase 10.2 was declared a "polish phase" with no formal requirement IDs assigned. It contributes to informal EXP-06 and EXP-07 from REQUIREMENTS.md (both listed under "Phase 10, Pending"), but those are Phase 10 umbrella requirements not formally assigned to this sub-phase.

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| EXP-06 | Place explorer reflects updated 7,068 places with correct data and dynamic count | PARTIAL | Place explorer is rebuilt with new layout and correct data infrastructure; dynamic count (`#place-count-live`) is wired in `place-explorer.js`. Full satisfaction also depends on the place index data being current — outside this phase's scope. |
| EXP-07 | Protomaps CDN basemap fixed on place detail pages | VERIFIED | `openfreemap` fully removed; `api.protomaps.com` present in both `place.js` and `place-explorer.js` |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/_data/site.js` | 10 | `"YOUR_KEY_HERE"` placeholder for `protomaps_key` | Info | Intentional — guarded in `place-explorer.js` (line 213) with a plain-style fallback; documented as T-10.2-01 in plan. Maps will render without tiles until a real key is set in the `PROTOMAPS_KEY` env var. |

Note: `place-explorer.js` line 160 contains `placeholder = 'Buscar por nombre de lugar…'` — this is a valid HTML input placeholder attribute, not a code stub.

### Human Verification Required

All automated code checks pass. The following interactive behaviours require a browser to confirm.

#### 1. Place Explorer Layout

**Test:** Open `/lugares/` in browser
**Expected:** Explorer-grid renders with map in top-left area, selected place card in top-right column, viewport filter + search + facets in bottom-left, place results list in bottom-right — matching `/entidades/` layout
**Why human:** CSS grid area assignment and visual layout cannot be verified with grep

#### 2. Protomaps Basemap — Place Explorer

**Test:** Open `/lugares/` and observe the map panel
**Expected:** Terrain-only tiles load — earth tones, water bodies, administrative boundaries, and place name labels visible; no road lines, building footprints, or POI icons
**Why human:** Tile network request and rendered layer content require live browser with CDN access and a real API key set

#### 3. Selected Place Card Interaction

**Test:** Click any circle on the place explorer map (or any place in the results list)
**Expected:** Right-column card populates with place name, type badge, document count, TGN/WHG links (if present), and "Ver ficha →" link; close button (×) resets card to stub text
**Why human:** Requires Pagefind index loaded, map rendered, and live click event

#### 4. Empty State and Example Place Buttons

**Test:** Open `/lugares/` with no URL params (clear query string)
**Expected:** Empty state overlay is visible with four place buttons; clicking one (e.g. "Santa Fe de Bogotá") hides the overlay, highlights that place in the map, and shows its card
**Why human:** Requires live Pagefind index to look up place by normalised slug

#### 5. Viewport Filter Toggle

**Test:** Click the "Filtrar por vista del mapa" button on the place explorer
**Expected:** Button gains active state; label changes to "Filtrando por vista del mapa"; panning or zooming the map re-runs the search and updates the results list
**Why human:** Requires live map + Pagefind search interaction

#### 6. Protomaps Basemap — Place Detail Page

**Test:** Open any place detail page that has coordinates, e.g. `/lugar/santa-fe-de-bogota/`
**Expected:** Terrain-only Protomaps tiles render; burgundy dot marker visible at correct coordinates; no errors in console
**Why human:** Tile rendering requires live browser; API key must be set in environment

#### 7. Click-to-Pin Tooltip on Place Detail Marker

**Test:** Click the burgundy marker on a place detail page map
**Expected:** Tooltip appears above the marker showing place name (bold) and place type (muted); × close button dismisses it; panning the map also dismisses it
**Why human:** Requires live MapLibre render and click interaction

#### 8. Entity Detail Accordion Role Filter

**Test:** Open any entity detail page (e.g. `/entidad/ne-91872/`)
**Expected:** Role filters show as a collapsed accordion — no pill buttons; clicking a group header expands it; checking a role checkbox filters the timeline; "Limpiar" button appears only when at least one role is checked
**Why human:** Accordion expand/collapse, tri-state checkbox, and filter application require live JS with real shard data

#### 9. Focal Node Colour — Person vs. Corporate/Family

**Test:** Compare graph focal nodes: open a person entity and a corporate body/family entity
**Expected:** Person focal node rendered in burgundy (#8B2942); corporate body and family focal nodes rendered in periwinkle (#6666BB)
**Why human:** Requires live bipartite graph render with real entity type data from `.level-badge` text

#### 10. Ver en explorador Deep-Link

**Test:** Click the "Ver en explorador" button on any entity detail page
**Expected:** Browser navigates to `/entidades/?entidad=ne-XXXXX`; entity explorer opens and the entity is auto-loaded in the graph centred on that node
**Why human:** Cross-page navigation and entity explorer graph auto-load require live browser interaction

### Gaps Summary

No gaps found. All 13 must-have truths are verified in the codebase. All artifacts exist, are substantive, and are correctly wired. The `YOUR_KEY_HERE` placeholder in `site.js` is intentional (documented threat T-10.2-01, guarded with a plain-style fallback) and does not block goal achievement once the environment variable is set. The 10 human verification items above are the only remaining checks — they confirm interactive behaviour and visual rendering that cannot be assessed statically.

---

_Verified: 2026-04-12T00:23:27Z_
_Verifier: Claude (gsd-verifier)_
