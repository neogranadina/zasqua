---
phase: 10.2-explorer-parity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/explorar/lugares.njk
  - src/js/place-explorer.js
  - src/_data/site.js
autonomous: true
requirements: []

must_haves:
  truths:
    - "Place explorer uses sidebar-left + map-right grid layout matching entity explorer"
    - "Selected place card is always reserved in the sidebar, not resizing the map"
    - "Place explorer shows Protomaps CDN Light terrain-only basemap (no roads, buildings, POIs, landuse)"
    - "Place explorer shows empty state with example places on first load"
    - "Viewport filter toggle button matches entity explorer styling"
  artifacts:
    - path: "src/explorar/lugares.njk"
      provides: "Explorer-grid template matching entidades.njk structure"
      contains: "explorer-grid"
    - path: "src/js/place-explorer.js"
      provides: "Rewritten PlaceExplorer with sidebar, selected card, empty state, Protomaps basemap"
      contains: "buildTerrainStyle"
    - path: "src/_data/site.js"
      provides: "Protomaps API key for basemap tiles"
      contains: "protomaps_key"
  key_links:
    - from: "src/explorar/lugares.njk"
      to: "src/js/place-explorer.js"
      via: "PlaceExplorer class instantiated on #place-explorer container"
      pattern: "new PlaceExplorer"
    - from: "src/js/place-explorer.js"
      to: "api.protomaps.com"
      via: "MapLibre style using Protomaps CDN tiles"
      pattern: "api.protomaps.com"
---

<objective>
Redesign the place explorer page to mirror the entity explorer's 2-row grid layout (map + selected place card on top, filters + place index below), replace the openfreemap basemap with Protomaps CDN Light terrain-only style, add an empty state with example places, and wire a viewport filter toggle matching the entity explorer pattern.

Purpose: The place explorer currently uses a flat search-page layout with sidebar + content. This plan brings it to visual and structural parity with the Phase 10.1 entity explorer redesign (per D-01, D-02, D-03, D-04, D-05, D-06, D-07).

Output: Redesigned `/lugares/` page with explorer-grid layout, Protomaps terrain basemap, selected place card, empty state, and viewport filter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.2-explorer-parity/10.2-CONTEXT.md
@.planning/phases/10.2-explorer-parity/10.2-RESEARCH.md

@src/explorar/entidades.njk
@src/explorar/lugares.njk
@src/js/place-explorer.js
@src/js/entity-explorer.js
@src/css/main.css
@src/_data/site.js
@src/_data/ui.js

<interfaces>
<!-- Entity explorer template structure (the pattern to mirror): -->

From src/explorar/entidades.njk:
- `.explorer-page` wrapper (no `.container` — full viewport)
- `.explorer-page-header` with `h1.explorer-page-title` and `p.explorer-page-intro`
- `.explorer-grid` CSS grid with areas: graph | selected (row 1), filters | results (row 2)
- `#graph-empty-state.graph-empty-state` with `.is-active` class toggle
- `#focal-entity-card.selected-entity-card` in `.explorer-selected-col`
- `#viewport-filter-toggle.viewport-filter-btn` in `.explorer-filters-col`
- Viewport filter label toggles between "Filtrar por vista del explorador" / "Filtrando por vista del explorador"

From src/css/main.css (line 2895):
```css
.explorer-grid {
  display: grid;
  grid-template-columns: 280px 1fr 320px;
  grid-template-rows: auto auto;
  grid-template-areas: "graph graph selected" "filters results results";
  gap: 24px;
  align-items: start;
}
```

From src/js/entity-explorer.js (line 1088+):
- `highlightEntity(entityCode, entityMeta, shard)` builds selected card HTML
- Card uses classes: `selected-entity-card`, `selected-entity-header`, `selected-entity-name`, `selected-entity-badge`, `selected-entity-stat`, `selected-entity-footer`, `selected-entity-link`

From src/_data/ui.js:
```javascript
place: {
  types: { /* place_type -> Spanish label */ }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite lugares.njk template and add Protomaps API key to site.js</name>
  <files>src/explorar/lugares.njk, src/_data/site.js</files>
  <read_first>
    - src/explorar/entidades.njk (reference layout to mirror)
    - src/explorar/lugares.njk (current template to replace)
    - src/_data/site.js (add protomaps_key field)
    - src/css/main.css (explorer-grid CSS at line 2895)
  </read_first>
  <action>
**src/_data/site.js** — Add a `protomaps_key` field. The user needs to obtain a Protomaps API key from protomaps.com/dashboard and set it here. For now, add a placeholder:
```javascript
protomaps_key: process.env.PROTOMAPS_KEY || "YOUR_KEY_HERE"
```

**src/explorar/lugares.njk** — Replace the entire template body with the entity explorer's grid structure adapted for places. The template must:

1. Drop the `.container.font-sans` wrapper. Use `.explorer-page` wrapper (matching `entidades.njk`).

2. Add `explorer-page-header` with:
   - `h1.explorer-page-title`: "Explorar lugares"
   - `p.explorer-page-intro`: "Explora los **`<strong id="place-count-live">{{ places | length | numberFormat }}</strong>`** lugares vinculados a las descripciones de Zasqua. Busca por nombre, filtra por tipo de lugar o explora el mapa."

3. Add `.explorer-grid` div with exactly the same grid area structure as `entidades.njk`:

   **Row 1 left — map panel** (`section.explorer-graph-panel`):
   - `div#explorer-map` (map canvas, 100% width/height)
   - `div#map-empty-state.graph-empty-state` — empty state overlay (see Task 2 for JS wiring)
     - `.graph-empty-state-inner` with logo img (`/img/zasqua-3-burgundy-sm.svg`), text paragraph explaining the place explorer, and 4 example place buttons:
       ```html
       <button type="button" class="graph-empty-state-link" data-place="santa-fe-de-bogota">Santa Fe de Bogotá</button>,
       <button type="button" class="graph-empty-state-link" data-place="cartagena-de-indias">Cartagena de Indias</button>,
       <button type="button" class="graph-empty-state-link" data-place="quito">Quito</button>
       o
       <button type="button" class="graph-empty-state-link" data-place="popayan">Popayán</button>.
       ```
     - Empty state text: "Cada lugar del archivo se conecta con los documentos que lo mencionan. Selecciona un lugar para ver su ubicación en el mapa y sus documentos vinculados, o explora el índice de lugares más abajo."
     - Second paragraph: "Empieza con uno de estos ejemplos:" followed by buttons

   **Row 1 right — selected place card** (`aside.explorer-selected-col`):
   - `div#selected-place-card.selected-entity-card` (reuse same CSS class)
   - Stub content: `div.selected-entity-stub` text "Selecciona un lugar para ver más detalles"

   **Row 2 left — filters** (`aside.explorer-filters-col`):
   - Viewport filter button (copy from `entidades.njk` lines 83–93 verbatim, same `#viewport-filter-toggle.viewport-filter-btn` with SVG icon, but label "Filtrar por vista del mapa" / "Filtrando por vista del mapa")
   - `div#place-search-input.explorer-filter-search` (search input slot)
   - `div#sidebar-facets` (facets slot)

   **Row 2 right — results** (`div.explorer-results-col`):
   - `div#place-explorer` with `data-place-types='{{ ui.place.types | dump | safe }}'` and `data-protomaps-key='{{ site.protomaps_key }}'`

4. `{% block head %}` — Load MapLibre CSS+JS (already present), add `@protomaps/basemaps` CDN script:
   ```html
   <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/maplibre-gl@5/dist/maplibre-gl.css" />
   <script src="https://cdn.jsdelivr.net/npm/maplibre-gl@5/dist/maplibre-gl.js"></script>
   <script src="https://unpkg.com/@protomaps/basemaps@5/dist/basemaps.js" crossorigin="anonymous"></script>
   ```

5. `{% block scripts %}` — Load `place-explorer.js` and instantiate:
   ```html
   <script src="/js/place-explorer.js"></script>
   ```
   No inline wiring script needed — PlaceExplorer class handles everything internally (unlike entity explorer which wires graph + sidebar).
  </action>
  <verify>
    <automated>grep -c "explorer-grid" src/explorar/lugares.njk && grep -c "explorer-page" src/explorar/lugares.njk && grep -c "protomaps_key" src/_data/site.js && grep -c "basemaps@5" src/explorar/lugares.njk</automated>
  </verify>
  <acceptance_criteria>
    - src/explorar/lugares.njk contains `class="explorer-page"` (not `class="container font-sans"`)
    - src/explorar/lugares.njk contains `class="explorer-grid"`
    - src/explorar/lugares.njk contains `id="explorer-map"`
    - src/explorar/lugares.njk contains `id="selected-place-card"`
    - src/explorar/lugares.njk contains `id="map-empty-state"`
    - src/explorar/lugares.njk contains `id="viewport-filter-toggle"`
    - src/explorar/lugares.njk contains `class="viewport-filter-btn"`
    - src/explorar/lugares.njk contains `data-protomaps-key`
    - src/explorar/lugares.njk contains `basemaps@5/dist/basemaps.js`
    - src/explorar/lugares.njk does NOT contain `class="container font-sans"`
    - src/_data/site.js contains `protomaps_key`
  </acceptance_criteria>
  <done>Template restructured to explorer-grid layout matching entity explorer; Protomaps API key field added to site.js; basemaps CDN script loaded in head block.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite PlaceExplorer JS class for explorer-grid layout with Protomaps basemap, selected card, empty state, and viewport filter</name>
  <files>src/js/place-explorer.js</files>
  <read_first>
    - src/js/place-explorer.js (current implementation — 969 lines)
    - src/js/entity-explorer.js (reference for highlightEntity card, viewport filter, renderFocalRoleFacet)
    - src/explorar/entidades.njk (wiring pattern for viewport filter, empty state)
    - src/css/main.css (explorer-grid, selected-entity-card, viewport-filter-btn, graph-empty-state CSS)
    - src/_data/ui.js (place.types labels)
  </read_first>
  <action>
Rewrite `PlaceExplorer` class to work with the new template from Task 1. The class must NOT build the grid layout in `buildDOM()` — the template provides the grid structure. Instead, `buildDOM()` builds content INSIDE the template slots.

**Key changes to PlaceExplorer:**

**1. Constructor** — Read `data-protomaps-key` from the container element:
```javascript
this.protomapsKey = container.dataset.protomapsKey || '';
```

**2. `buildTerrainStyle()` method** (new) — Builds the Protomaps terrain-only MapLibre style object per D-05, D-06:
```javascript
buildTerrainStyle() {
  var REMOVE = new Set(['roads', 'transit', 'buildings', 'pois', 'landuse', 'landcover']);
  var allLayers = basemaps.layers('protomaps', basemaps.namedFlavor('light'), { lang: 'es' });
  var terrainLayers = allLayers.filter(function(l) { return !REMOVE.has(l['source-layer']); });
  return {
    version: 8,
    glyphs: 'https://protomaps.github.io/basemaps-assets/fonts/{fontstack}/{range}.pbf',
    sprite: 'https://protomaps.github.io/basemaps-assets/sprites/v4/light',
    sources: {
      protomaps: {
        type: 'vector',
        url: 'https://api.protomaps.com/tiles/v4.json?key=' + this.protomapsKey,
        attribution: '<a href="https://protomaps.com">Protomaps</a> \u00a9 <a href="https://openstreetmap.org">OpenStreetMap</a>'
      }
    },
    layers: terrainLayers
  };
}
```

**3. `buildDOM()`** — Instead of creating the grid, populate the template slots:
- Locate `#place-search-input` and inject the search `<input>` into it (same rounded search input style)
- Locate `#sidebar-facets` and render facets into it
- Locate `#place-explorer` (the results area) and build the results list, pagination, sort controls, and active filter pills inside it
- Locate `#selected-place-card` — the JS will populate this when a place is selected

**4. `initMap()`** — Change the style from `'https://tiles.openfreemap.org/styles/liberty'` to `this.buildTerrainStyle()`. Container is `'explorer-map'` (from the template). Keep the same heatmap+circle layers, same click popup, same cursor handlers.

**5. `highlightPlace(placeData)` method** (new, per D-04) — Mirrors `EntityExplorer.highlightEntity`. When a place circle is clicked on the map OR a place is clicked in the results list:
- Populate `#selected-place-card` with HTML using the SAME CSS classes as the entity card:
  ```html
  <div class="selected-entity-header">
    <h3 class="selected-entity-name">{place.display_name}</h3>
    <span class="selected-entity-badge">{placeTypes[place.place_type] || place.place_type}</span>
    <button type="button" class="selected-entity-close" aria-label="Cerrar">&times;</button>
  </div>
  <div class="selected-entity-stat">{n} documentos vinculados</div>
  {authority links section — only if TGN or WHG exist:}
  <div class="selected-entity-footer">
    {if tgn_id: <a href="https://vocab.getty.edu/page/tgn/{tgn_id}" target="_blank" rel="noopener" class="selected-entity-link">TGN {tgn_id}</a>}
    {if whg_id: <a href="https://whgazetteer.org/places/{whg_id}/portal" target="_blank" rel="noopener" class="selected-entity-link">WHG {whg_id}</a>}
  </div>
  <a href="/lugar/{slug}/" class="selected-entity-link" style="display:block;margin-top:0.5rem">Ver ficha &rarr;</a>
  ```
- Close button clears the card back to stub content
- Pan/zoom the map to the selected place's coordinates

**6. Empty state** (per D-03) — On init, if no URL params are present:
- Add `.is-active` class to `#map-empty-state`
- Wire click handlers on `button[data-place]` elements — each button triggers selecting/highlighting that place (look up from `allPlaces` by slug match or name match)
- Hide the empty state once any place is selected

**7. Viewport filter** (per D-03) — Wire the `#viewport-filter-toggle` button:
- On click, toggle `this.state.mapBound`
- Toggle `.is-active` class on the button
- Update `aria-pressed`
- Change label text: "Filtrar por vista del mapa" / "Filtrando por vista del mapa"
- Trigger `this.search()` with `state.page = 1`
- On map `moveend`, if viewport filter is active, debounce a re-search (250ms)

**8. Place count update** — After Pagefind init, update `#place-count-live` with the actual place count from the index (same pattern as entity explorer's `#entity-count-live`).

**9. Results list click** — When a place in the results list is clicked, call `highlightPlace()` with that place's data AND pan the map to its coordinates (if it has coords). This is the bidirectional map-list sync.

**Important implementation notes:**
- The search `<input>` goes inside `#place-search-input` div in the filters column (NOT above the grid as currently)
- Active filter pills, sort toggle, and results info bar go inside the `#place-explorer` results column
- Keep all existing Pagefind search, facet, URL state, pagination logic — just relocate where the DOM elements are inserted
- The `search-layout` class is no longer used — all layout is from `.explorer-grid`
- Keep the `escapeHtml` helper function
  </action>
  <verify>
    <automated>grep -c "buildTerrainStyle" src/js/place-explorer.js && grep -c "highlightPlace" src/js/place-explorer.js && grep -c "api.protomaps.com" src/js/place-explorer.js && grep -c "openfreemap" src/js/place-explorer.js | grep "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - src/js/place-explorer.js contains `buildTerrainStyle` method
    - src/js/place-explorer.js contains `highlightPlace` method
    - src/js/place-explorer.js contains `api.protomaps.com`
    - src/js/place-explorer.js does NOT contain `openfreemap` or `tiles.openfreemap.org`
    - src/js/place-explorer.js does NOT contain `class="search-layout"` or `className = 'search-layout'`
    - src/js/place-explorer.js contains `protomapsKey` or `protomaps_key`
    - src/js/place-explorer.js contains `map-empty-state` or `graph-empty-state`
    - src/js/place-explorer.js contains `viewport-filter-toggle`
    - src/js/place-explorer.js contains `selected-place-card` or `selected-entity-card`
    - src/js/place-explorer.js contains `REMOVE` set with `'roads', 'transit', 'buildings', 'pois', 'landuse', 'landcover'`
  </acceptance_criteria>
  <done>PlaceExplorer class rewritten: Protomaps terrain-only basemap replaces openfreemap; selected place card populates sidebar on circle/list click; empty state with 4 example places shows on first load; viewport filter toggle wired with map-bound search re-run; search input and facets render into template grid slots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Protomaps CDN | API key sent in tile requests (frontend credential, CORS-restricted) |
| Browser → Pagefind WASM | Client-side search index loaded from same origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10.2-01 | I (Information Disclosure) | Protomaps API key in HTML | accept | Frontend-only key; CORS is the security model per Protomaps docs. Key is visible in page source by design. |
| T-10.2-02 | T (Tampering) | place-index.json | accept | Static file served from same origin; no user input modifies it |
</threat_model>

<verification>
1. Open `/lugares/` in browser — verify explorer-grid layout (map top-left, selected card top-right, filters bottom-left, index bottom-right)
2. Verify Protomaps basemap renders (no roads, no buildings, no POIs — only earth, water, boundaries, place labels)
3. Click a circle on the map — verify selected place card populates with name, type badge, doc count, authority links, "Ver ficha" link
4. Click close button on selected card — verify card returns to stub
5. On first load without URL params — verify empty state overlay with 4 example place buttons
6. Click an example place button — verify empty state hides and place loads
7. Click viewport filter toggle — verify button toggles active state and label text changes
8. Pan/zoom map with viewport filter active — verify results list updates
</verification>

<success_criteria>
Place explorer at `/lugares/` mirrors entity explorer layout: Protomaps terrain-only basemap, sidebar-left with filters + place index, map filling main area, selected place card in dedicated column, empty state on first load, viewport filter synced to map bounds.
</success_criteria>

<output>
After completion, create `.planning/phases/10.2-explorer-parity/10.2-01-SUMMARY.md`
</output>
