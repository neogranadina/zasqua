---
phase: 12-place-explorer-and-place-detail-page-rework
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/lugar.njk
  - src/js/place.js
autonomous: false
requirements:
  - D-11
  - D-12
  - D-13
  - D-14
  - D-20
  - D-21
  - D-23
must_haves:
  truths:
    - "Place detail page shows map (when available) always visible at top of aside -- no toggle"
    - "Below the map, linked descriptions are shown as a scrollable list with title, date, and role"
    - "Sort toggle switches between chronological (default) and alphabetical"
    - "Undated descriptions appear at the bottom under a Sin fecha group in chronological sort"
    - "Places without coordinates show Ubicacion no disponible placeholder then description list"
  artifacts:
    - path: "src/lugar.njk"
      provides: "Place detail template with always-visible map and description list"
      contains: "place-description-list"
    - path: "src/js/place.js"
      provides: "Place detail JS without toggle logic, with sort state and description list rendering"
      contains: "sortMode"
  key_links:
    - from: "src/js/place.js"
      to: "place-links shard"
      via: "fetch /data/place-links/{id}.json"
      pattern: "place-links.*\\.json"
    - from: "src/lugar.njk"
      to: "src/js/place.js"
      via: "script tag"
      pattern: "place\\.js"
---

<objective>
Refactor the place detail page layout: remove the broken map/timeline segmented toggle, make the map always visible when coordinates exist, and show linked descriptions as a sorted scrollable list below the map.

Purpose: The current place detail page has a segmented map/timeline toggle that is broken (the `mapEl` variable reference is out of scope, so `currentView` always defaults to `'timeline'`). D-11 through D-14 replace this toggle with a simpler, always-visible layout: map at the top of the aside (when coords available), description list below it.

Output: Modified `lugar.njk` template and `place.js` script with the new layout and sort functionality.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-place-explorer-and-place-detail-page-rework/12-CONTEXT.md
@.planning/phases/12-place-explorer-and-place-detail-page-rework/12-RESEARCH.md
@.planning/phases/12-place-explorer-and-place-detail-page-rework/12-01-SUMMARY.md

@src/lugar.njk
@src/js/place.js

<interfaces>
<!-- Current lugar.njk aside structure (lines ~80-170 approximately) -->
Key template elements:
- #place-view-toggle -- segmented toggle with data-view="map" and data-view="timeline" buttons (TO BE REMOVED)
- #place-map-frame -- contains #place-map MapLibre container (TO KEEP, always visible)
- #place-timeline-frame -- hidden by default, contains #place-timeline (TO RENAME/restructure)
- #place-intro -- inline "Ver en un mapa, como una linea de tiempo" links (TO BE REMOVED)
- #place-role-filters -- role filter pills (TO KEEP)

<!-- Current place.js structure -->
First IIFE (async, line 14-):
- Loads place-links shard via fetch
- var currentView = mapEl ? 'map' : 'timeline'  (BUG: mapEl undefined in this scope)
- buildIntro(links) -- creates view-switch links (TO BE REMOVED)
- buildRoleFilters(links) -- creates role filter pills (TO KEEP)
- wireToggleButtons() -- wires segmented toggle (TO BE REMOVED)
- renderTimeline(el, links, activeRoles) -- renders description entries with timeline CSS (TO REUSE as description list)

Second IIFE (line ~290-):
- var mapEl = document.getElementById('place-map') -- map initialization
- Map init with Protomaps basemap (TO KEEP as-is)

renderTimeline internals (reusable):
- Separates links into dated and undated arrays
- Sorts dated by date_expression.localeCompare()
- Creates timeline entries with: title link, date, role label
- Appends "Sin fecha" divider for undated entries

Place link shard record structure:
{
  "desc_id": "co-agn-saa-ii-17-13-d16",
  "title": "Titulo de la descripcion",
  "date_expression": "1580-01-15",
  "role": "mentioned"
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor place detail layout -- always-visible map, description list with sort</name>
  <files>src/lugar.njk, src/js/place.js</files>
  <read_first>
    src/lugar.njk
    src/js/place.js
    src/_data/ui.js
    .planning/phases/12-place-explorer-and-place-detail-page-rework/12-RESEARCH.md
  </read_first>
  <action>
**Template changes (lugar.njk):**

1. Find and remove the `#place-view-toggle` element entirely -- the segmented toggle with `data-view="map"` and `data-view="timeline"` buttons.

2. Make `#place-map-frame` always visible -- remove any `style="display: none"` or conditional hiding. Keep it wrapped in the existing `{% if place.latitude and place.longitude %}` Nunjucks conditional so it only renders when coordinates exist.

3. For places WITHOUT coordinates (D-14), add a placeholder notice at the top of the aside:
```html
{% if not (place.latitude and place.longitude) %}
<div class="place-no-coords-notice" style="padding:1rem;background:var(--color-stone-100);border-radius:0.5rem;margin-bottom:1rem">
  <p class="text-sm text-stone-500" style="margin:0">Ubicaci&oacute;n no disponible para este lugar.</p>
</div>
{% endif %}
```

4. Replace `#place-timeline-frame` with a description list container. Change the element id to `place-description-list` (or add it alongside). Remove `style="display: none"` so it is always visible. The structure:
```html
<div id="place-description-list-wrapper">
  <div id="place-description-sort" style="display:flex;align-items:center;justify-content:space-between;margin-bottom:0.75rem">
    <span class="text-sm font-medium text-stone-700" id="place-desc-count"></span>
    <div class="sort-toggle" style="display:flex;gap:0.5rem">
      <button type="button" class="sort-btn is-active" data-sort="chronological">Cronol&oacute;gico</button>
      <button type="button" class="sort-btn" data-sort="alphabetical">Alfab&eacute;tico</button>
    </div>
  </div>
  <div id="place-timeline" data-place-id="{{ place.id }}"></div>
</div>
```

Keep `#place-timeline` as the container element id that `place.js` uses for rendering (renaming internally is fine but the JS targets this id).

5. Remove the `#place-intro` element if it exists in the template (the inline view-switch links are no longer needed).

6. Keep `#place-role-filters` as-is -- role filter pills remain.

**JavaScript changes (place.js):**

7. Remove the `currentView` variable declaration (`var currentView = mapEl ? 'map' : 'timeline'`) and all references to `currentView`. This fixes the scoping bug where `mapEl` was undefined.

8. Remove `wireToggleButtons()` function and its call. Remove `switchView()` function.

9. Remove `buildIntro()` function and its call. The intro sentence with view-switch links is no longer needed.

10. Add sort state and sort toggle wiring. After loading the shard and building role filters:
```javascript
var sortMode = 'chronological';  // default per D-13

// Wire sort buttons
var sortBtns = document.querySelectorAll('#place-description-sort .sort-btn');
sortBtns.forEach(function(btn) {
  btn.addEventListener('click', function() {
    sortMode = btn.getAttribute('data-sort');
    sortBtns.forEach(function(b) { b.classList.remove('is-active'); });
    btn.classList.add('is-active');
    renderDescriptionList(timelineEl, links, activeRoles, sortMode);
  });
});
```

11. Refactor `renderTimeline()` into `renderDescriptionList(el, links, activeRoles, sortMode)`:
   - When `sortMode === 'chronological'` (default): same as current -- separate dated/undated, sort dated by `date_expression.localeCompare()` ascending (oldest first), append undated at bottom under a "Sin fecha" group heading
   - When `sortMode === 'alphabetical'`: sort ALL entries (dated + undated together) by `link.title.localeCompare('', 'es')`, no "Sin fecha" group separator
   - Each entry per D-12: title as a link to `/{{ link.desc_id }}/`, date if available (formatted same as current), role label from `placeRoleLabels[link.role]`

12. Update the description count display. After loading the shard, set:
```javascript
var countEl = document.getElementById('place-desc-count');
if (countEl) {
  countEl.textContent = links.length + ' ' + (links.length === 1 ? 'descripci\u00f3n vinculada' : 'descripciones vinculadas');
}
```

13. Update the initial render call from `renderTimeline(timelineEl, links, activeRoles)` to `renderDescriptionList(timelineEl, links, activeRoles, sortMode)`.

14. Update role filter pill click handlers to call `renderDescriptionList(timelineEl, links, activeRoles, sortMode)` instead of `renderTimeline(...)`.

15. Add CSS for the sort buttons (inline or in the template). The `.sort-btn` class should match the existing sort toggle styling used in the explorer pages:
```css
.sort-btn {
  font-size: 0.8rem;
  padding: 0.25rem 0.75rem;
  border: 1px solid var(--color-stone-300);
  border-radius: 0.25rem;
  background: transparent;
  color: var(--color-stone-600);
  cursor: pointer;
}
.sort-btn.is-active {
  background: var(--color-burgundy);
  color: #fff;
  border-color: var(--color-burgundy);
}
```
Add these to `src/css/main.css` if they don't already exist, or use inline styles in the template if the project prefers avoiding CSS additions for small features.
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && grep -c "place-view-toggle" src/lugar.njk | grep "^0$" && grep -c "wireToggleButtons" src/js/place.js | grep "^0$" && grep -c "switchView" src/js/place.js | grep "^0$" && grep "sortMode" src/js/place.js && grep "Ubicaci" src/lugar.njk && grep "place-description-sort" src/lugar.njk</automated>
  </verify>
  <acceptance_criteria>
    - src/lugar.njk does NOT contain `place-view-toggle` (segmented toggle removed)
    - src/lugar.njk does NOT contain `data-view="map"` or `data-view="timeline"` buttons
    - src/lugar.njk contains `Ubicaci` in a no-coords notice (D-14 placeholder)
    - src/lugar.njk contains `place-description-sort` div with sort buttons
    - src/lugar.njk contains `data-sort="chronological"` and `data-sort="alphabetical"` buttons
    - src/js/place.js does NOT contain `wireToggleButtons` function
    - src/js/place.js does NOT contain `switchView` function
    - src/js/place.js does NOT contain `buildIntro` function (view-switch links removed)
    - src/js/place.js does NOT contain `currentView` variable
    - src/js/place.js contains `sortMode` variable initialised to `'chronological'`
    - src/js/place.js contains `renderDescriptionList` function (or equivalent renamed renderTimeline)
    - src/js/place.js contains `localeCompare` for alphabetical sorting
    - src/js/place.js contains `Sin fecha` group heading for undated entries in chronological mode
    - src/lugar.njk contains `place-timeline` element with `data-place-id` attribute (render target preserved)
  </acceptance_criteria>
  <done>Place detail page shows map always visible at top (when coords available), description list always visible below with sort toggle. No segmented map/timeline toggle. "Ubicacion no disponible" notice for places without coordinates. Sort switches between chronological and alphabetical.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional verification of all Phase 12 changes</name>
  <files>src/explorar/lugares.njk, src/js/place-explorer.js, src/lugar.njk, src/js/place.js</files>
  <action>
Verify all Phase 12 changes across both the place explorer and place detail pages. Start the dev server and walk through each verification step.

Place explorer rework (Plan 01): clustered markers, filter sync, checkbox fix, index click selection, card simplification.
Place detail rework (Plan 02 Task 1): always-visible map + description list, sort toggle, no segmented toggle.

Steps:
1. Run `npm run build` (or `npx @11ty/eleventy --serve`) and open the local dev server

**Place explorer (/lugares/):**
2. Page loads with clustered burgundy markers on the map -- no splash screen overlay
3. Zoom out -- cluster circles show place counts
4. Click a cluster -- map zooms in to show children
5. Click an individual marker -- sidebar card shows name, type badge, document count, "Ver ficha" link (NO authority IDs like TGN/WHG)
6. Click a place name in the index list -- map centres on it and sidebar card populates. Does NOT navigate to detail page
7. Click "Ver ficha" in the card -- navigates to the place detail page
8. Type "Bogota" in search -- index and map markers both filter to matching places
9. Check a type filter checkbox (e.g. "Ciudad") -- checkbox stays checked, index and map update
10. Example buttons in header text ("Santafe de Bogota", "Cartagena", etc.) select places

**Place detail page (e.g. /nl-94850/ or any place with coordinates):**
11. Map is visible at top of aside -- no segmented toggle
12. Below the map, linked descriptions shown as a list with title (linked), date, role label
13. Sort toggle: "Cronologico" active by default. Click "Alfabetico" -- list re-sorts by title
14. Undated entries appear at bottom under "Sin fecha" in chronological mode

**Place without coordinates (find one in the index with no pin icon):**
15. Detail page shows "Ubicacion no disponible" notice where map would be
16. Description list still shows below

**Data verification:**
17. Check total place count shown in explorer header -- should reflect current data count
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User has visually verified all Phase 12 changes and approved them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-only | Static template and client-side JS changes -- no server, no auth, no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | I (Information Disclosure) | place-links shards | accept | Public archival data -- intentionally accessible. No PII or sensitive data in place/description links |
</threat_model>

<verification>
1. Place detail page for a place with coordinates: map visible, description list below, sort toggle works
2. Place detail page for a place without coordinates: "Ubicacion no disponible" notice, description list below
3. Sort toggle: chronological (default, oldest first, Sin fecha at bottom) and alphabetical (all entries sorted by title)
4. Role filter pills still work and re-render the description list
5. `npm run build` completes without errors
</verification>

<success_criteria>
- Place detail page shows always-visible map + description list (no toggle)
- Sort toggle works for chronological and alphabetical
- Places without coordinates show placeholder notice
- Role filter pills still function
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/12-place-explorer-and-place-detail-page-rework/12-02-SUMMARY.md`
</output>
