---
phase: 09-entity-network-graph
task: null
total_tasks: null
status: in_progress
last_updated: 2026-04-03T23:50:31.112Z
---

<current_state>
Phase 9 has expanded well beyond the original entity network graph scope. This session focused on Figma-driven redesign of entity and place detail pages, plus integrating updated place data from zasqua-entities v1.4 audit. 7 commits made. No uncommitted work. An Eleventy build may still be running in the background (task bsefjh56r).
</current_state>

<completed_work>

**Entity page redesign (from Figma):**
- Segmented toggle (timeline/graph) with SVG icons — timeline first, graph second
- Left-aligned vertical timeline with dots, cards containing date/role badge/title/ref
- Inline explorer link after role filter pills
- Commits: 25e703b, 925028d

**Place data integration:**
- places.json updated: 7,068 places (was 8,177) with TGN links, country codes, audited authorities
- place_links.json updated: 193,892 records with new place IDs
- place-index.json regenerated with correct linked counts (fixed string/int ID mismatch)
- All three files uploaded to B2 (zasqua-export bucket)
- Commits: 84d2745

**Place page redesign (from Figma + parity with entity pages):**
- Timeline view with segmented toggle (map/timeline) and role filter pills
- TGN authority pill, country name (Intl.DisplayNames filter), remove redundant Type field
- Full-width authority rows with codes and external link icons (Figma style)
- Control section always visible with Neogranadina ID, coordinates as metadata field
- IIFE pattern for place.js (DOMContentLoaded doesn't fire for scripts at bottom of body)
- Map init isolated from main logic to prevent CDN failures from killing timeline
- Commits: 84d2745, e30c7df, 279bf64, 69e1690, 3bf8386
</completed_work>

<remaining_work>

**Immediate:**
- Fix map tiles: Protomaps CDN style URL (cdn.protomaps.com/basemaps/v4/en.json) returns fetch error — maps show burgundy marker but no basemap
- Apply full-width authority row style to entity pages too (currently only on place pages)

**Design decisions needed:**
- Entity explorer UX: graph is meant as a discovery tool (like the place map), but current implementation is broken — 50vh panel above everything, slow shard fetches. Options discussed: B (view mode toggle) or C (deep-link ego-network only)
- Description pages: need links to entity/place detail pages and explorer views. Requires build-time enrichment (desc-entity-lookup.json and desc-place-lookup.json exist with 88K/84K keys but only carry codes, not names)
- Whether description-to-entity/place links is v0.5.0 scope or deferred to v0.6.0

**Cleanup + close-out:**
- Clean up unused files (precompute-bipartite-graph.js, entity-doc-graph.json, old Sigma/graphology refs from entidades.njk)
- Place explorer UX improvements (now unblocked by place data update)
- Full site rebuild + deploy
- v0.5.0 close-out (version bump, deploy, changelog, release)

**Upstream:**
- Tuquerres wrongly listed as Abancay variant — prompt written for zasqua-entities
- Entity data audit from parallel zasqua-entities session not yet ready
</remaining_work>

<decisions_made>

- Segmented toggle follows Figma design — rounded pill shape (border-radius: 50px per shared visual language), burgundy active state, periwinkle hover
- Timeline first in toggle order, graph/map second — timeline is the default view
- Place pages get timeline view with same design as entity pages — both are discovery tools
- Authority identifiers shown as full-width rows with codes (Figma style), not compact pills
- Country codes shown as full Spanish names (Intl.DisplayNames API)
- Coordinates in Control section metadata field, not floating under map
- Control section always visible on place pages with numeric ID as Neogranadina identifier
- Place.js uses IIFE pattern because DOMContentLoaded already fired by script load time
- Map init isolated in separate IIFE — MapLibre CDN failures must not kill timeline/filter logic
- Local Eleventy builds need NODE_OPTIONS=--max-old-space-size=8192 (206K pages OOM at default)
</decisions_made>

<blockers>
- Protomaps CDN basemap failing — maps render marker only, no tiles. Need to investigate URL, API key requirements, or switch to alternative tile source
- Entity explorer UX direction not decided — blocking that work stream
- Tuquerres/Abancay variant merge — upstream data fix needed
</blockers>

<context>
Design work driven by Figma Make file LBeOKVLOkLXsJRe6zYdLyt (Visual identity for Zasqua v2). EntityPage.tsx and PlacePage.tsx in that file are the reference designs. The entity page redesign is implemented and committed. The place page redesign is implemented and committed but the map tiles aren't loading (CDN issue, not code issue — the marker renders fine). The data-pipeline.md in workspace docs is outdated and should be updated when closing v0.5.0. The place data integration surfaced an ID consistency issue between places.json and place_links.json (different ID systems from different export runs) — this was resolved by re-running precompute-links.js after both files were consistent. There are 13 open GitHub issues in juancobo/zasqua, including frontend issues #9 (AHRB viewers) and #7 (IIIF info.json).
</context>

<next_action>
1. Fix the Protomaps CDN map tile issue — check if cdn.protomaps.com/basemaps/v4/en.json needs an API key or if the URL has changed
2. Apply full-width authority row style to entity pages for parity
3. Decide on entity explorer UX direction
4. Decide on description-to-entity/place links scope
</next_action>
