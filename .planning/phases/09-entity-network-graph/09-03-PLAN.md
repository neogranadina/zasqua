---
phase: 09-entity-network-graph
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified: []
autonomous: false
requirements: [GRAPH-01, GRAPH-02, GRAPH-03, GRAPH-04]

must_haves:
  truths:
    - "Network graph renders with visible nodes and edges on the entity explorer page"
    - "Hovering a node shows entity name and document count in a tooltip"
    - "Single-clicking a node highlights its ego-network and fades other nodes"
    - "Double-clicking a node navigates to the entity detail page"
    - "Role filter checkboxes hide/show edges by relationship type"
    - "Search/facet changes in the entity explorer suppress non-matching nodes in the graph"
    - "On mobile the graph panel collapses and expands via toggle"
  artifacts: []
  key_links: []
---

<objective>
Visual and functional verification of the complete network graph implementation.

Purpose: Confirm that the graph renders correctly, interactions work as specified, and filter synchronisation with the entity explorer is functional. This is a human verification checkpoint — all code was written in Plans 01 and 02.

Output: Verification confirmation or issue list for remediation.
</objective>

<execution_context>
@/Users/juancobo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juancobo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-entity-network-graph/09-CONTEXT.md
@.planning/phases/09-entity-network-graph/09-UI-SPEC.md
@.planning/phases/09-entity-network-graph/09-01-SUMMARY.md
@.planning/phases/09-entity-network-graph/09-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual and functional verification of network graph</name>
  <files>src/js/entity-network-graph.js, src/explorar/entidades.njk, src/css/input.css</files>
  <read_first>
    - .planning/phases/09-entity-network-graph/09-UI-SPEC.md (visual reference)
    - .planning/phases/09-entity-network-graph/09-CONTEXT.md (decisions to verify against)
  </read_first>
  <action>
    This is a human verification checkpoint. Start the dev server with `npm run dev` and present the following verification checklist to the user.

    **What was built (Plans 01 + 02):**
    - Graph panel with pre-computed ForceAtlas2 layout (Plan 01)
    - EntityNetworkGraph class with Sigma.js rendering (Plan 02)
    - Ego-network expansion, tooltips, double-click navigation
    - Role filter checkboxes (Productor, Colaborador, Editor, Materia, Mencionado)
    - Filter synchronisation with entity explorer search/facets
    - Mobile-collapsible graph panel

    **Verification steps for user:**

    1. **Graph renders (GRAPH-01, GRAPH-02):** Visit http://localhost:8080/explorar/entidades/ — verify graph panel appears above results with heading "Red de co-ocurrencia", nodes visible with type colours (burgundy/blue/brown), edges connect nodes, starter network shows ~100 nodes.

    2. **Node interactions (GRAPH-03):** Hover a node — tooltip with name + count. Single-click — ego-network expands. Click canvas — ego-network collapses. Double-click — navigates to /entidad/{code}/.

    3. **Edge interactions:** Hover an edge — tooltip shows both names, total docs, role-pair breakdown.

    4. **Role filters:** Uncheck "Productor" — edges disappear. Nodes losing all edges also fade. Re-check — they return.

    5. **Filter sync (GRAPH-04):** Type a name in search — non-matching nodes fade. Select entity type facet — other type nodes fade. Clear filters — all restore.

    6. **Mobile (resize to less than 768px):** Graph collapsed by default. "Mostrar red de entidades" expands. "Ocultar red de entidades" collapses.

    7. **Reset view:** Zoom/pan, then click "Restablecer vista" — camera resets.

    Wait for user response: "approved" or issue description.
  </action>
  <verify>
    <automated>cd /Users/juancobo/Databases/zasqua/zasqua-frontend-dev && npx @11ty/eleventy --dryrun 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Graph panel is visible at http://localhost:8080/explorar/entidades/ with heading "Red de co-ocurrencia"
    - At least 50 nodes are rendered with colour coding
    - Hover tooltip appears on nodes with entity name and count
    - Single click expands ego-network (neighbours highlight)
    - Double-click navigates to entity detail page
    - Role filter checkboxes control edge visibility
    - Search query suppresses non-matching nodes
    - Mobile toggle shows/hides graph panel
    - "Restablecer vista" resets camera zoom
  </acceptance_criteria>
  <done>User confirms all 7 verification areas pass with no visual regressions or console errors.</done>
</task>

</tasks>

<verification>
All four GRAPH requirements verified through manual interaction:
- GRAPH-01: Network graph showing co-occurrence renders on entity explorer page
- GRAPH-02: Nodes positioned from pre-computed ForceAtlas2 layout (no force simulation in browser)
- GRAPH-03: Click expands ego-network, hover shows tooltip, double-click navigates
- GRAPH-04: Graph filters in sync with entity explorer search/facet state
</verification>

<success_criteria>
- User confirms all 7 verification areas pass
- No visual regressions on the entity explorer list view
- No console errors during normal interaction
</success_criteria>

<output>
After completion, create `.planning/phases/09-entity-network-graph/09-03-SUMMARY.md`
</output>
